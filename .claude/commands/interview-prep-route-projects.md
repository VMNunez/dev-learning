---
description: Build the cross-project interview study route over the project question banks
argument-hint: MODE=update|dry-run
---

Read `notes/prompts/knowledge/interview-prep/interview-prep-route-projects-prompt.md` and execute it in full as the orchestrator it describes, running inside Claude Code.

Configuration from the user: $ARGUMENTS

If configuration is incomplete, ask only for the missing config-block value. Default MODE to `dry-run`.
