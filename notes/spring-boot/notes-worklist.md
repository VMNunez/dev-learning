# Worklist — Spring Boot (+ Java)   (generated by notes-plan · 2026-07-13)

Each item is one run of the four-stage pipeline (author → reviewer → translator → Spanish reviewer).
The checkbox is flipped to [x] by the Spanish reviewer (stage C) when that file's build finishes. The
orchestrator deletes this file once every row is [x].

Scope note: this run covered NOTES_PATH = notes/java/en/ and notes/spring-boot/en/ together (Spring
Boot code leans on Java language concepts). No gaps were found in notes/java/en/ against
notes/java/coverage.md — all 14 coverage sections already map to an existing numbered file (01–15).
Every gap below is in notes/spring-boot/en/.

- [x] #1 · notes/spring-boot/en/11-business-logic-domain-modeling.md
      TASK = create-file. Rule-1 gap: coverage.md "Business logic and domain modelling" (anemic vs
      rich domain model, guarding the state machine in one place / canTransitionTo(), where a derived
      value like hours-worked is computed) has no home in any existing file — grep for
      "canTransitionTo|state machine|anemic|rich domain" only turns up an incidental mention in
      02-rest-controllers.md. Coverage.md calls the state-machine question "the highest-signal design
      question in the TimeEntry workflow" — directly relevant to the DRAFT→SUBMITTED→APPROVED/REJECTED
      workflow Victor is building right now in TimeEntryService (project 07, step 5, in progress).
      Thread: continues from 08-transactions.md (business rules need a transaction boundary) and
      05-exception-handling.md (illegal transitions raise a custom exception); sets up 12 (a bad state
      transition is exactly the kind of bug you find via production debugging).
      REWRITE_MODE = first-pass

- [x] #2 · notes/spring-boot/en/12-production-debugging.md
      TASK = create-file. Rule-1 gap: coverage.md has two full sections, "Production debugging —
      startup failures" and "Production debugging — runtime failures and performance" (APPLICATION
      FAILED TO START, port-in-use, UnsatisfiedDependencyException, HikariPool init failure,
      localhost-inside-a-container, ddl-auto=validate drift, Whitelabel Error Page, slow-endpoint
      checklist, /actuator/metrics, CORS-only-fails-from-the-browser, OutOfMemoryError) — grep confirms
      these terms only appear scattered (Whitelabel/actuator/HikariPool touched incidentally in
      01-basics.md, 02-rest-controllers.md, 06-security-jwt*.md) with no dedicated file walking the
      "app won't start" vs "app is slow/wrong" diagnosis flow. Merge both coverage sections into one
      file — they are the same skill (reading the evidence in front of you) at two different moments.
      Thread: continues from 10-tooling.md (Docker/Compose failure modes) and 04-spring-data-jpa.md
      (N+1, indexing); sets up 13 (once you can diagnose a failure, logging is how you find the
      evidence in the first place).
      REWRITE_MODE = first-pass

- [x] #3 · notes/spring-boot/en/13-logging-observability.md
      TASK = create-file. Rule-1 gap: coverage.md "Logging and observability" (@Slf4j, log levels,
      parameterised logging, log.error(msg, e) vs log.error(e.getMessage()), what you must never log,
      Actuator, endpoint exposure risk) has no dedicated file — grep for "@Slf4j|log\.info" only hit
      09-testing.md incidentally. Independently searchable and interview-relevant on its own (code-review
      question: "what would you log in the approve endpoint?").
      Thread: continues from 12-production-debugging.md (logging is the tool you reach for while
      debugging); closes the Spring Boot numbered sequence for now — future-learning.md covers what
      comes after (correlation ids, HikariCP pool tuning in production).
      REWRITE_MODE = first-pass

- [x] #0 · notes/spring-boot/en/00-intro-spring-boot.md
      TASK = add-docs-link: every `##` section (Spring vs Spring Boot, The IoC container, Annotations
      replace configuration, The request lifecycle, Where TimeTrack fits) has no per-section Docs link
      — only the file-level Baeldung link at the top exists. This is a real gap here specifically
      because the sibling file 01-basics.md (same topic, same structured-mode folder) DOES give its
      deep-dive section ("Spring Boot internals") its own inline `Docs:` link
      (https://www.baeldung.com/spring-boot-autoconfiguration), so 00 drops below its own sibling's
      established pattern, not just the calibration file. Suggest per-section links once confirmed
      (e.g. a Baeldung IoC-container sub-section for "The IoC container", a component-scanning
      sub-section for "Annotations replace configuration" — TODO if no confident URL rather than guess).
      fix-quality: (1) zero-assumption gap — "Spring vs Spring Boot" mentions "a servlet container like
      Tomcat" and deploying "as a `.war` file" without ever explaining what a servlet container is or
      what a `.war` file actually is (a packaged web-app archive a container loads), unlike the same
      section's careful treatment of every other new term; (2) no real-world analogy anywhere in the
      file — "The IoC container" is exactly the kind of abstract mechanism the standard calls out for
      one (e.g. IoC container as a staffing agency: you describe the role you need, the agency finds
      and hands you the worker, you never `new` one yourself), and the file currently relies on the
      before/after code diagram alone with no physical anchor; (3) the "Without IoC" / "With IoC" code
      block is a clear wrong-vs-right contrast but is not labelled `// MAL` / `// BIEN` (or ❌/✅) per
      the wrong-vs-right labelling rule; (4) "Annotations replace configuration" states that "the
      container discovers it automatically by scanning the package" but never traces the mechanism —
      what package it starts scanning from and why (the package of the `@SpringBootApplication` class,
      scanning downward) — a real "how does it know where to look?" question the anticipate-the-TODO
      pass would surface, left unanswered; (5) the annotation table's rows (`@Service`, `@Repository`,
      `@RestController`, `@Configuration`/`@Bean`) have no forward-reference markers to the files that
      fully cover each one (03-dependency-injection.md, 02-rest-controllers.md, 04-spring-data-jpa.md),
      unlike the diagram in "The request lifecycle" two sections later, which does link each layer to
      its file; (6) partial duplication with 01-basics.md — "Spring vs Spring Boot" restates the same
      two-idea framing (auto-configuration + embedded server) and the same "annotations replace
      configuration" line nearly verbatim from 01-basics.md lines 19–24, rather than only naming the
      themes and deferring the explanation (which the file does correctly elsewhere, e.g. deferring
      IoC detail to 03-dependency-injection.md) — tighten section 1 to preview-and-link instead of
      re-explaining, matching how the rest of the file already defers depth to the numbered files.
      REWRITE_MODE = standard

- [x] #1 · notes/spring-boot/en/01-basics.md
      TASK = fix-quality: (1) narrative seams — the file opens cold with a glossary of four terms
      (Tomcat, .jar, Bean, Jackson) with no thread-pickup line connecting back to 00-intro-spring-boot.md,
      which already introduced beans and the IoC container conceptually; 01 re-defines "Bean" from
      scratch instead of a one-sentence "you met beans in 00, here's the vocabulary you need to actually
      set one up" bridge. The file also has no closing handoff at the end (the last section, the
      data.sql case study, ends on a lessons-learned callout with no forward pointer to
      02-rest-controllers.md), so the thread only works in the reading-order list in 00, never inside 01
      itself; (2) concrete bug — line 404, the `spring.datasource.url` table row, reads "JDBC URL —
      protocol + driver + host + port + database name apropiado": a stray Spanish word ("apropiado") is
      leaked into the English table row (confirmed against es/01-basicos.md line 404, which correctly
      has no equivalent word there) — delete it; (3) missing "how to read it" sentence after three
      tables: "What each field means" (Spring Initializr), "Dependencies for a full Spring Boot project",
      and the `spring.datasource.*` properties table all end with no explanatory line before the next
      heading/paragraph, unlike the calibration file's tables which always close with one; (4) the
      `@SpringBootApplication` annotation table (`@Configuration`/`@EnableAutoConfiguration`/
      `@ComponentScan`) has no forward-reference markers to the files that cover each one in full
      (03-dependency-injection.md, 04-spring-data-jpa.md, 02-rest-controllers.md) — the sibling file
      00-intro-spring-boot.md's own annotation-role table and request-lifecycle diagram both link each
      row/box to its file, so 01 drops below that established sibling pattern; (5) format inconsistency
      — from "Spring profiles" onward every section uses the structured-mode `Purpose:`/`Docs:`/`File:`
      triad, but the earlier sections (Spring Initializr, Adding dependencies, Lombok, Project structure)
      use plain prose + inline Docs links with no `Purpose:`/`File:` fields at all — the file switches
      format conventions partway through itself.
      add-docs-link: "Why Spring Boot exists", "Spring Initializr — starting a project", "Adding
      dependencies after project creation", and "Project structure" have no per-section Docs link at
      all — only the file-level Baeldung/official-docs links at the top and the inline link inside
      "Spring Boot internals" exist. Suggest the file-level Baeldung bootstrap article's matching
      sub-section for "Spring Initializr" (confirm exact anchor before writing), and a Baeldung Lombok
      intro page for the Lombok subsection specifically — TODO if no confident URL rather than guess.
      REWRITE_MODE = standard

- [ ] #2 · notes/spring-boot/en/02-rest-controllers.md
      TASK = add-docs-link: "The three-layer architecture" (opening section) has no per-section Docs
      link of its own — only the two file-level links at the top exist; this is the single most
      interview-relevant section in the file (it says so itself: "the first thing an interviewer will
      ask about") and yet is the one section pointing nowhere specific. Suggest a Baeldung layered-
      architecture / n-tier sub-section if a confident URL is found, otherwise TODO rather than guess.
      fix-quality: (1) narrative seams — the file opens with a bare statement ("Every Spring Boot API
      follows three layers") and no thread-pickup line back to 01-basics.md (which ended on the
      internals/classpath discussion), unlike the calibration file's opening paragraph, which states
      the pain before the concept; the file also has no closing handoff at the end — the last section
      ("A complete controller example") ends on a code block with no forward pointer to
      03-dependency-injection.md or 05-exception-handling.md; (2) missing "how to read it" sentence
      after two tables — the HTTP-methods annotation table ("HTTP methods — what each one means") and
      the status-code table ("Key status codes to know") both end with no explanatory line before the
      next heading, unlike the calibration file's tables, which always close with one; (3) no real-world
      analogy anywhere in the file — `ResponseEntity`'s builder-pattern mechanism and the
      `@RequestBody`→Jackson deserialization mechanism are both abstract, step-by-step processes the
      standard calls out for a physical anchor (e.g. `ResponseEntity.status(201).body(created)` as
      filling out a shipping label — first the stamp/status, then what goes in the box), and the file
      currently relies on numbered prose steps alone with no analogy; (4) no wrong-vs-right (`// MAL` /
      `// BIEN`, or ✅/❌) labelled example anywhere in the file, despite two clear candidates: the
      "without `ResponseEntity` every method returns plain 200 OK" contrast, and the "without
      `@RequestBody` the object arrives null" contrast — both are stated as prose warnings instead of
      a labelled code pair, the same systemic gap already flagged across several java/en files on this
      worklist; (5) format inconsistency — per the structured-mode rule this folder uses, code-concept
      sections should open with `Purpose:`/`File:`/`Docs:` metadata lines, but only "@JsonIgnore"
      follows that triad; every other code section (three-layer architecture, ResponseEntity,
      @PathVariable/@RequestParam/@RequestBody, the Project 07 TimeTrack walkthrough) uses plain prose
      + bullets instead — consistent throughout this file, so likely a pre-existing folder-wide pattern
      rather than a one-off, but still below the standard's format-mode rule as written.
      REWRITE_MODE = standard

- [ ] #3 · notes/spring-boot/en/03-dependency-injection.md
      TASK = fix-quality: (1) blockquote density is far below the standard's bar — only ONE `>`
      callout in the entire file (the closing "Why interviewers ask this" note under
      @ConfigurationProperties), across 9 sections, despite several non-obvious mechanism questions
      the anticipate-the-TODO pass would surface and none of them answered in a callout: why a
      singleton bean being shared means service fields must be stateless (§ "Spring beans" states the
      rule but never traces the mechanism — concurrent HTTP request threads calling methods on the
      exact same instance at the same time, so a mutable field would be a race condition); where the
      string `"smsNotificationService"` in the `@Qualifier` example actually comes from (§ "@Qualifier
      and @Primary" never explains Spring's default bean-naming convention — the class name with its
      first letter lowercased — so the reader cannot tell why that exact string works); and why the
      startup diagram's order is what it is (§ "How Spring wires everything at startup" draws the
      sequence but never states that Spring resolves a dependency graph rather than reading top-to-
      bottom). All three are exactly the kind of "why does this work?" doubt the standard says must be
      pre-answered in prose, not left implicit in a diagram or list; (2) zero real-world analogies
      anywhere in the file, despite three abstract mechanisms that qualify for one per the "one good
      analogy per hard concept" rule (a bean/Application Context as a staffing agency that hands you a
      worker instead of you hiring one yourself; a singleton bean as one shared whiteboard vs a fresh
      notepad per request; the wiring-order diagram as a dependency graph, not a checklist read in
      order); (3) no wrong-vs-right (`// MAL` / `// BIEN`, or ✅/❌) labelled example anywhere — the
      clearest candidate is field injection vs constructor injection in "Constructor injection — the
      correct way", which currently uses only inline comments ("avoid", "rare, mostly legacy",
      "correct choice") instead of the standard's explicit labels — the same systemic gap already
      flagged for 02-rest-controllers.md, 10-generics.md, 11-enums.md and 13-annotations.md elsewhere
      on this worklist; (4) no exact error message quoted anywhere — the file states twice that
      "Spring fails at startup with a clear error" (missing bean, missing config key) but never shows
      the real exception text a junior would see in IntelliJ, e.g.
      `NoSuchBeanDefinitionException: No qualifying bean of type ... available` or the
      `UnsatisfiedDependencyException` wrapper — per the exact-error-message rule already applied in
      the calibration file and flagged for several sibling files on this worklist; (5) missing "how to
      read it" sentence after the `@Value` vs `@ConfigurationProperties` comparison table — the table
      is followed directly by the interview-relevance blockquote with no line explaining what the
      "Type safety" / "Testability" rows actually mean in practice; (6) narrative seams — the file
      opens cold ("Without DI, a class creates its own dependencies.") with no thread-pickup line back
      to 02-rest-controllers.md, and closes on the @ConfigurationProperties interview-quote blockquote
      with no forward handoff to 04-spring-data-jpa.md — the same systemic gap already flagged for
      00-intro-spring-boot.md, 01-basics.md and 02-rest-controllers.md on this worklist; (7) format
      inconsistency — per this folder's structured-mode rule, every section should open with
      `Purpose:`/`File:`/`Docs:` metadata lines, but every section in this file uses only a bare
      `Docs:` line (or none) with no `Purpose:` or `File:` field at all — the same systemic gap already
      flagged for 01-basics.md and 02-rest-controllers.md.
      add-docs-link: two sections have no per-section Docs link at all — "Why dependency injection
      exists" (the opening section, only the two file-level links at the top exist) and "How Spring
      wires everything at startup" (relies entirely on its own diagram with no linked sub-section) —
      every other section in the file does have one. Suggest a Baeldung IoC/DI intro sub-section for
      the first, and Baeldung's Spring bean-lifecycle sub-section for the second — confirm exact URLs
      before writing, TODO if unconfident rather than guess.
      REWRITE_MODE = standard

- [ ] #4 · notes/spring-boot/en/04-spring-data-jpa.md
      TASK = fix-quality: (1) narrative seams — the file opens cold with "JPA vs Hibernate — the spec
      vs the implementation" with no thread-pickup line back to 03-dependency-injection.md (which ends
      on the `@Value` vs `@ConfigurationProperties` interview blockquote, no forward pointer to 04
      either — same systemic gap already flagged on 03's own row), and the last section ("save() —
      insert or update") ends on a plain code block with no closing handoff to
      05-exception-handling.md — the same systemic gap already flagged for 00/01/02/03 on this worklist,
      now confirmed on both ends of this file too; (2) internal format inconsistency inside the SAME
      file — sections 3 (`@Enumerated`) and 4 (`@ColumnDefault`) use the full structured-mode
      `Purpose:`/`Docs:`/`File:` triad (and hit the calibration file's bar: exact error message quoted,
      before/after mechanism trace, interviewer callout), but sections 1, 2, 5, 6, 7, 8, 9, 10, 11, 12,
      13 use only a bare `Docs:` line with no `Purpose:`/`File:` fields at all — this is not just the
      folder-wide pattern already flagged elsewhere (01/02/03 are consistently one way throughout), it
      is two different conventions inside one file, which reads as unfinished rather than a stable
      style choice; (3) section 1 ("JPA vs Hibernate") is thin relative to its neighbours — four
      sentences, no mechanism trace of *how* Hibernate turns an annotated class into SQL (reflection at
      startup, proxies), no callout, no docs link of its own — it does not match the texture of section
      4 two positions later in the same file; (4) missing "how to read it" sentences after two tables —
      the `@GeneratedValue` strategy table (section 2) and the `FetchType.LAZY` vs `EAGER` table
      (section 11) are both followed directly by more prose without a line decoding a specific
      column/row for the reader, unlike section 4's `@ColumnDefault` prose, which explains every line of
      its own code block; (5) missing docs-link priority — section 10 (`@ManyToMany`) links only the
      Jakarta EE spec page (jakarta.ee) as its sole Docs reference; per the standard's docs-link
      priority rule, Spring/Java concepts must use Baeldung as primary and official/spec docs only as a
      secondary link — this is the one section in the file that violates that rule outright (every
      other section correctly leads with a baeldung.com link); (6) section 5 ("Automatic timestamps")
      has no `File:` reference to a real project file despite the standard's "reference real projects"
      rule, even though `@CreationTimestamp`/`@PrePersist` are almost certainly used in TimeTrack's
      entities (check PROGRESS.md / project 07 source for the actual file, e.g. `TimeEntry.java` or
      `User.java`, and cite it the same way section 3 and section 4 already do).
      add-docs-link: section 1 ("JPA vs Hibernate") has no Docs link at all (only the file-level links
      at the top exist) and section 10 (`@ManyToMany`) needs a Baeldung link added as primary, keeping
      the Jakarta EE spec link as a secondary reference — TODO if no confident URL rather than guess.
      REWRITE_MODE = standard

- [ ] #4 · notes/spring-boot/en/09-testing.md
      TASK = add-section. Rule-1 gap: coverage.md "Exercising the API — Postman and HTTP failures"
      (Postman request essentials, environments/variables, 415 vs 405 vs 404, verifying the write in
      pgAdmin not just the response, curl as a GUI-less fallback) is not covered anywhere — grep for
      "Postman" across notes/spring-boot/en/ returns no hits. Small enough (6 bullets) to land as a new
      section in the existing testing file rather than its own file: it is the manual counterpart to
      the automated testing 09 already covers (JUnit/Mockito/@WebMvcTest), so it belongs next to it, not
      isolated. Add as the closing section, after "Testing — the web and persistence layers", with a
      thread-note that ties it back: automated tests prove the code once, but Postman/curl is what you
      still reach for to demo the collection live and to eyeball the exact response a reviewer will see.
      fix-quality (from inspection pass): (1) the JUnit 5 lifecycle table (`@Test`/`@BeforeEach`/
      `@AfterEach`/`@BeforeAll`) has no "how to read it" sentence after it — the file jumps straight from
      the table into "Common assertions" with nothing decoding the one non-obvious row (`@BeforeAll` —
      "must be static"), unlike this same file's later tables (`@MockBean` vs `@Mock`, "What each test
      type catches") which both close with an explanatory line; (2) the `@BeforeAll` row states "(must
      be `static`)" as a bare parenthetical with no mechanism — a real anticipate-the-TODO gap ("why
      static?"): it runs once before the test class ever creates an instance of itself, so at that point
      there is no object to call an instance method on — it must belong to the class itself, not to any
      one instance. This is unanswered and should become a `>` blockquote; (3) zero-assumption gap —
      `@AutoConfigureMockMvc` appears in the `@SpringBootTest` code block ("Full integration test")
      with no explanation anywhere in the file of what it does or why `@SpringBootTest` alone is not
      enough to get a working `MockMvc` bean, unlike every other annotation introduced in this file
      (`@Mock`, `@InjectMocks`, `@MockBean`, `@WebMvcTest`, `@DataJpaTest`), which are all explained
      inline; (4) format inconsistency — per this folder's structured-mode rule every section should
      open with `Purpose:`/`File:`/`Docs:` metadata lines, but every section in this file uses only a
      bare `Docs:` line (or none, e.g. "The problem without tests", "The layered testing strategy", "The
      minimum for project 07") with no `Purpose:`/`File:` fields at all — the same systemic gap already
      flagged for 01/02/03/04/08 on this worklist; (5) narrative seams — the file opens cold ("Without
      tests, you find bugs in production...") with no thread-pickup line back to 08-transactions.md, and
      the closing section ("The minimum for project 07") ends on a checklist with no forward handoff to
      10-tooling.md — the same systemic gap already flagged for 00/01/02/03/04/08 on this worklist.
      add-docs-link (from inspection pass): docs-priority violation — "JUnit 5 — the test runner" links
      only the official JUnit docs (junit.org) as its sole reference; per the standard's docs-priority
      rule, Spring/Java concepts must lead with Baeldung, with official docs only as a secondary link —
      every other section in this file correctly leads with baeldung.com. Add a Baeldung JUnit 5 guide as
      primary (confirm exact URL before writing, TODO if unconfident), keep the junit.org link as
      secondary.
      REWRITE_MODE = standard

- [x] #5 · notes/spring-boot/en/11-business-logic-domain-modeling.md → es
      TASK = create-es. Spanish translation of #1, same structure/code, natural Spanish prose per the
      standard (not literal). Filename: es/11-logica-de-negocio-modelado-dominio.md.
      REWRITE_MODE = first-pass

- [x] #6 · notes/spring-boot/en/12-production-debugging.md → es
      TASK = create-es. Spanish translation of #2. Filename: es/12-depuracion-en-produccion.md.
      REWRITE_MODE = first-pass

- [x] #7 · notes/spring-boot/en/13-logging-observability.md → es
      TASK = create-es. Spanish translation of #3. Filename: es/13-logging-observabilidad.md.
      REWRITE_MODE = first-pass

- [ ] #8 · notes/spring-boot/en/09-testing.md → es (section sync)
      TASK = translate the new "Exercising the API — Postman and HTTP failures" section (item #4) into
      the existing es/09-testing.md counterpart, same position (closing section).
      REWRITE_MODE = standard

- [ ] #1 · notes/java/en/01-variables-types.md
      TASK = add-docs-link: every `##` section (Primitive types, Variables, Type casting, Wrapper
      classes, String, var) is missing its own per-section Docs link — only the file-level Baeldung/
      Oracle link at the top exists, unlike the calibration file (08-exceptions.md) where every `##`
      section has its own Docs block pointing to an exact sub-section. Suggest Baeldung sub-sections
      per section (e.g. "Primitive Data Types", "Type Casting", "Autoboxing and Unboxing", "Java
      String" — confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: (1) the primitive-types table (§ Primitive types) has no "how to read it" sentence
      after it, unlike the wrapper-classes and StringBuilder tables in the same file which both have
      one; (2) `Comparable<BigDecimal>` is used in "Comparing BigDecimal" with no forward-reference
      marker even though interfaces aren't covered until 05-interfaces-abstract.md; (3) "Autoboxing
      and unboxing" states the behaviour (Java converts automatically) but not the mechanism — what
      the compiler actually inserts (`Integer.valueOf()` / `.intValue()`) — a behaviour-only gap per
      the anticipate-the-TODO rule.
      REWRITE_MODE = standard

- [ ] #2 · notes/java/en/02-control-flow.md
      TASK = add-docs-link: every `##`/`###` section (if/else, ternary, switch statement, switch
      expression, for loops, classic for, enhanced for, while/do-while, break/continue, null checks)
      has no per-section Docs link — only the file-level Baeldung/Oracle link at the top exists,
      same gap already flagged in 01-variables-types.md against the calibration file
      (08-exceptions.md), where every section has its own Docs block pointing to an exact sub-section.
      Suggest Baeldung sub-sections per section (e.g. "If-Else Statement", "Switch Statement",
      "For Loop", "Enhanced For Loop", "While and Do-While Loop", "Break and Continue Keywords" —
      confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: (1) blockquote density is below the standard's bar — only 2 callouts across 6
      top-level sections (if/else truthy-vs-boolean, break-vs-return), while fall-through, the
      switch-expression's break removal, do-while's guaranteed-first-run, and the off-by-one error in
      enhanced-for are all non-obvious sub-concepts stated as plain prose instead of a `>` callout;
      (2) fall-through under "Classic switch (statement)" is the one concept in this file with clear
      spatial/structural shape (control moving from one case block into the next) and has no ASCII
      diagram, unlike the call-stack diagram the calibration file uses for an analogous "flow moves
      from A to B" mechanism; (3) "Null checks" states the NullPointerException mechanism but never
      quotes the exact runtime error text (e.g. `Exception in thread "main"
      java.lang.NullPointerException: Cannot invoke "String.toUpperCase()" because "name" is null"`),
      unlike the exact-compiler-message rule already applied earlier in the same file for
      `incompatible types: String cannot be converted to boolean`.
      REWRITE_MODE = standard

- [ ] #3 · notes/java/en/03-methods.md
      TASK = add-docs-link: every `##` section (Method declaration, Access modifiers, Return types,
      void vs Void, Static methods, Method overloading, Varargs, Calling methods, Naming conventions)
      is missing its own per-section Docs link — only the file-level Baeldung/Oracle link at the top
      exists, the same gap already flagged in 00-intro-java.md, 01-variables-types.md, and
      02-control-flow.md against the calibration file (08-exceptions.md), where every section has its
      own Docs block pointing to an exact sub-section. Suggest Baeldung sub-sections per section (e.g.
      "Method Declaration", "Access Modifiers", "Static Keyword", "Method Overloading", "Varargs" —
      confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: "Varargs" section shows only the correct form (`Type... name` as the last parameter)
      and states the rule ("must be the last parameter... Java could not tell which argument belongs
      to the varargs list") but never shows the tempting wrong version or its exact compiler error —
      per the wrong-vs-right labelling rule and the anticipate-the-TODO rule, a `// MAL` example of
      varargs not in last position (e.g. `sum(int... numbers, int extra)`) with the resulting error
      message would close this the same way the calibration file pairs "why this rule" with a concrete
      broken example elsewhere in the standard.
      REWRITE_MODE = standard

- [ ] #0 · notes/java/en/00-intro-java.md
      TASK = add-docs-link: three sections have no per-section Docs link (only the file-level Baeldung
      link at the top and the OOP section's closing link exist) — "Static typing" (§2), "Everything is
      a class" (§3), and "The entry point — public static void main" (§4) each introduce distinct
      Baeldung-documentable sub-concepts (static typing, class-as-unit, the main method signature) but
      point nowhere; per the standard every section needs a link to the exact sub-section, not just the
      page-level intro link. Suggest: "Static typing" → Baeldung "Introduction to Java" has no dedicated
      static-typing subsection, so this may need a different Baeldung page (e.g. a Java types/variables
      article) — flag as TODO if no confident URL exists rather than guess. "Everything is a class" and
      "The entry point" could both reasonably point to Baeldung's Java tutorial main-method/class-basics
      coverage if a precise sub-section is confirmed.
      REWRITE_MODE = standard

- [ ] #4 · notes/java/en/04-oop-classes.md
      TASK = add-docs-link: every `##` section (What is object-oriented programming, What is a class,
      `this`, Constructors, Encapsulation, Static fields and methods, Constructor overloading,
      `toString()`, `equals()` and `hashCode()`, Records) is missing its own per-section Docs link —
      only the file-level Baeldung/Oracle link at the top exists, the same gap already flagged in
      00-intro-java.md, 01-variables-types.md, 02-control-flow.md, and 03-methods.md against the
      calibration file (08-exceptions.md), where every section has its own Docs block pointing to an
      exact sub-section. Suggest Baeldung sub-sections per section (e.g. "The `this` Keyword in Java",
      "Guide to Constructors", "Encapsulation in Java", "Guide to `equals()` and `hashCode()`", "Java
      16 Record" — confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: (1) "Constructors" states that defining any constructor removes Java's implicit
      default one but never explains why (no mechanism, and this is the only section in the file with
      zero `>` blockquote callout — every neighbouring section has at least one, breaking the "no
      section drops below its neighbours" rule); (2) "Constructor overloading" never explains the
      overload-resolution mechanism itself — how Java decides which constructor runs when several
      share a name (matching by argument count/type at compile time) — a real "how does it decide?"
      question the anticipate-the-TODO pass would surface, left unanswered.
      fix-es-quality: es/04-poo-clases.md spells "sobreescribes" / "sobreescribir" (lines 262, 285)
      with a double e — the RAE-correct spelling is "sobrescribes" / "sobrescribir" (single e); minor
      but worth correcting in the next translation pass.
      REWRITE_MODE = standard

- [ ] #5 · notes/java/en/05-interfaces-abstract.md
      TASK = add-docs-link: every section (Interface, A class can implement multiple interfaces,
      Default methods, Abstract class, Interface vs Abstract class, Subclass constructors, Functional
      interfaces, Spring Boot connection incl. Pattern 1/2/The full flow) has no per-section Docs link
      — only the three file-level links (Baeldung, Oracle, Spring Security) exist. Same systemic gap
      already flagged for 00/01/02/03/04 against the calibration file (08-exceptions.md), where every
      `##`/`###` section carries its own Docs block pointing to an exact sub-section. Suggest Baeldung
      sub-sections per section (e.g. "Default Methods in Java 8" already used file-level — point
      "Default methods" section specifically there; "Functional Interfaces" for the functional-
      interfaces section; confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: (1) neither the "implements an interface but doesn't provide every method" compiler
      error nor the diamond-problem compile error is quoted verbatim anywhere in the file, even though
      both sections describe "the compiler gives you an error" / "Java refuses to compile it" —
      per the exact-error-message rule (already applied in the calibration file and in 01/02) these
      should show the real message (e.g. `X is not abstract and does not override abstract method
      print() in Printable` for the missing-method case); (2) no wrong-vs-right (`// MAL` / `// BIEN`)
      labelled example anywhere in the file — the closest candidate is the diamond-problem code, which
      shows the ambiguous case and its fix but without the MAL/BIEN labels the standard calls for.
      fix-es-quality: es/05-interfaces-abstractas.md repeats the same misspelling already flagged for
      es/04-poo-clases.md — "sobreescribir"/"sobreescribe" (lines 22, 55 ×2, 78) uses a double "e";
      RAE-correct spelling is "sobrescribir"/"sobrescribe" (single "e"). Fix in the next translation
      pass alongside the 04 fix already on the worklist.
      REWRITE_MODE = standard

- [ ] #6 · notes/java/en/06-inheritance-polymorphism.md
      TASK = add-docs-link: every `##` section (Inheritance — extends, `super`, Method overriding,
      Polymorphism, `instanceof` and pattern matching, `final` classes/methods/fields, The Object class,
      Spring Boot connection) has no per-section Docs link — only the two file-level links (Baeldung,
      Oracle) at the top exist. Same systemic gap already flagged for 00–05 against the calibration file
      (08-exceptions.md), where every `##`/`###` section carries its own Docs block pointing to an exact
      sub-section. Suggest Baeldung sub-sections per section (e.g. "Types of Inheritance" for §1, "Method
      Overriding" for §3, "Polymorphism" for §4 — already used file-level, point section-specifically;
      confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: (1) the Polymorphism section explains the method-table/vtable mechanism in detail
      (object → hidden table pointer → method) but has no ASCII diagram for it, even though this is
      exactly the kind of spatial/structural mechanism the standard calls out for a diagram (the
      calibration file draws the call stack for an analogous "how does the JVM find the right thing"
      mechanism) — the section instead relies on prose + one analogy (the phone-book callout), which is
      good but does not replace the diagram; (2) the `instanceof` and pattern matching section never
      quotes the exact `ClassCastException` runtime message (e.g. `class Dog cannot be cast to class
      Cat`), despite the exact-error-message rule already being applied elsewhere in the file's own
      sibling notes (00–05) and in the calibration file — it only says "the cast would throw a
      ClassCastException at runtime" in the abstract.
      REWRITE_MODE = standard

- [ ] #7 · notes/java/en/07-collections.md
      TASK = add-docs-link: every `##`/`###` section (List, List.of() vs ArrayList, List vs Array,
      ArrayList vs LinkedList, Map, HashMap vs LinkedHashMap vs TreeMap, Set, HashSet vs LinkedHashSet
      vs TreeSet, Common methods, Collections utility methods, Sorting/Comparable/Comparator,
      ConcurrentModificationException, Quick reference, Spring Boot connection) has no per-section Docs
      link — only the two file-level links (Baeldung, Oracle) at the top exist. Same systemic gap
      already flagged for 00–06 against the calibration file (08-exceptions.md), where every `##`/`###`
      section carries its own Docs block pointing to an exact sub-section. Suggest Baeldung sub-sections
      per section (e.g. "The List Interface" for List, "The Map Interface" for Map, "HashSet, LinkedHashSet
      and TreeSet" for Set implementations, "Guide to Java Comparator" for Sorting, "Guide to
      ConcurrentModificationException" for that section — confirm exact URLs before writing, TODO if
      unconfident rather than guess).
      fix-quality: (1) `ConcurrentModificationException` section never quotes the exact runtime stack
      trace/exception message, unlike the exact-error-message rule already applied in this same file's
      List.of() sub-section (`UnsupportedOperationException`) only by name, and unlike the calibration
      file's verbatim `ArithmeticException`/`unreported exception IOException` messages — add the real
      printed exception (e.g. `Exception in thread "main" java.util.ConcurrentModificationException` +
      the `at java.util.ArrayList$Itr.checkForComodification` frame) so the section matches the
      standard's bar for showing what Victor will actually see in IntelliJ; (2) the "This throws
      ConcurrentModificationException" / `removeIf()` pair is a clear wrong-vs-right case but is not
      labelled `// MAL` / `// BIEN` per the wrong-vs-right labelling rule, unlike the `List.of()` ❌
      examples earlier in the same file.
      REWRITE_MODE = standard

- [ ] #8 · notes/java/en/08-exceptions.md
      TASK = fix-quality: (1) narrative seams — the file opens cold ("Without exceptions, every method
      would have to return...") with no thread-pickup line connecting back to 07-collections.md,
      unlike 07 (opens "You can now create objects... [06-inheritance-polymorphism.md]") and 09
      (opens with an explicit "Where this fits in the story" section referencing file 08 by number).
      08 also has no closing handoff to 09-streams-lambdas.md — the last section ("Spring Boot
      connection") ends on the GlobalExceptionHandler example with no forward pointer, so the
      thread-continuation currently only works one-directionally (09 reaches back into 08, 08 never
      reaches forward); (2) forward-reference marker missing — "Checked vs unchecked exceptions"
      introduces `Optional` and `orElseThrow()` with an inline explanation but no marker that the full
      treatment lives in 10-generics.md (confirmed: `Optional<T>` is covered there, file 10 > file 08),
      violating the "mark forward references within the same topic" rule; the same unmarked usage
      recurs in "Custom exceptions" and "Spring Boot connection" wherever `orElseThrow()` reappears.
      REWRITE_MODE = standard

- [ ] #10 · notes/java/en/10-generics.md
      TASK = add-docs-link: every `##`/`###` section (Generics, Generic class, Generic method, Bounded
      type parameters, `Optional<T>`, Creating an Optional, Using an Optional, Optional.map() vs
      Stream.map(), Chaining map() + orElseThrow(), The most common Spring Boot pattern, Generics in
      Spring Boot) has no per-section Docs link — only the three file-level links (two Baeldung, one
      Oracle) at the top exist. Same systemic gap already flagged for 00–09 against the calibration file
      (08-exceptions.md), where every `##`/`###` section carries its own Docs block pointing to an exact
      sub-section. Suggest Baeldung sub-sections per section (e.g. "Type Erasure" for the erasure part of
      "Generics", "Bounded Type Parameters" for that section, "Optional isPresent/orElse/orElseGet/
      orElseThrow" for the Optional sections — confirm exact URLs before writing, TODO if unconfident
      rather than guess).
      fix-quality: (1) type erasure is the most abstract mechanism in the file (the compiler discards
      `<String>` and inserts casts you never see) and gets a diagram but no real-world analogy, unlike
      the standard's "one good analogy per hard concept" rule (e.g. the call-stack-as-plates analogy in
      the calibration file) — a physical anchor here (e.g. a labelled box whose label is peeled off and
      thrown away once the contents are checked) would close the gap; (2) two side-by-side contrast
      blocks — "Without generics" vs "With generics" under `## Generics`, and the `null`-returning vs
      `Optional`-returning `findById` under `## Optional<T>` — are the same wrong-vs-right shape already
      flagged for missing `// MAL` / `// BIEN` labels in 05-interfaces-abstract.md, but here the comments
      are unlabelled prose ("must cast and can cause ClassCastException" / "no cast needed") instead of
      the standard's explicit MAL/BIEN (or ✅/❌) labelling.
      REWRITE_MODE = standard

- [ ] #11 · notes/java/en/11-enums.md
      TASK = add-docs-link: the closing section "Enum vs constant strings vs TypeScript enum" has no
      Docs link at all — every other section in this file (Basic enum, Enum with fields and methods,
      Built-in enum methods, Enum in switch expression, Comparing enums, Spring Boot connection) has its
      own Docs block pointing to an exact sub-section, so this is the one section that drops below its
      neighbours in the same file. Suggest a Baeldung sub-section comparing enums to the old
      constant-string pattern if a confident URL exists, otherwise TODO rather than guess.
      fix-quality: (1) the singleton/class-loading mechanism (each constant built once at class load,
      one fixed object forever) gets an ASCII diagram but no real-world analogy, unlike the standard's
      "one good analogy per hard concept" rule (the calibration file anchors call-stack-as-plates for an
      analogous "how does this actually work under the hood" mechanism) — a physical anchor here (e.g.
      "like a fixed set of numbered lockers built once when the building opens — no one can ever add a
      new locker") would close the gap; (2) the opening "Without enum" / "With enum" comparison block is
      the wrong-vs-right shape the standard calls for but is not labelled `// MAL` / `// BIEN` (or ✅/❌)
      — only descriptive comments — the same systemic gap already flagged in 10-generics.md for an
      analogous unlabelled contrast block.
      REWRITE_MODE = standard

- [ ] #9 · notes/java/en/09-streams-lambdas.md
      TASK = add-docs-link: every `##` section (Where this fits in the story, Why this matters for
      Spring Boot, The problem lambdas solve, Functional interfaces, Lambda syntax, Method references,
      What a stream is, Intermediate vs terminal operations, Optional, Common patterns, Grouping and
      joining, Entity to DTO mapping, Stream vs for loop, Collectors quick reference) has no per-section
      Docs link — only the two file-level Baeldung links at the top exist. Same systemic gap already
      flagged for 00–08 against the calibration file (08-exceptions.md), where every `##`/`###` section
      carries its own Docs block pointing to an exact sub-section. Suggest Baeldung sub-sections per
      section (e.g. "Functional Interfaces" for the functional-interfaces section, "Method References"
      for the method-references section, "Collectors" for the grouping/joining and quick-reference
      sections — confirm exact URLs before writing, TODO if unconfident rather than guess).
      fix-quality: the `.toList()` immutability callout states the exception by name
      (`UnsupportedOperationException`) without quoting the exact runtime message, the same shortfall
      already flagged in 07-collections.md's `List.of()` sub-section for the identical exception type —
      per the exact-error-message rule, add the real printed exception so this section matches the bar
      the same fix already targets in 07.
      REWRITE_MODE = standard

- [ ] #12 · notes/java/en/12-dates.md
      TASK = add-docs-link: every `##` section (The three main types, Instant, Creating values,
      Reading values, Formatting, Adding and subtracting, Comparing dates, Period and Duration, Spring
      Boot connection, Quick reference) has no per-section Docs link — only the file-level Baeldung/
      Oracle link at the top exists, the same systemic gap already flagged for 00–11 against the
      calibration file (08-exceptions.md), where every `##`/`###` section carries its own Docs block
      pointing to an exact sub-section. Suggest Baeldung sub-sections per section (e.g. a Java 8
      Date/Time sub-article for "Instant", "Parsing and Formatting" for Creating values/Formatting,
      "Period, Duration, and Instant" for Period and Duration — confirm exact URLs before writing, TODO
      if unconfident rather than guess).
      fix-quality: (1) fabricated example instead of a real project file — "Spring Boot connection"
      uses a made-up `Employee` class with `hireDate`/`createdAt`, but no `Employee` class exists
      anywhere in project 07 (grepped `projects/07-timetrack` — zero matches); the real, already-written
      entity is `projects/07-timetrack/backend/timetrack/src/main/java/com/victor/timetrack/model/
      TimeEntry.java`, which already has `LocalDate date` (line 33) and `LocalDateTime createdAt`/
      `updatedAt` (lines 47, 50) — swap the JPA-entity, DTO, and `@PrePersist` examples to use the real
      `TimeEntry` fields per the "reference real projects, not a fabricated example" rule (also fix the
      identical fabricated example in es/12-fechas.md's "Conexión con Spring Boot" section); (2)
      "Creating values" never shows the parse-failure gotcha or its exact error — `LocalDate.parse(
      "11/05/2026")` without a formatter throws `DateTimeParseException: Text '11/05/2026' could not be
      parsed at index 2`, a real and likely mistake (forgetting the custom formatter) with no blockquote
      or exact-error-message coverage, unlike the immutability bug callout later in the same file; (3)
      "Formatting" states a real gotcha ("this is display only — never store or send a custom-formatted
      date") as a plain inline sentence instead of a `> blockquote`/`Why not X?` callout, breaking the
      "no section drops below its neighbours" rule (the sections immediately before and after both use
      blockquote callouts for their non-obvious point); (4) "Period and Duration" is the thinnest section
      in the file — no blockquote, no analogy, no "how to read it" line, no gotcha, and no mechanism for
      why `Period.between()` returns calendar-aware years/months/days instead of a raw day count; it also
      never mentions `ChronoUnit.DAYS.between()` as the tool for a raw day/hour count, which is exactly
      the kind of calculation TimeTrack's own hours-worked reporting would need — add a callout
      contrasting `Period`/`Duration` (calendar-aware) with `ChronoUnit` (raw count) and explain the
      mechanism.
      REWRITE_MODE = standard

- [ ] #13 · notes/java/en/13-annotations.md
      TASK = add-docs-link: every `##`/`###` section (What annotations do, Built-in Java annotations,
      Meta-annotations, `@Retention`, `@Target`, The repeating pattern, Spring Boot annotations —
      Bean annotations/Dependency injection/REST controller annotations/Entity annotations/Transaction
      management, Quick reference) has no per-section Docs link — only the two file-level links
      (Baeldung, Oracle) at the top exist. Same systemic gap already flagged for 00–12 against the
      calibration file (08-exceptions.md), where every `##`/`###` section carries its own Docs block
      pointing to an exact sub-section. Suggest Baeldung sub-sections per section (e.g. "Java
      Annotations" sub-pages for `@Retention`/`@Target`, "Guide to Spring @Autowired" for Dependency
      injection, "Spring's RequestMapping Annotation" for REST controller annotations, "The DAO with
      Spring and Hibernate" or JPA-annotation Baeldung page for Entity annotations, "Guide to
      @Transactional" for Transaction management — confirm exact URLs before writing, TODO if
      unconfident rather than guess).
      fix-quality: (1) "Built-in Java annotations" has zero `>` blockquote callout, the only section in
      the file with none, breaking "no section drops below its neighbours" — it also never shows the
      exact compiler error `@Override` produces when the signature does not actually match a parent
      method (e.g. `method does not override or implement a method from a supertype`), a real,
      easy-to-hit mistake with no wrong-vs-right (`// MAL`/`// BIEN`) labelled example, unlike the
      exact-error-message and MAL/BIEN precedent already set in 01/02/05/07; (2) `@Target` never states
      or quotes the exact compiler error produced when an annotation is placed on the wrong kind of
      element (e.g. `annotation type not applicable to this kind of declaration`) — a real "what
      happens if I break the rule" question the anticipate-the-TODO pass would surface, left
      unanswered, unlike `@Retention` in the same sub-section pair which does trace its consequence in
      full (`@Service` never registered as a bean); (3) "Dependency injection" shows field vs
      constructor injection as an unlabelled wrong-vs-right contrast ("works but avoid it" / "preferred")
      instead of `// MAL` / `// BIEN` labels, the same systemic gap already flagged in 10-generics.md
      and 11-enums.md for analogous unlabelled contrast blocks; (4) "Transaction management" explains
      the proxy mechanism well but omits the classic `@Transactional` gotcha — a private method or a
      self-invoked method (`this.transferDepartment(...)` called from inside the same class) bypasses
      the proxy entirely and the transaction silently never opens — this is exactly the kind of "why not
      just call it directly?" callout the standard calls for and a common interview/code-review question;
      add a `> blockquote`/`Why not X?` explaining that the proxy only intercepts calls that go through
      the Spring-managed bean reference, not internal `this.` calls.
      REWRITE_MODE = standard

- [ ] #14 · notes/java/en/14-maven.md
      TASK = add-docs-link: every `##` section (What Maven does for you, pom.xml structure, The parent
      block and version management, How to add a dependency, Dependency scopes, Maven lifecycle —
      common commands, Maven vs Gradle, Maven folder structure, Where this leaves you) has no
      per-section Docs link — only the two file-level links (Baeldung, official Maven guide) at the
      top exist. Same systemic gap already flagged for 00–13 against the calibration file
      (08-exceptions.md), where every `##`/`###` section carries its own Docs block pointing to an
      exact sub-section. Suggest Baeldung sub-sections per section (e.g. a Baeldung POM/dependencies
      sub-page for "pom.xml structure", "Dependency Scope" for "Dependency scopes", a Maven lifecycle
      sub-page for "Maven lifecycle — common commands" — confirm exact URLs before writing, TODO if
      unconfident rather than guess).
      fix-quality: (1) the file explicitly links `projects/07-timetrack/backend/timetrack/pom.xml` at
      the top and closing paragraph as "the exact one behind project 07", but the `pom.xml` code block
      shown under "pom.xml structure" is a fabricated generic template that does not match that real
      file — real file has `groupId=com.victor`, `artifactId=timetrack`, parent version `4.0.6`,
      `java.version=25`, and `spring-boot-starter-webmvc` (not `-web`), while the note shows
      `com.example`/`hr-portal` (the name of project 06, an Angular-only project, not even Spring Boot),
      parent version `3.3.0`, `java.version=21`, and `spring-boot-starter-web`; the "How to add a
      dependency" jjwt example likewise shows version `0.12.5` and omits the `jjwt-jackson` dependency,
      while the real pom.xml uses `0.12.6` and includes `jjwt-jackson` — per the "reference real
      projects... not a fabricated example" rule, replace both code blocks with fragments actually
      quoted from `projects/07-timetrack/backend/timetrack/pom.xml` (verified 2026-07-13); (2) no exact
      build-failure error message is quoted anywhere in the file (e.g. a missing/unresolved dependency,
      a version conflict, or a malformed `pom.xml`), despite Maven being notorious for verbose build
      errors a junior will hit immediately — per the exact-error-message rule already applied in
      08-exceptions.md and flagged for 01/02/05/07/09, add a real error (e.g. `Non-resolvable parent
      POM` or `Could not find artifact ... in central`) tied to a concrete mistake (e.g. forgetting
      `<version>` on a non-Spring dependency); (3) no wrong-vs-right (`// MAL` / `// BIEN`) labelled
      example anywhere in the file — the closest candidate is the "why keep target/ out of git"
      callout, which states the reasoning but never shows a MAL (`target/` committed, causing merge
      conflicts) vs BIEN (`.gitignore`'d) pair — same systemic gap already flagged in 10-generics.md,
      11-enums.md, and 13-annotations.md for analogous unlabelled contrasts.
      REWRITE_MODE = standard

- [ ] #15 · notes/java/en/15-memory-model.md
      TASK = fix-quality: the file leans entirely on the arrow/diagram motif and never gives the stack
      and the heap an explicit real-world/physical analogy the way the calibration file does for the
      call stack ("a stack of plates") or file 01 does for `StringBuilder` ("a whiteboard"). "Stack vs
      heap" calls the stack "a per-method scratchpad" (one clause, not developed) and never gives the
      heap an equivalent physical anchor (e.g. "a shared warehouse anyone holding the address can walk
      into"), and "Garbage collection" has no physical analogy at all for reachability — it stays at
      diagram + prose. Per the "one good analogy per hard concept" rule, add a short physical analogy
      for the heap in "Stack vs heap" (paired with the existing scratchpad clause for the stack) and one
      for unreachability in "Garbage collection" (e.g. an abandoned locker no one has the key to
      anymore). Everything else in the file is at bar: per-section Docs links present on all three `##`
      sections, narrative open/close seams both present and correct, User example carried through,
      exact NullPointerException message quoted, MAL/BIEN labelled StringBuilder example, no
      duplication with 01/06/07/08, es/15-modelo-de-memoria.md is a faithful natural-Spanish mirror
      with no calque found.
      REWRITE_MODE = standard

- [ ] #10 · notes/spring-boot/en/10-tooling.md
      TASK = fix-quality: (1) format inconsistency — per this folder's structured-mode rule every
      section should open with `Purpose:`/`File:`/`Docs:` metadata lines, but every section in this
      file (Dockerfile, docker-compose, Flyway) uses only a bare `Docs:` line with no `Purpose:`/`File:`
      fields at all — the same systemic gap already flagged for 01/02/03/04/08/09 on this worklist;
      (2) narrative seams — the file opens with "These are deployment-time tools..." with no
      thread-pickup line back to 09-testing.md (e.g. "now that the code is tested, the next question is
      how you actually ship it"), and closes on the Flyway "why teams use it" blockquote with no forward
      handoff — the same systemic gap already flagged for 00/01/02/03/04/06/08/09; the opening section
      ("Why containerise") also has no per-section Docs link of its own, only the file-level links at
      the top — matching the identical gap already flagged for 00's and 01's opening sections;
      (3) missing mechanism — "each service is reachable by its name" (docker-compose section) states
      the behaviour (Compose's internal DNS resolves service names to container IPs on its private
      network) but never traces *why* — a genuine anticipate-the-TODO gap ("how does `db` resolve to an
      IP if it's not a real hostname?"); (4) missing mechanism / real gotcha — `EXPOSE 8080` is glossed
      as "document the port the app listens on" but the file never states the classic gotcha that
      `EXPOSE` is documentation only and does NOT publish the port — only `-p 8080:8080` (or
      `ports:` in Compose) actually maps it; a reader could reasonably ask "so why do I still need `-p`
      if `EXPOSE` already says the port?" and the note leaves it unanswered; (5) zero real-world
      analogies anywhere in the file, despite "It works on my machine" being exactly the kind of abstract
      packaging mechanism the standard calls out for one (e.g. a shipping container: the crate carries
      everything needed inside it, so it does not matter what ship or dock it lands on);
      (6) no ASCII diagram anywhere, despite docker-compose's service network (app ↔ db, reachable by
      service name, distinct from `localhost`) being exactly the kind of spatial/structural mechanism the
      standard calls out for one; (7) no exact error message quoted anywhere in the file — this is a
      topic where junior devs hit very concrete, recognisable errors (e.g. `Bind for 0.0.0.0:8080 failed:
      port is already allocated` when a container is already running, or Flyway's checksum-mismatch error
      when a migration file is edited after being applied) — none are shown, unlike the calibration
      file's exact-error-message treatment; (8) no wrong-vs-right (`// MAL` / `// BIEN`) labelled example
      anywhere — the clearest candidate is the `volumes:` section, which states the "without `pgdata`,
      every `docker compose down` wipes the database" gotcha as plain prose instead of a labelled
      before/after `docker-compose.yml` pair; (9) blockquote density is thin — only 2 callouts across 3
      `##` sections (multi-stage builds, why-teams-use-Flyway), while at least two more non-obvious
      points (the `depends_on` readiness caveat, the `EXPOSE`-vs-`-p` gotcha from (4) above) are stated as
      plain bullets instead of `>` callouts.
      Everything else is at bar: Baeldung-first docs priority is respected in every per-section link
      that exists (Dockerfile and Flyway sections both lead with Baeldung, official docs secondary); the
      `timetrack` example is carried consistently across all three sections; the cross-reference to
      [01-basics.md](./01-basics.md) for `ddl-auto` carries a one-sentence reminder per the link rule; no
      duplication found against any sibling file; es/10-herramientas.md is a faithful, natural-Spanish
      mirror with matching structure and code blocks — no calque found, no fix-es-quality needed.
      REWRITE_MODE = standard

## Existing files to inspect
(the orchestrator dispatches one notes-inspect-prompt subagent per file below; each appends its own
fix-quality / add-docs-link rows above)

- notes/java/en/00-intro-java.md
- notes/java/en/01-variables-types.md
- notes/java/en/02-control-flow.md
- notes/java/en/03-methods.md
- notes/java/en/04-oop-classes.md
- notes/java/en/05-interfaces-abstract.md
- notes/java/en/06-inheritance-polymorphism.md
- notes/java/en/07-collections.md
- notes/java/en/08-exceptions.md
- notes/java/en/09-streams-lambdas.md
- notes/java/en/10-generics.md
- notes/java/en/11-enums.md
- notes/java/en/12-dates.md
- notes/java/en/13-annotations.md
- notes/java/en/14-maven.md
- notes/java/en/15-memory-model.md
- notes/spring-boot/en/00-intro-spring-boot.md
- notes/spring-boot/en/01-basics.md
- notes/spring-boot/en/02-rest-controllers.md
- notes/spring-boot/en/03-dependency-injection.md
- notes/spring-boot/en/04-spring-data-jpa.md
- notes/spring-boot/en/05-exception-handling.md
- notes/spring-boot/en/06-security-jwt-learning.md
- notes/spring-boot/en/06-security-jwt.md
- notes/spring-boot/en/07-validation.md
- notes/spring-boot/en/08-transactions.md ← inspected, see row #8 below
- notes/spring-boot/en/09-testing.md
- notes/spring-boot/en/10-tooling.md

## Anomaly found (not fixed here — needs Victor's decision, not a mechanical Step 0 fix)

`notes/spring-boot/en/` has **two files sharing the number prefix `06`**:
`06-security-jwt-learning.md` (1137 lines) and `06-security-jwt.md` (2261 lines) — with matching
Spanish counterparts `06-seguridad-jwt-aprendizaje.md` and `06-seguridad-jwt.md`. The naming standard
requires one file per number prefix. Deciding which is canonical (or whether one is a draft to merge
into the other) requires reading both in full — that is inspection/judgment work, not a planner-level
mechanical fix, so both are listed above for the per-file inspectors and flagged here for Victor to
decide before a new file claims number 11 in the middle of a future run.

- [ ] #6 · notes/spring-boot/en/06-security-jwt.md
      TASK = fix-quality: missing narrative opening seam — file opens cold with the "Java syntax"
             table instead of picking up the thread from 05-exception-handling.md (why security comes
             now); missing narrative closing handoff to 07-validation.md at the end of Step 4; §"JWT
             signing algorithms — why HS256" table and §"AuthenticationProvider — why
             DaoAuthenticationProvider" table both lack the mandatory "how to read this table"
             sentence after the table.
             add-docs-link: no file-level `Docs:` line directly under the title (the "Documentation"
             section exists but sits ~600 lines down, after several sections already use concepts —
             structured-mode format requires the general Docs link right after the `#` title).
      REWRITE_MODE = standard

- [ ] #8 · notes/spring-boot/en/08-transactions.md
      TASK = fix-quality: (1) narrative seams — the file opens cold with "A transaction is a group of
      database operations..." with no thread-pickup line back to 07-validation.md (validation stops bad
      data at the door; this file's actual problem — one business action spanning several writes — is
      never framed as "now that input is clean, what happens when a single request must update more than
      one table?"), and the file has no closing handoff at the end — the last section ("Common mistake —
      catching the exception inside the method") ends on a code block with no forward pointer to
      09-testing.md; the same systemic narrative-seam gap already flagged for 00/01/02/03/04/06 on this
      worklist; (2) format inconsistency — per this folder's structured-mode rule every section should
      open with `Purpose:`/`File:`/`Docs:` metadata lines, but every section in this file uses only a
      bare `Docs:` line (or none, in "What is a transaction") with no `Purpose:`/`File:` fields at all —
      the same systemic gap already flagged for 01/02/03/04 on this worklist, now confirmed here too;
      (3) blockquote density is below the standard's bar — only ONE `>` callout in the whole file (the
      self-invocation/proxy-bypass note), across 7 sections, despite non-obvious mechanism questions the
      anticipate-the-TODO pass would surface with no callout answering them: why REQUIRES_NEW is the one
      case an outer rollback does not also roll back the inner write (§"Transaction propagation" states
      the behaviour but never traces that a suspended-then-resumed transaction is a genuinely separate
      commit unit), and why marking a method `readOnly = true` is safe to do liberally (§"Read-only
      transactions" states "annotate every service method" as a rule with no explanation of what could go
      wrong if you mistakenly mark a writing method read-only); (4) no real-world analogy anywhere in the
      file, despite the proxy-interception mechanism in "@Transactional does not work on private methods"
      being exactly the kind of abstract mechanism the standard calls out for one (e.g. the proxy as a
      receptionist who only manages the desk's visitors — a call that walks in through a back door,
      i.e. `this.method()`, never passes the receptionist at all); (5) show-the-exact-error-message gap —
      "LazyInitializationException" and "the annotation is silently ignored with no error" are both named
      but never quoted verbatim (e.g. `org.hibernate.LazyInitializationException: failed to lazily
      initialize a collection of role: ..., could not initialize proxy - no Session`), unlike the
      calibration file's exact compiler/runtime message rule; (6) missing "how to read it" sentence after
      the propagation table — the `REQUIRED`/`REQUIRES_NEW`/`SUPPORTS` table is followed directly by a
      "when to use REQUIRES_NEW" paragraph, but nothing decodes what "Join the existing transaction"
      concretely means for the caller's rollback outcome (i.e. that joining means one shared commit/
      rollback unit, so an inner failure rolls back the outer's work too) — the paragraph explains usage,
      not the table's own semantics.
      add-docs-link: docs-priority violation on 4 of 6 per-section links — "Where to put @Transactional",
      "@Transactional does not work on private methods", "Transaction propagation", and "Common mistake —
      catching the exception inside the method" all link docs.spring.io as their only reference, but per
      the standard's docs-link-priority rule Spring/Java concepts must lead with Baeldung, with official
      Spring docs only as a secondary link; only "Read-only transactions" and "LazyInitializationException"
      correctly lead with baeldung.com. Add a Baeldung link as primary for the four violating sections
      (keep the existing docs.spring.io link as secondary) — confirm exact sub-section URLs before
      writing, TODO if unconfident rather than guess.
      REWRITE_MODE = standard

- [ ] #5 · notes/spring-boot/en/05-exception-handling.md
      TASK = fix-quality: (1) narrative seams — no opening thread-pickup from 04-spring-data-jpa.md, no
      closing handoff to 06-security-jwt.md; (2) format inconsistency — only §"@RestControllerAdvice vs
      @ControllerAdvice", §"Error response DTO" and part of §"Handling Bean Validation errors" carry the
      full Purpose:/File:/Docs: triad; §"@RestControllerAdvice — the global exception handler",
      §"Custom exceptions" and §"Handling Bean Validation errors" have no Purpose:/File: metadata;
      (3) §"@RestControllerAdvice — the global exception handler" states handler-routing BEHAVIOUR but
      never traces the MECHANISM of how Spring picks a handler when exception types overlap in a
      hierarchy (a specific handler vs an Exception.class catch-all) — a real anticipate-the-TODO gap;
      no analogy for this dispatch mechanism; (4) §"Custom exceptions" — zero-assumption violation:
      .orElseThrow() is used with no explanation and no forward-reference marker to java/10-generics.md
      (where Optional<T> is fully covered); (5) §"HTTP status codes" table has no "how to read it"
      sentence.
      add-docs-link: §"The problem — without a global handler" has no per-section Docs link.
      REWRITE_MODE = standard

- [ ] #7 · notes/spring-boot/en/07-validation.md
      TASK = fix-quality: (1) HIGHEST PRIORITY — duplication + contradiction: the closing section
      "ConstraintViolationException vs MethodArgumentNotValidException" re-teaches
      MethodArgumentNotValidException handling from scratch with a WEAKER, INCONSISTENT
      handleValidation() (.findFirst() → single flat string, new ErrorResponse(400, message)) that
      contradicts the fuller, already-established handler in 05-exception-handling.md §"Handling Bean
      Validation errors" (Collectors.toMap field→message, buildError() + setFieldErrors(), exact JSON
      shown). Rewrite to REFERENCE 05's existing handler (link + one-sentence reminder) and show only
      the NEW piece this file adds — the ConstraintViolationException handler for @Validated path/query
      params. Do not leave two rival implementations of the same handler in the notes.
      (2) narrative seams — opens cold on the pom.xml dependency with no thread-pickup from
      06-security-jwt.md, closes with no forward handoff to 08-transactions.md; (3) no real-world
      analogy anywhere, despite "validate at the boundary" being exactly the kind of abstract mechanism
      the standard calls out for one; (4) no exact error message or response body quoted anywhere,
      unlike 05's sibling section which quotes the full JSON; (5) no // MAL / // BIEN labelled example
      anywhere (clear candidate: a DTO that silently is NOT validated because the starter is missing);
      (6) missing "how to read it" sentence after two tables — §"The annotations you use most" and the
      Trigger/Exception table in the closing section.
      add-docs-link: §"The problem without validation", §"Validate where the data enters — DTOs, not
      entities", and §"Custom error messages" have NO per-section Docs link at all (the other five
      sections in the file all have one).
      REWRITE_MODE = standard

## DEFERRED — notes/java/ rows (Victor's decision, 2026-07-13)

All `notes/java/en/**` rows above are OUT OF SCOPE for this run. Victor scoped this run to
`notes/spring-boot/` only. Leave them `[ ]`; do NOT build them. They remain queued for a future run
(`SCOPE = folder`, `TOPIC = Java`). The worklist file is therefore NOT deleted at the end of this run.
