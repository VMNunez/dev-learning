# Minimum Coverage — JavaScript

Core JavaScript concepts needed to write and review TypeScript/Angular code confidently.

## Variables and scope
- [ ] `var` vs `let` vs `const` — scope rules and why `var` is avoided
- [ ] Hoisting — what gets hoisted and what does not
- [ ] Closures — what they are and where they appear in real code

## Functions
- [ ] Function declarations vs arrow functions — the `this` difference
- [ ] Default parameters, rest parameters, spread operator
- [ ] Higher-order functions — functions that take or return functions

## Arrays
- [ ] `map`, `filter`, `reduce` — the three most important array methods
- [ ] `find`, `findIndex`, `some`, `every`, `includes`
- [ ] Spread in arrays: `[...arr1, ...arr2]`

## Objects
- [ ] Object literals, property shorthand, computed properties
- [ ] Destructuring: objects and arrays
- [ ] Spread in objects: `{ ...obj1, key: value }`
- [ ] `Object.keys`, `Object.values`, `Object.entries`

## Async
- [ ] Callbacks — the problem they cause (callback hell)
- [ ] Promises: `then`, `catch`, `finally`, `Promise.all`
- [ ] `async` / `await` — reading and writing async functions
- [ ] The event loop — why JavaScript is non-blocking (conceptual, not deep)

## Modern syntax
- [ ] Template literals: `` `Hello ${name}` ``
- [ ] Optional chaining `?.` and nullish coalescing `??`
- [ ] Logical assignment: `||=`, `&&=`, `??=`

## Modules
- [ ] `import` / `export` — named and default exports
- [ ] How module resolution works in a browser context

## Error handling
- [ ] `try` / `catch` / `finally`
- [ ] `Error` object: `message`, `name`, `stack`
- [ ] Throwing custom errors

## `this` keyword
- [ ] What `this` refers to in different contexts
- [ ] Why arrow functions do not have their own `this`
- [ ] `bind`, `call`, `apply` — when they appear in old code
