# Pipeline self-report — review-audit

**Date:** 2026-08-06 · **Project:** projects/07-timetrack · **Scope:** backend

**Status:** applied in b301d4b

- **Plan vs reality** — 5 resources (auth, users, projects, time-entries, reports) × flow+security, plus persistence-config (flow), security-infra (security), plus the Step 3b consistency pass = 13 dispatches, all required, all run. Map correct: every slice's trace covered its owned files with line count and closing quote on the first completed attempt. **Evidence limit:** no step in this pipeline reads the finished backlog whole, so this bullet rests on the traces, which are self-reports by the reviewers whose work is in question — it supports "the machinery ran", not "the findings are sound". The orchestrator did independently verify the two Highs against real code and `git log`/`git show` before filing them.
- **Report discipline** — clean. All 13 subagents returned bounded findings tables + traces; nothing had to be trimmed or discarded. Two reviewers correctly cited secrets by location and kind only, including the exposed one.
- **Failures & retries** — the whole of batch 3 (reports flow, reports security, persistence-config, security-infra) was killed mid-flight by an `API error: session limit`, four subagents at once, returning only partial preambles. Not a pipeline failure and not a trace failure — no partial report was accepted as clean, which is what the "no trace, no review" rule is for. Re-dispatched in two pairs instead of one batch of four; all four then returned complete traces. Zero slices left "not reviewed", zero genuine re-dispatches for bad work.
- **Rule friction and rule breaches** — no rule breached. One real defect in the prompt, which is this run's finding: **Step 0's unreviewed-code gate had only one operational test — a completed ✅ step — and a backlog-fix campaign moves no step.** The backend was 14 days inside the 30-day window with no ✅ step gained, but 76 commits and 49 closed backlog tasks after its last review, so the letter said "stop and offer FORCE" while the rule's own stated purpose said run. The cold reviewer found that the **previous** run (2026-07-23) had already bypassed on closed tasks alone and graded itself "pipeline clean" without noticing — two runs reading past the test to the purpose. Dedup was light this time: the backlog was empty of open tasks, so cross-slice overlap (email normalisation ×3, self-demotion ×2, inactive-project oracle ×2, `getSummary` fetch ×3, `sort` ×2) merged into single tasks without ambiguity. No cross-slice contradiction arose. Two ledger checks changed a verdict rather than a wording: `UpdateTimeEntryRequest` was filed as plan-drift only, not "delete the duplicate DTO", because the `## Closed` ledger shows the split was a deliberate 2026-07-29 close; `ddl-auto=update` was dropped entirely as a documented §20 decision.
- **Verdict** — change worth considering, drafted and applied: the gate now reads two signals, either one enough — a ✅ step, or a dated `## Closed` line for that tier. Tightened two existing sentences in `review-audit.md` and `_review-standard.md` rather than adding a clause, and removed the vague "the backlog's own task notes" ancestor the precise clause supersedes. `review-audit.md` is 477 lines, under the ~500 budget; largest section is "Single-project procedure" (~290 lines).
  **cold reviewer: approve-with-tightening** — required dropping "in this order" from the two signals (they are OR'd; a stated precedence invites the inversion "the ledger only counts if no step moved"), and named the removable stale line. It also recorded a residue this edit does **not** close: code that moves no step *and* closes no backlog task — ordinary in-step commits — stays invisible to both signals, and it advised against a third `git log` signal that would turn a bounded test into a judgement call.
