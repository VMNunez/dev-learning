# Pipeline self-report — review-audit

**Date:** 2026-09-23 · **Project:** projects/07-timetrack · **Scope:** frontend

**Status:** open

- **Plan vs reality** — 11 frontend slices (login, dashboard, entries, entry-dialog, approvals, projects, team, reports, shell-account, frontend-infra, styles-theme) + the Step 3b consistency pass = 12 dispatches required, 12 run, all standard-tier. Two mapping choices are worth recording: shared components and services were owned by their consuming feature again, which split cleanly for the second run running, and `styles-theme` was added as an 11th slice rather than folded into `frontend-infra`, because the six files in `src/styles/` are §14's real contract surface and `frontend-infra` was already 28 files. Every file under `src/` plus `angular.json` is owned by exactly one slice. No step reads the finished backlog whole, so this rests on the traces plus the orchestrator's own verification of every surviving finding against real code.
- **Report discipline** — one failure and one near-miss, both about the delivery channel rather than the content. `entries` returned a prose summary whose table and trace it said were "in my final assistant message", which is not what reaches the orchestrator; it was re-dispatched and came back correct. Batches 2 and 3 carried an added dispatch line requiring the two blocks **in the reply itself**, and none of those eight reviewers repeated it. No code dumps anywhere; nothing had to be discarded for length.
- **Failures & retries** — 1 of 12 traces failed the Step 5 check (`entries`, no trace at all). One re-dispatch, returned with all 12 file rows, valid line counts and closing quotes, and findings unchanged in substance. No slice was left "not reviewed", so the frontend line takes a plain date with no `(incomplete — …)` qualifier. The trace closing-quote rule, which cost two re-dispatches on 2026-09-19, was restated in every dispatch this run and broke nowhere.
- **Rule friction and rule breaches** — no rule breached by the run. The Step 5 reconciliation did real work and the cross-slice contradiction clause earned its place: `dashboard` filed a **High** asserting that `.stat-value.skeleton` has no fill and that no `@keyframes` pulse exists anywhere in the app, having grepped `src/app` and missed `src/styles.scss`; `styles-theme` reported the same pulse as present and correct. Resolved against the file — `styles.scss:37-52` carries the background, the animation, the keyframes and the `prefers-reduced-motion` guard — and the High was dropped. It would otherwise have been this run's only High. Three more findings were dropped on verification: `dashboard`'s `.table-wrapper` Low (the partial declares that border on `.table-area`, a different selector), `entry-dialog`'s missing-spec Medium (`confirm-discard.ts` is in no §16 row and Step 9 is unbuilt, so the test exclusion covers it), and `frontend-infra`'s `localStorage` JWT (§10's "Token lifetime" rules on it explicitly, so the silent-plan branch does not apply). One consistency finding was dropped against the `## Closed` ledger: the smart/dumb outlier re-raised a question a 2026-09-18 `DECISION, no code change` line already settles. One duplicate was merged — `entries` and `team` each found the failed-reload focus gap in their own file; it is one shared defect across all seven list pages and is filed once. `FRIC-0001` adjudicated and dismissed on condition 3.
- **Verdict** — change worth considering: **`_review-flow-prompt.md`'s Output section never says where the report must be delivered**, only what it must contain. Two runs have now lost a slice to that silence — `frontend-infra` persisting its trace to a scratchpad on 2026-09-19, `entries` answering with a pointer to an earlier message today — and each cost the slice its single re-dispatch, which is the only thing standing between the silence and an `(incomplete — …)` qualifier written into the backlog. Clears all four bar conditions, including 3: a second failure writes a different file. The eight dispatches that carried an explicit delivery line this run are the control. `review-audit.md` is 517 lines, over the ~500 smoke alarm; its largest section is "Single-project procedure" (~386 lines). The proposed edit is one clause in a 159-line component, so one-in-one-out does not apply.
