# Breach log — portfolio-audit

Append-only. Rows are never deleted; a closed row stays as the evidence that this step was once a
problem. Contract: `notes/prompts/_internal/_pipeline-self-report.md` → "The breach log".

| ID | Date | Target | Breached step | Scope | Evidence | Disposition |
|---|---|---|---|---|---|---|
| BRCH-0001 | 2026-09-09 | `PROJECT_PATH = projects/03-expense-tracker`, `PORTFOLIO_SCOPE = full`, `DRY_RUN = true` | `CLAUDE.md` → `Claude Code adapter` | shared | The adapter mandates reading `_session-rules.md` completely before giving guidance or changing files; it was read only at Phase 3, after the bank, the twin and the CV bullet had been written. No commit was made under the gap and nothing had to be undone, but the boundary reads came after the writes rather than before | routed to REC-232 |
| BRCH-0002 | 2026-09-12 | `PROJECT_PATH = projects/04-meal-finder`, `PORTFOLIO_SCOPE = full`, `DRY_RUN = true` | `CLAUDE.md` → `Claude Code adapter` | shared | Same step as `BRCH-0001`, one run later: `_session-rules.md` was not read before the writes, and not completely even then — it was opened at the close-out, after the 126-question bank, its `es/` twin, the `Last banked` stamp and the tracker cell had all been written, and then only its outline plus four sections. The cost is no longer hypothetical: the non-negotiable **"No `Co-Authored-By` lines"** lives in that file while this session's harness reminder instructed the opposite footer and claimed to supersede earlier attribution guidance — on `DRY_RUN = false` the bank commit precedes this read, so that ordering ships a forbidden footer. Caught here only because the dry branch defers every commit past the read. Second row on one step → threshold cleared, routed rather than fixed (scope is `shared`) | routed to REC-232 |
