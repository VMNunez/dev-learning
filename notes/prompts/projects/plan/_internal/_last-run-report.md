# plan-audit — last run report

**Date:** 2026-07-28 · **Target:** MODE = review, PROJECT = projects/07-timetrack (third review pass of the day — the first run under the six-specialist split and the rewritten §14 standard)
**Status:** applied in 984ff41 · a second finding from the same run applied in 013f1bb (see the post-run addendum below)

1. **Plan vs reality** — the six-way split did the job the five-way split could not: `ui-design`, dispatched as its own concern for the first time, found §14 **absent from the plan entirely** and wrote it from scratch (8 fixes). Under the previous shape that section rode at the tail of `data-model-api`, which is exactly where REC-013 predicted it would be skimmed. `branches-coverage` separately found **§1 missing** — the plan had been shipping with 23 of 24 sections through several prior passes, so the section-coverage check only fires when the specialist genuinely enumerates rather than spot-checks. Check counts 15/18/19/10/20/17.
2. **Report discipline** — nothing trimmed or discarded. All ten returns (six specialists + three reconciles + the cold reviewer) opened with a `wc -l` count and read-to-EOF confirmation, and none pasted plan content. The Bash-vs-`Measure-Object` instruction was carried in every dispatch after the previous run's incident: no line-count mismatch this run.
3. **Failures & retries** — no acceptance-check failures and no unusable work. Three reconciliation re-dispatches, all from a single root cause (bullet 5): `ui-design`, `data-model-api`, and the ripple handed forward into `steps-tests`'s first dispatch. The one-per-concern cap held everywhere; `architecture` also took its one re-dispatch, to adjudicate a deploy-step referral it resolved correctly against the standard's item 14/15 split.
4. **Rule friction and rule breaches** — no rule breached by the orchestrator: step-0 guards ran (branch `fix/backend-backlog`; the previous report's `Status: applied` needed no surfacing), all ten dispatches were deep-tier, the orchestrator never read the plan or the standard itself, and both gates re-ran before the commit (acceptance ✅, history preservation ✅ — Steps 1–6 still ✅, §0 done-condition byte-identical, Step 7a still the in-progress position). One spec ambiguity: `_pipeline-self-report.md` mandates reconciling the recommendation ledger, but its commit rule says the report commit "must list **two** files" — the ledger is a third. Committed together here, so the stat check was applied as report + tracker + ledger.
5. **Verdict** — change applied: **REC-014**. §10 declared an endpoint with no UI consumer anywhere in the plan — no route, component, dialog, wireframe, build step or frontend test — and **no mandated check maps §10 to §13**, so `data-model-api`, which owns both sections, passed its slice green. It surfaced only as a discretionary coherence observation by `rules-security`, a specialist that owns neither section. Bar condition 3 is met on the counterfactual, not on this run's cost: a run where nobody happens to notice commits a plan that builds, tests and documents an unreachable feature. `cold reviewer: approve-with-tightening` — it cut the drafted "and drawn in §14" clause as both a near-duplicate of invariant 3 and factually wrong for dialogs (which §14 does not wireframe per page), required the review prompt's *reading map* and line-130 ownership split to be edited alongside the *Owns* cell, and caught a pre-existing error in that same line (`branches-coverage` listed as owning invariants 6 and 8 when the scope table gives it 6, 8, 9 and 10) — repaired in the same edit. One-in-one-out honoured: the §16 self-labelled "Reminder, not a check" parenthetical about interview-prep came out, verified unreferenced across the `plan/` family. `_planning-standard.md` 563 → 565; `plan-audit.md` is 277 lines, well under budget. REC-012 reproduced a fourth time and stays `rejected` on bar condition 3 — the ripple protocol again reached the right result unaided. Also corrected outside the bar, as verified staleness rather than a finding: `notes/prompts/README.md`'s `plan-audit` row still described five specialists (no `ui-design`) and a `DRY_RUN` switch retired 2026-07-16.

**Post-run addendum — REC-015, the finding this report could not have produced.** After the commit, Victor asked for the plan to be read end to end. That single whole-file read found five defects the all-green six-way review had passed: §5 dated Docker to the wrong step, §18's README plan described the opposite of §6's state rule and omitted a component §13/§14 had just added, §14's Reports card carried a label §8 forbids, and `PROJECT-BACKLOG.md` still specified the status code §8 had just overruled. Root cause is **coverage, not depth**: the six rows own 14 of 24 sections, leaving ten with no content owner, and no row reads the backlog. Remedy applied in 013f1bb — a seventh `whole-plan` specialist, dispatched automatically after the six and **before** the orchestrator's commit. The machinery lesson worth carrying: **this self-report is written by the orchestrator, which by design never reads the plan**, so a coverage gap in the specialist table is invisible to it — bullet 1 reported "the six-way split did the job" while five defects sat in the committed file. The new pass is the structural fix; the report's blind spot is not.
