# Pipeline self-report — readme-audit

Date: 2026-09-02 · Project: projects/04-meal-finder (Angular → target `global` only)
Status: open

- **Report discipline** — nothing discarded or trimmed; all four dispatches returned the `N lines, read to EOF` proof for every file assigned whole, and none blew its budget.
- **Trace verification** — the reviewer's trace covered all 12 required sections in standard order with a per-section verdict; no gap, no re-dispatch, no false alarm. The applier's re-dispatch returned a second full trace unasked, which cost nothing.
- **Coherence** — N/A: Angular project, single README, coherence subagent correctly skipped.
- **Effect judge** — 3 items + 3 KEEPs on the one target; applier B applied all 3 and returned `Objections returned: none`; 0 items carry `⚠ regenerable — standard gap` by the flag's own wording, which is scoped to `What I learned`. **The zero-objection count is the finding, not the reassurance.** One of the three was the `effect-only` cut of `Future improvements` that shipped in `367c3c97` and that Victor reverted in `98daae04` — the same two bullets, the same section, `3 → 1` a second time — applied under `REC-199`'s widened clause without that clause being reached. Per bullet 1 the judge outranks the green traces, and here it outranks its own applier: the item was right about wish-list prose and wrong about which bullets carry it, and nothing downstream of the judge tested it. **No objection reached the orchestrator's arbitration, so the arbitration never fired**; the catch came from the `git diff` the commit rule already requires, and B reinstated both bullets on a re-dispatch that named rule 8's two inclusion tests. Settled as `sustained` in substance, by a route the prompt does not describe.
- **Failure protocol** — not triggered; no subagent errored, no re-dispatch under it, no README excluded from the commit.
- **Anything else** — the verification dispatch above is not in this prompt. It was justified by `_agent-runtime-standard.md`'s orchestrator clause (*"must verify returned evidence before editing or committing"*) against a silent application, and it is the whole reason the commit is correct. Recorded as an extension, not a breach: no step was skipped and no rule contradicted — but a pipeline whose correctness this run depended on an undocumented check is the machinery fact worth carrying.
- **Verdict** — **open**: `REC-199` widened *what an applier may invoke* and added nothing that makes it *look*, so on the first run under it the applier never reached the clause and reproduced the exact defect it was written to prevent. Clears all four conditions — it happened this run (1), the prompt is silent where it needed to speak, since an applier's default-to-apply plus a passive permission to object leaves the test unreached (2), the committed file would have been wrong and identical to the state Victor already reverted once (3), and `REC-199` covers the width of an objection and not the obligation to test (4). Not drafted here: routed to `_recommendation-ledger.md` as **`REC-202`** (`a9e5c20e`), whose step 1 must first weigh the applier against the judge as the cheaper site — the judge proposed a rule 8 cut without reading rule 8. (`REC-198`, open against this file's close-out wording, was again not reached by this run's evidence.)
