# Prompt-system recommendation ledger

Self-report recommendations use one of four states — a close-out writes `open` or `accepted` and nothing
else, which is what the two self-report contracts and `_system-map.md` §12 state. A fifth, `accruing`, is
raised by hand only:

- `open` — observed and not yet adjudicated.
- `accepted` — agreed, with implementation still pending.
- `applied` — implemented and verified, then collapsed into one line in `_recommendation-ledger-closed.md`.
- `rejected` — intentionally not implemented, then collapsed the same way, with the reason kept.
- `accruing` — not workable and never scheduled: its input is **lived use**, so it fills up rather than
  being run, and it is not part of the queue. `REC-054` only; a second one would be a smell.

**This ledger holds defects in the *machinery* — a prompt, a skill, a standard, a launcher, the validator
or either map — and nothing else.** Scope stated 2026-08-10, when three rows were deleted for failing it.
Three tests, in order. **Is the wrong thing a file or a cell?** A prompt that would produce the right
output if someone ran it is a cell in `_run-tracker.md`, never a row (the `REC-046` rule, in `_recommendation-resolution-doctrine.md`). **Whose
file is it?** A defect in a project's `PLANNING.md`, a practice doctrine, a coverage or a notes file is
owed to that file's declared writer, and a machinery session may not hand-edit it — so it is ours only
where the *owner's* text is also wrong, which is `REC-083`'s split. **Does it need a lived day?** Anything
measured from how much a ritual has actually been used is evidence that accrues (`accruing` above), never
work that is queued. A finding that fails these tests is still written down — in the closed line of the
row that found it, in `_recommendation-ledger-closed.md`, or in the tracker cell that owns it — it is simply not carried here, because a
ledger holding operational worklists is one nobody can drain.

**But `_run-tracker.md` holds a debt only in its own defined `⚠ stale` form**, which the owed prompt is
built to clear on its next run. Free prose appended to a cell is not a sink: `_pipeline-self-report.md`
tells every close-out to **overwrite** the cell, so the next run of that prompt deletes the note without
having read it — and the audit prompts read their `_last-run-report`, not this file. `REC-102` wrote
such a note and reverted it on its cold reviewer's finding. When the owner's own standard already
carries the check, the closed line is the sink and the standard is what carries it forward.

## How an item is resolved

Four steps, in order, fixed 2026-08-07. The ledger reached 127k characters before this was written
down, and the three items before it each cost more than they were budgeted for.

**The steps are here; their case law is `_recommendation-resolution-doctrine.md`, beside this file.**
Split out 2026-08-18, when the promoted rules of every closed row had grown to four fifths of this
file and the queue was the part nobody could find. Steps 1 and 3 cite it by section; a rule a closure
promotes lands there and never here, which is what stops this file growing again.

1. **Analyse the problem, never only the row.** A row records where a defect was *found*; it is
   routinely wrong about where the defect *lives*. `REC-053` named one file and the rot sat in five.
   `REC-051` named three sites and there were six, two of them naming the gate by the very thing the
   item removed. `REC-057` asked for a check that already existed and was blind not to a *location* but
   to a path **form**. Measure against disk before proposing anything, and budget the sweep rather than
   the edit. **Name the set you measured**: step 3 hands it to the reviewer, and a measurement nobody
   wrote down cannot be handed to anyone.
   **The case law of this step — every way a measurement has been wrong before, and the tests that
   cost one command each — is `_recommendation-resolution-doctrine.md` → "Step 1". Read it before
   proposing anything; it is where the promoted rules of every closed row live.**
2. **Resolve from that analysis, not from the row's wording.** When re-verification falsifies a clause
   of the finding, correct it in writing and say so. Several items were worth more for what their
   re-verification uncovered than for what they originally claimed.
3. **Run a cold reviewer. Mandatory, no exceptions.** Both self-report contracts have always stated
   this gate unconditionally. The `REC-025` "carve-out" that let verified factual corrections skip it
   is **void, not superseded — it never existed**: that row ruled on *bar condition 3* (a false
   statement in a dispatch document is a defect regardless of what it earns), which is about whether a
   finding is worth applying, not about who checks the fix. Five rows cited it to skip the gate anyway.
   The reviewer then caught items doing real damage on exactly that footing: `REC-041-B` was falsifying
   the very sentence its own predecessor had just added as a root fix, and `REC-051` would have stopped
   a tier that had never been reviewed at all — `07-timetrack`'s frontend, on its own scheduled gate. A
   fix being factual does not make it safe. **A gate that cannot be *reached* leaves the row `open` and
   the working tree accounted for.** Step 2 has already written the fix to disk — unlike the self-report
   refinement gate, where the edit is still a draft in context — so a dead reviewer leaves an edited,
   uncommitted tree that reads exactly like an applied fix on the next `git status`. Run the standard's
   dispatch ladder to its end. Only when it is exhausted does the edit stop waiting, and **parking it is the equal of reverting, not the lesser option** — a resume may have to
   wait for the next session's budget, so park the edit with the row naming where it sits and what it is
   still owed. What is forbidden is neither: an edited tree with nothing recording why.
   **One reviewer per row, dispatched one at a time** — a batched reviewer would have to be cold on
   unrelated analyses at once, and three parallel deep reviewers is what met the session limit on
   2026-08-10.
   **What it is handed.** Both self-report contracts state the five inputs — the scratch path among
   them — and the `N lines, read to EOF` proof; neither is restated here. **Three of the five take a
   different form on this path**, because the object is a fix **already on disk** over a site set the
   row did not define: the finding **as step 2 corrected it**, not as the row worded it; the working
   tree as it stands, not a drafted replacement; and, in place of the whole prompt file, **every file
   step 1 measured, read whole** — the untouched ones above all, since that is where the sweep either
   holds or does not (`REC-069`'s fourth copy fell only to a reviewer reading the family). That set is
   a **floor, not a ceiling**: the dispatcher names it, because unnamed it resolves inside a saturated
   context to the files the fix happened to edit, and a site the reviewer finds outside it is
   `sweep: incomplete` — a short measurement is the failure step 1 exists to catch, so bounding the
   reviewer by it would only move the blind spot.
   **Its return carries two lines beside the verdict token, and a persisted return missing either one
   is a partial return rather than a verdict** — the standard's two-part test is not sufficient on a
   path where the edit is already on disk when the reviewer dies. `sweep: complete` /
   `sweep: incomplete — <sites still owed>`: *right but incomplete* is neither a wording note nor a
   rejection, so it leaves the row `open` and the edit parked **whatever the token says** — the three
   tokens were written for a drafted edit, and this path has already improvised a fourth (`revise`).
   Then the two-map test's own declaration, `maps unaffected` / `maps: <map> — <row>`, because the
   reviewer is the last gate before the commit that has to carry that edit.
   **Write the returned token into the row the moment it returns, not at the collapse.** The review and
   the collapse are routinely different sessions, and the second cannot recover what the first never
   wrote down: `REC-190`'s verdict was lost exactly that way and its line now reads
   `cold reviewer: unrecorded (REC-195)` forever. The token costs four words in the Resolution cell.
   **The rounds are what this gate costs, and the loop has four controls.** Only `reject` or
   `sweep: incomplete` opens a round — `approve-with-tightening` means what it says: apply it in the
   form approved and close the row. The reviewer passes the bar it applies, so a finding that changes
   the *cost* and not the result is a tightening and never a blocker. **Two rounds, and a third does
   not repair: it re-scopes** — revert the out-of-scope edit, open it as a row of its own carrying the
   revert and its reason, and let the reduced fix stand alone. **The one third round that is neither is
   a *verification* round** (`REC-180`): where both rounds returned `reject` and every finding of both is
   on disk, the fix is finished and step 4's gate is unreachable, because the loop that produces an
   approving token is over. It asks one narrowed question and is calibrated for it; the doctrine's
   "Step 3" holds its shape and the two conditions that owe it. A repair states no new fact. Round N is
   handed round N−1's return, whose *considered and not opened* list binds it — a cold reviewer
   re-litigating a settled adjudication is the loop, not the check. The instances, the shapes that
   produce a fourth round, and the two tests before splitting are in
   `_recommendation-resolution-doctrine.md` → "Step 3".
4. **Collapse the row into `## Closed` in `_recommendation-ledger-closed.md`** — only a `sweep: complete` review returning `approve` or
   `approve-with-tightening` may reach this step; `reject` and every incomplete sweep leave the row
   open under step 3. The one-line closure carries the ID, what the item was,
   `cold reviewer: approve | approve-with-tightening` — or the `unrecorded (REC-NNN)` **record**
   `_recommendation-ledger-closed.md`'s header defines, which annotates a closure already on disk and is
   never a route to this step, written in that form because the citation is read from the token's own
   parentheses and nowhere else on the line (`REC-210`) — the two-map declaration, and the implementation
   commit. This reviewer field starts with `REC-107`;
   historical lines are not retrofitted. **`validate-prompt-system.ps1` invariant 9 reads that
   schema** — the shape, the ID order, the commit field, the two-map declaration from `REC-058` and
   the verdict on any line naming a real commit — so a closure that drops a field fails the run
   instead of being found by the next reader. **Run it before the closure commit, not after**: nothing
   triggers that script automatically, and `REC-190` was collapsed on 2026-09-01 missing two fields and
   a verdict that by then no artefact held — found a day later by a run doing something else, when the
   token was already unrecoverable (`REC-195`). One command, at the one moment it can still be repaired.
   What it cannot see is whether the reviewer ran;
   `README.md` states that limit with the rest of its contract. Before collapsing, promote any rule the row established that
   governs **future** work into `_recommendation-resolution-doctrine.md`, the file this preamble's steps
   cite. A rule that other items obey must not stay buried in a
   row about something else; that is how the `REC-025` precedent came to be cited seven times from
   inside a row about SQL specialists — and misread every time, which is the second reason to promote a
   rule rather than cite it across rows.

   **A closure has a budget, and it is one line plus at most one promotion.** Written 2026-08-18, when
   this file was cut from 324k characters to a third of that. The engine that produced those 324k was
   not the open rows — they were 3% of it — but a closure that cost about 5k every time: a `## Closed`
   "line" of a thousand characters restating the whole resolution, two or three fresh preamble rules,
   and a retrospective paragraph under the order section. All three failed a rule this file already
   carried. So: the closure line is the schema above and nothing more, and the reasoning is in
   `git log -p` on this file, where the row's own commit already put it. A rule the row established is
   **merged into the rule it is an instance of**, cited as `Also REC-NNN` with the one clause it adds —
   a new standalone rule only where no existing one covers it, which is rarer than it looks. And the
   pricing lesson goes in the doctrine's row-shape table as a row, never as a paragraph. If the closure will
   not fit that budget, what is over-running is the promotion, and the test is whether the extra text
   states a rule a future row must obey or retells the case that produced it.

**Every row shape this ledger has seen, and what each actually costs, is the table in
`_recommendation-resolution-doctrine.md` → "Row shapes, and what each actually costs".** Price the row
against it before analysing anything; each of its rows was written by an item that was priced wrong on
arrival, and a new pricing lesson is added there as a row and nowhere else.

**Collapsing rows by script: split on `(?<!\\)\|` and assert exactly 7 fields per row.** A plain
`split('|')` silently deleted a just-written row on the first attempt, because cells legitimately carry
escaped pipes (`REC-060` quotes a shell pipeline). The row was recovered only because the collapsed
count disagreed with the hand-written list by one — so count both sides and compare the ID sets before
writing anything. The strict field assertion then caught a *second* case, an unescaped `|` that was also
breaking the table's rendering.

**Moving an item to `applied` includes the two-map test** — `_session-rules.md` → "The two maps follow
every change to the machinery". An item that changed what a file contains, who writes it, when something
runs, or which prompts and skills exist carries its `README.md` / `_system-map.md` edit **in the same
commit as the fix**; one that changed none of those is reported as `maps unaffected`. **The `## Closed`
line carries that declaration in the line itself**, so a later reader can tell a checked map from a
forgotten one. The 46 lines written on 2026-08-07 do **not**: seven old rows carried a declaration and
the collapse dropped all seven, so for those the evidence is in `git log -p` and the field starts with
the next item collapsed.

## Open

**What may become a row is the scope test above, applied twice.** Once when a run proposes one, and
again when a resolution's own cold reviewer returns with a finding of its own — at the moment it
returns, because a reviewer is paid to find things and filing each of them as `open` is how this queue
came to refill at the speed it drained: `REC-081`, `REC-086` and `REC-087` were each raised by the
reviewer of a *different* row. The bar, and where a finding that fails it is written down instead, are
in `_recommendation-resolution-doctrine.md` → "An incidental finding is evidence, not automatically a
row".

**Two of the intake routes hand over a row that is only half-decided** — an `absence` row from
`/system-gaps`, a `contradiction` row from `/system-check` — and how each is resolved is case law, not
queue: `_recommendation-resolution-doctrine.md` → "Row dispositions". Step 1 is not optional for either:
the settling file is read first, because a row whose absence branch turns out to be the map's is a
`map-sync` repair and never an edit to the machinery.

**Say the workable number and name the accruing one separately; they are not summed.** `REC-054` is the
only accruing row and is not part of the queue. **An empty table is not a finished system**: it means
the next row comes from a run, not from this file, and `/system-gaps` and `/system-check` are what
produce one.

| ID | Source | Recommendation | State | Resolution |
|---|---|---|---|---|
| REC-054 | Victor, 2026-08-06, alongside the two-map rule; **reshaped 2026-08-10 on Victor's own falsification of it** | **The lived-day review cannot be scheduled, because its evidence does not exist — so it stops being an audit that is run and becomes a verdict that accrues while Victor studies.** What it judges is unchanged and is still the only question nothing else in the system asks: do the current 08:00, 12:30 and 13:30 loops fit a real day, are the interview opener's grading and the block closers cheap enough to keep running, what does each block leave unrecorded, and does ritual load outweigh the work it records. Its original day snapshot is also still obsolete — the 13:30 block now has `interview-prep-block-open` plus `study-block-close`, authored/refined/studied state is separated, and there are eighteen mirrored skills. What changed is that **there is nothing to judge yet**: the rituals have barely been exercised, so the review has no input and would fabricate one. Three parts, in order. **(a) A capture point — built 2026-08-10, `_internal/_ritual-friction.md`.** `_skill-friction.md` accepts only an observable *failed declared step* (`FRIC-NNNN`), so the complaint that will actually occur — "this ritual ate the block", "nothing ever reads this output", "I do it by hand anyway" — was unrecordable and died in the session it was said in. It is the machinery-level instance of exactly what `sql-block-close` was built for: **friction without failure**. It is a separate file on purpose: `FRIC` rows are adjudicated by the next close-out's four-condition bar and can become a `REC`, which is the one thing (b) forbids, and one file holding two kinds of row under a consumer that counts them is the `REC-074` smell. **(b) The rule that keeps this out of the refill loop:** a ritual-friction line **never opens a `REC` and never dispatches a cold reviewer**. One line, written as it is said, accumulating. **(c) The verdict, per ritual, once its evidence is enough to rule on** — kept, thinned, or deleted. This is the only item in this ledger licensed to **remove** machinery; every other row has added a check, a pointer or a rule | accruing | **Not workable, never scheduled, and it gates nothing** — it is the one `accruing` row and the reason that state exists. Its original sequencing said to run it once the machinery stopped moving, and that premise was the wrong one: the binding constraint was never **motion**, it was **absence of use**, which no amount of waiting for the machinery to settle repairs. Its input is the `RITF` rows themselves plus whatever `_run-tracker.md` shows has actually run — not a measurement pass built for it, which is what `REC-070` (b) was and why that row is gone. Do not schedule it and do not let it block a row again. **(a) shipped 2026-08-10** — the sink, its `_session-rules.md` trigger and its `_system-map.md` §7 and §11 rows — so the row is now fillable and what remains is (b) holding in practice and (c) having something to rule on. **(b) is not a build: it is a prohibition**, and it is stated in all three places at once (the sink, the session rule, this row) rather than in one, because the failure it prevents is a future session skipping straight to a `REC`. `REC-055` (e) was parked with it and is **unparked 2026-08-10**: a map that states *which* rituals fire in which block is a structural description, and this row is a verdict on whether they are worth their cost — the two do not collide, and the second is not a prerequisite for the first. Maps unaffected by this reshaping — no prompt, skill, writer, trigger or file existence changes |
| REC-184 | Victor, 2026-08-29, stating what the TODO loop is *for*: from his TODOs plus the initial and final version of an answer, the system should extract how **he** answers and progressively refine the whole of `interview-prep`, not just the file in front of it | **`REC-171`'s harvest sink exists, is empty, and is scoped to note prose — it cannot see a single interview answer, and its own design refuses to store the one thing this needs.** Measured on disk 2026-08-29: `_note-todo-harvest.md` holds **0 rows**, its header names `_note-quality-standard.md`'s two rule families as its only consumer, and the string `interview` does not appear in the file. Its § *The raw archive stays in git* is a **deliberate** refusal of exactly the payload requested here — *the prose that provoked each complaint is not in it and never will be* — decided under the countability argument (`REC-171` (a), one row per pair and category, never per TODO, because `REC-054` watches the per-row cost inside a daily block). So this is not a scope widening of a sink that already does the job: **it is a second, different question asked of the same evidence.** `REC-171` asks *which rule of the standard was missing or unapplied* — a category, countable across pairs. This asks *what is Victor's answering voice* — which is only legible in the before/after pair the sink was designed not to hold. Note `study-content-writer` **already spans both families** (its trigger names `notes/{topic}/` and `notes/interview-prep/{LEVEL}/en\|es/`), so the writer that would collect this is in place; what is missing is a sink it may write to and a consumer that reads it | open | **Rule first whether this is one sink or two, because the countability argument does not transfer and pretending it does is how the first row gets written wrong.** An answering-voice corpus is valuable *per instance* (one before/after is already a datum), where a rule-breach index is valuable only *in aggregate* — so the `one row per pair and category` invariant and the git-archive refusal are both properties of `REC-171`'s question, not of this one. The likely shape is a **separate sink** under `knowledge/interview-prep/_internal/`, holding the question ID (`REC-180` is what makes IDs exist for the project bank), his verbatim TODO, and the before/after answer text — with the `REC-054` cost objection answered explicitly, since storing prose is what `REC-171` priced and declined. **Sequencing is forced and must be said before starting**: this is blocked behind `REC-180` (no IDs, no `es/` twin, no TODO channel in the project bank), `REC-183` (which direction a TODO is repaired in), and it should not precede `REC-171` (g)'s first harvest — building a second extraction mechanism while the first has never run once produces two untested designs. **Victor asked 2026-08-29 for the standard's refinement to be *automatic*, from the accumulated corrections, every TODO he writes and the refined files — and that collides head-on with `REC-171` (h), his own ruling on the same mechanism one folder away:** *the standard is hand-written only, so a due category opens its own `REC-NNN` and is resolved under the four steps with the mandatory cold reviewer — never by the skill, never by a run*. The collision is real but narrower than it looks, because *automatic* names two different things and only one of them was refused. **Automatic detection was never in dispute and is already the design**: the writer collects on every resolution, the counter prints on every close-out, a recurring pattern is surfaced and its rule **drafted** without Victor initiating anything — he is told a rule is due, not asked to notice it. **What (h) refused is the unreviewed landing**, and the reason transfers here with more force, not less: `_interview-prep-standard.md` governs every question written afterwards, so one bad auto-derived voice rule is applied silently to the whole future bank, and the only reader who could catch it is the one whose voice it claims to describe. **So the shape to price is: automatic up to an approved draft.** Whether the approval is the full four-step `REC-NNN` route or a lighter in-session gate (one cold reviewer + Victor's word, no ledger row) is the decision this row owes, and it is the same decision for `notes/` — whatever is ruled here should be ruled for `REC-171` (g)/(h) at the same time or the two mechanisms diverge. **One limit is not negotiable and comes from the freeze, not from cost**: a derived rule never rewrites a question already `[refined]`, which would overwrite the very answers the pattern was derived from. It governs what is written next **Step 1 reads `_internal/_interview-voice-spec.md` first** — the target flow this row is one slice of, written down so a resolution sees the whole loop before editing its corner; it is a target, not a description of what runs, and a standard beats it wherever they disagree. |
| REC-164 | both cold-review rounds of `REC-163`, 2026-08-19, as a `sweep: incomplete` site verified against quoted sentences from each `SKILL.md` and **re-scoped under the two-round cap** — `REC-163`'s own four cell corrections were reverted with it, because a column measured in four of twelve places reads as measured | **`_system-map.md` §9's `Primary reads` is incomplete in both directions, across ten skills.** The column claims each skill's reads and `/system-check`'s `Purpose` says this map owns them, so an omitted read is `missing claim` and a claimed one the skill never opens is `incorrect`. **Omissions, by the section of the `SKILL.md` that mandates each:** `coverage-mark` — its *read before editing anything* sentence (`_coverage-standard.md` § "Evidence markers" **and** `_topic-ownership.md`) plus §4's `progress-update-prompt.md` step D8; `coverage-bullet-add` — §5's step D8; `sql-step-close` — §1's `_coverage-standard.md` → "The drill marker" and §2's `_sql-plan-standard.md` Section E, sixth row; `backlog-task-close` — `_topic-ownership.md` before invoking the coverage skills, and `_planning-standard.md` invariant 10; `step-complete` — `_concept-extraction-standard.md` step 3 and the same invariant 10; `study-block-close` — §3.2's `_interview-prep-standard.md` fingerprints; `sql-block-close` — the `notes/sql/coverage/{LEVEL}.md` its `Coverage section` is copied verbatim from; `simulation-block-open` — §0's read list item 6 (`_coverage-standard.md`'s digest rule, `_simulation-plan-standard.md`); `backlog-task-open` — `_shared-context.md`. **The opposite direction, one site:** `sql-block-open`'s cell claims `PROGRESS.md` and that `SKILL.md` never reads it | open | **Measure from the definer and take all nineteen at once, or the next partial sweep repeats this one.** The denominator is `system-check-prompt.md`'s `Purpose` field list, not the nine cells named above: read each `SKILL.md` whole and take every file it is *told to read*, both directions. **The one thing already settled is what is licensed out**: `REC-163` wrote §9's `Primary reads` fence, which discharges `_session-rules.md` alone — background and provenance citations of it are `source-only by ownership split`, and its two owed cases are stated there; `_shared-context.md` gets no such licence, being a mandated read of one skill rather than background to all. **Why this is a row and not a `_run-tracker.md` cell** (the `REC-046` test): no run clears it. `/system-check` is explicit-only and token-intensive, and `map-sync`'s read trigger fires one file at a time, so ten cells are cleared only by a session that reads ten `SKILL.md` files whole — which is the work, not a by-product of it |
| REC-170 | the `REC-169` resolution, 2026-08-27, from its own carve-out; diagnosis corrected and scheduling fixed by Victor the same day | **The file the note standard calls its calibration reference was never validated by Victor, so the bar every notes stage measures against is already false today — not false the day `notes-audit` reaches note 11.** `_note-quality-standard.md` names the first section of `notes/java/junior/es/11-excepciones.md` three times (l.352, l.444, l.549) as "the single best reference for what a finished note looks like", and stages A, B, T and C each open it as their depth-and-texture calibration. This row first read the defect as one that would *become* true when the route rewrote the file. It is already true: `git log --follow` on that pair shows its last passes are all machinery commits (`docs: audit Java exceptions notes…`, `docs: first-pass audit of Java notes 05–14`), and a validated file, by Victor's definition, is one **he** read and drove to his taste through TODOs — which this file has never had. The standard is therefore already calibrating the pipeline against the pipeline's own prose, and freezing that passage inside the standard, the shape this row first proposed, would only fossilise unvalidated prose. The dependency also reaches further than first listed: besides the three sites in `_note-quality-standard.md` and the four notes prompts, the file is cited by `_session-rules.md` l.662 and by `study-content-writer/SKILL.md` in **both** adapters — ten sites, not seven. Resolution shape, decided by Victor: the exemplar is pinned to genuinely refined pairs, and the row waits until **four** exist — enough for a cold reader to separate *how Victor writes* from *what one topic happened to need*, and enough to rewrite the standard's depth rules from real evidence instead of one sample. Only `00-intro-java.md` qualifies today, and an introduction carries voice, not depth. | accepted | Blocked on lived authoring, not on a decision: it runs once **four** pairs are refined — the count Victor set on 2026-08-27 — at least two of them full-depth rather than an introduction, so the set shows an invariant style and not one file’s shape. Fewer produces a bar built from too few samples, where a coincidence between two files reads as a rule; at `notes-audit NOTE = 11` is the worst moment, since the run that needs the bar would be the run rewriting it. When it runs: the three sites in `_note-quality-standard.md` and the reading lists in the four notes prompts move together with `_session-rules.md` l.662 and both `study-content-writer/SKILL.md` mirrors; `_notes-translate-prompt.md` and `_notes-review-es-prompt.md` cite the file without a sibling-read ban and need only the path changed, while `notes-audit.md`'s admissibility section and the write/review blockquotes carry the reasoning and must be rewritten, not repointed — and the `REC-169` depth-and-texture carve-out retires with them, since a `refined` exemplar needs no exemption |
| REC-171 | Victor, 2026-08-27, from the question of how to make `notes-audit` need fewer of his TODOs; every part of the shape below decided by him in that session — a sink over git, category rows over per-TODO rows, and a standing threshold rather than a one-off harvest | **Victor's TODO corrections are the only measurement of his prose bar that exists, and the system destroys them at the moment it resolves them.** The two rule families the note standard rests on were each derived once, by hand, from a single pass — the mechanism family (`_note-quality-standard.md` l.385, from `11-excepciones`) and the seven sentence-level register rules (l.447, from twenty TODOs in `00-intro-java`) — and neither's evidence survives: `git log -S TODO --follow` over `notes/java/junior/es/00-intro-java.md` returns **one** added TODO line in the entire history of that file, and the pipeline wrote it (`b9492c6f`, a pending marker), not Victor. The draft→refined diff does survive, because the pipeline commits its own draft, but it carries the answer without the question — and the question is what produced those seven rules ("MAL EXPRESADO", "¿QUÉ ME IMPIDE HACER?"). The store test is settled case law: `_recommendation-resolution-doctrine.md`, *evidence whose value is recurrence needs a store that can be counted, not merely one it can be recovered from*, applied to any proposed sink. The harvest's question is recurrence across pairs; `git log -p` answers it only by archaeology over N diffs with no normalised field for which rule was at fault. Design, eight parts. **(a) The sink** — a new file named `notes/prompts/knowledge/notes/_internal/_note-todo-harvest.md` (written in two pieces until 2026-08-27, when `REC-172` gave the validator an exemption class for a path a ledger row proposes; it is now one path and is reported, not failed), one row per **pair and category**, never per TODO, since one file yields twenty and that per-row cost inside a daily block is what `REC-054` watches. Fields: `NTH-NNNN`, date, the pair and the language the TODO was written in, a category slug, the count within that pair, **one or two of Victor's own words verbatim**, a **`missing` / `unapplied`** verdict, the standard's rule cited whenever `unapplied`, and a `Disposition` of `open` or `harvested YYYY-MM-DD`. **(b) The verbatim quote is the payload, not decoration** — the seven register rules exist because his literal wording survived; a paraphrase would not have produced one of them. **(c) `missing` and `unapplied` are opposite repairs and the column exists to keep them apart** — the standard lacked the rule, versus it carried the rule and the writer did not apply it. Without the split, a repeated breach is harvested as a new rule and the standard ends up stating the same thing twice; the doctrine already rules that past a count the verdict flips from the executor's fault to the rule being mis-worded or mis-placed. **(d) In-chat corrections are admissible rows, quoted** — they leave no marker in the file and today are lost whole, and `notes-plan-prompt.md` already obliges the run acting on one to quote it. **(e) The raw archive stays in git**: the TODO pass is committed before it is resolved, so the prose that provoked each complaint — which the sink cannot hold — is recoverable as `git diff <todo-pass-commit>..<refined-commit>`. Countable index in the file, full context in git. **(f) Writers** — `study-content-writer` at the moment it resolves the TODOs, while the reason is still known, and `notes-audit`, which already reports the markers it may not touch. **The counter** is the close-out that already writes `Status: refined`: it prints `cosecha: ninguna` or `cosecha: {categoría} madura` on every run, clean ones included, which is the `desvíos:` visible-line discipline and exists for the same reason. **(g) Threshold** — the first harvest at **four** refined pairs, `REC-170`'s own count over the same evidence set; standing after that, a category is due when it recurs in **two different pairs** since the last harvest. Cross-pair is the whole qualifier: five occurrences inside one pair are that note's quirk, two pairs is an invariant — `REC-170`'s own argument for four, applied to the steady state. **(h) A harvest writes at most one rule and is licensed to cut one** the harvested pairs never needed. The standard is 588 lines and otherwise only grows, which is how this ledger reached 324k characters. The standard is hand-written only, so a due category opens its own `REC-NNN` and is resolved under the four steps with the mandatory cold reviewer — never by the skill, never by a run | accepted | **Split the row when it is worked: (a)–(f) are not blocked and ship first; only (g)'s first harvest waits.** **The split was executed 2026-08-27 and (a)–(f) shipped**: the sink, its header contract, both `study-content-writer` mirrors (writer + `cosecha:` counter), `notes-audit` as secondary writer with its staging clause, both TODO-family sections of the standard, the `_session-rules.md` trigger, the validator exemption, and both maps. **What is left of this row is (g) alone** — the first harvest, still behind `REC-170`'s four refined pairs — plus (h), which only a harvest can exercise. Two rounds of cold review, both `approve-with-tightening`; round 1 found the sweep short by two sites and both were real. **The one that generalises: an insertion into a file this ledger cites by line number falsifies every citation below it, in other rows too** — 27 lines added to `_session-rules.md` and 16 to `_note-quality-standard.md` silently moved seven citations, four of them `REC-170`'s, plus this row's own `572 lines` count. Round 2 caught the count after round 1 caught the line numbers, which is the same defect twice in one resolution. The other: `notes-audit` was made to owe a verbatim quote its only channel was told to summarise — `_notes-write-prompt.md` said *list* the markers, so the mandate had no source until that paragraph was opened. `/system-check`'s missing inventory exclusion was found outside this row's sites and recorded in `REC-174`, widened there to three names. The sink and its writers are what *collect* the evidence, so building them at harvest time collects nothing — every pair refined before they exist is a pair whose TODOs are gone. The harvest half is blocked on the same lived authoring as `REC-170` and **sequenced behind it**: rewriting depth rules while the calibration exemplar is still unvalidated pipeline prose calibrates the bar against the pipeline's own writing, which is `REC-170`'s entire finding, so that row ships first or the two ship together over the same four pairs. Sites measured 2026-08-27, and the sweep is wider than the sink itself: the new file plus its header contract, stating the `NTH` schema, the immutable fields and the one mutable `Disposition`, on the model of the three existing sinks; **both mirrors** of `study-content-writer/SKILL.md` (`.claude/skills/` and `.agents/skills/`, identical, in the same commit, `diff`ed before committing); `notes-audit.md` where it reports markers it may not resolve (l.252, l.320); `_note-quality-standard.md`'s two TODO-family sections (l.385, l.447), which gain the sink as their named evidence source so the next harvest edits from data rather than memory; `_session-rules.md`, where the other three sinks declare their triggers, since this one fires inside daily sessions; and both maps — `_system-map.md` §7 (the new file, its writers, its reader), §9 (`study-content-writer`'s `writes` cell and its new close-out line) and a §11 symptom row if it earns one, plus `README.md`'s hub cells wherever they name that skill. **One validator item that is easy to miss**: `validate-prompt-system.ps1`'s platform-exclusive-syntax invariant excludes the friction sinks by name, because a transcribed row is evidence and not an instruction; this sink holds verbatim quotes and inherits that reason exactly, so it joins that exclusion set in the same commit or the first quote naming a tool fails the run. The threshold is what keeps this out of the refill loop the preamble forbids — evidence is promoted by the count, never by arrival. The cost that could sink it is the classification, and it is paid inside Victor's daily block: if it ever costs more than it gives, that is a `RITF` row and `REC-054` (c) rules on it, never an argument to have while building it |
| REC-188 | Victor, 2026-08-31, on reading `REC-187`'s closing report: the row had found a fourth unfounded scan-time assertion in `cover-letter-prompt` and deliberately left it standing, and he ruled that the cover letter gets its own evidence rather than staying the one document in the family nobody measured | **`REC-187` built the presentation evidence layer and the cover letter fell outside it, so `cover-letter-prompt.md` (125 lines) is now the least-founded document the apply family produces.** Measured on disk 2026-08-31, its unfounded assertions: `~250 words` and *"a recruiter reads in 20 seconds"* (l.89); `one page, four short paragraphs` with a fixed order — Opening / Why them / Why you / Close (l.78-87); `5–6 lines maximum` for `MODE = message` (l.98); *no links dump, no CV pasted in the body*; the two-version draft rule; and the forward-looking framing. Every one is a presentation rule and every one rests on its author's authority, exactly as the project count did. **The new file cannot silently absorb it**: `_application-evidence.md`'s `## Assertions` table already carries the 20 seconds as `Out of scope, deliberately` — every source it holds measures **CVs**, and a partial extract never proves an absence, so the honest state today is *unfounded and uncontradicted*, not *wrong*. **The question the evidence must answer first is prior to any formatting rule**: whether a cover letter is still read at all by Spanish consultancies in 2026, because if it is not, the prompt's value — not its word count — is what changes. `REC-187` also showed the two markets disagree (3-5 projects in Spanish sources, 2-3 in anglophone ones), so anglophone-only sourcing would repeat a known failure | open | **Search, capture, then derive — and keep the two layers apart.** Step 1 owes three measurements. (a) **Where it lives**: a `## Cover letters` section inside `_application-evidence.md` or a third file. The section is the cheaper default — the file is already declared as source 8 and read by all five prompts, and a third file multiplies the places a rule has to be looked up — but `cover-letter-prompt` declares the project-selection heuristic out of its own scope, so whether it should inherit the whole of source 8 is a real question and not a formality. (b) **Whether the letter still earns its prompt**, from sources about Spanish consultancy hiring in 2026, which is the finding that could shrink this row rather than grow it. (c) **Which of the six assertions above a first fill actually reaches**, since the population is the whole prompt and not the one line `REC-187` happened to walk past. **The row carries one design constraint Victor stated outright**: the file must hold *both* the criteria found in the search *and* the derived analysis of what a good letter must satisfy — which the counterpart file's `Quote, do not invent` discipline forbids mixing. So the two layers are written separately and labelled: quoted evidence with URL and `Captured:`, and beneath it a **derived criteria** block where every criterion either names the quote it follows from or is marked as reasoning with no source behind it. A derived criterion that names no quote is still legitimate — it is simply not evidence, and the file must never let a later reader mistake one for the other, which is the single failure `REC-187` was opened to end. **Cheapest first**: the search is the whole cost here; the writing is an afternoon once the quotes exist |
| REC-207 | `coverage-prompt` HTML junior first run, 2026-09-04, whose Step 5 mirror rebuild shipped two malformed mirror sections that every mandated validation passed | **`coverage-prompt.md` Step 5 tells the mirror rebuild only that "topic `##` headings become `###`; bullets and order remain identical; introduction appears once" — and "appears once" does not say *whose* introduction, so it is silent on the two shape rules the mirrors actually follow.** Rule one: a topic file that has `##` sections has its intro prose **dropped** from the mirror (the mirror's own header intro is the one that appears), while a *flat* topic file — every `senior.md` — keeps its intro, because there is no section heading to start from. Rule two: `notes/coverage/senior.md` separates every topic section with a `---` line and the other two levels do not. This run's first rebuild broke both: it carried four topics' intros into the junior mirror and omitted the senior separator. **The sharp half is that Step 5's own validations cannot see either defect** — validation 1 compares bullet text and order, validation 2 compares headings, and intro prose and a `---` are neither; `validate-prompt-system.ps1` invariant 2 compares only the top-level `- ` bullet multiset and passes too. The intro half was caught by accident, by diffing an *untouched* topic's section and finding it disagreed the same way; the separator half was caught by the first-run boundary reviewer as a self-declared "nit outside my remit". Neither was caught by anything the prompt mandates. A rebuild is the one step every coverage run performs, so a silent rule here is a defect any topic can ship. | open | — |
| REC-212 | `REC-189`'s step 1, 2026-09-04, cut from that row on Victor's ruling that the clause has no beneficiary on disk | **With `PORTFOLIO_SCOPE` shipped, a `full` run re-derives every tier including one a partial already banked — so three partials plus the `full` at G7 are added cost rather than a split, and `REC-189`'s own complaint is answered only for *writing the bank early*, never for the gate run itself.** What is owed is a **skip rule**: what a `full` run re-derives over a tier carrying a current `**Last banked — «tier»:**` line. The content merge needs nothing — `_portfolio-standard.md`'s *"append only questions not already there"* plus the `[refined]` freeze already make the gate re-runnable — but a `full` still re-reads the whole tier to dedupe, and unrefined questions a partial wrote stay open to that pass rewriting them. **Cut for two reasons, both measured 2026-09-04.** The beneficiary set is **empty**: 07-timetrack is the only project with tiers and it is live, so its backend keeps taking commits while Step 7a is built and `full` re-derives it anyway, and 01–06 have no tiers at all. And the draft test was wrong on its own terms: it hung freshness on the last commit touching the tier's **code**, while the bank also depends on the **contract** that shapes it — `REC-180` changed the bank's format with **zero** code commits, and a `full` under that test would have skipped the tier regardless | open | **The instrument already exists**: `REC-189` shipped the per-tier stamp for its own reason, so what is missing is only the definition of *current*, and it has **two** inputs rather than one — the tier's code and the contract that shapes the bank. Resolve when a **frozen** full-stack project exists to exercise it; until then this is a rule no run can falsify, which is the condition that produced the empty beneficiary set in the first place |
| REC-213 | `REC-211`'s cold reviewer, 2026-09-05, on the fix that gave `_batch-mode.md` its three dispositions | **`sql-exercises`'s `FILE` is a second field tied to the target that fits none of the three dispositions, and that prompt's own `TOPIC = all` line states nothing about it.** Measured 2026-09-05: `FILE` is one of its five declared keys, closed by *"That is the entire configuration. Do not add keys."*; `{FILE}` resolves from the level route's **§1** table row whose step matches `{TOPIC}` — not the §2 step `COUNT` and `FOCUS` come from — and a typed value **wins** over that resolution (*"the `FILE` key if Victor set it"*), so a batch run carrying one would write every topic's exercises into a single file. Line 244 walks every topic *"resolving each one's COUNT and FOCUS from its own route §2 step"* and is silent on the third. `_batch-mode.md`'s first disposition now names the gap in the shared file; the prompt's own `all` line is what is unfixed | open | **Not worked inside `REC-211`, whose approved tightening named it in the shared clause and stopped there.** Step 1 owes one measurement before anything is designed: whether a typed `FILE` is reachable in `TOPIC = all` at all — `practice` is the only batchable mode, and `FILE` is validated as *required* in `reinforce` alone, so the question is whether the practice branch reads a typed `FILE` or ignores it. If it ignores it, the fix is one clause on that line stating `FILE` is derived per target like the other two; if it reads it, the batch has a real collision and the fix is a validation error |

| REC-222 | the `REC-219` cold reviewer, 2026-09-08, on the residue its own fix leaves open | **Under `REC-219`'s D11, the writer that invalidates a legend sentence is still never told it did.** `/coverage-audit junior` turning HTML's `—` into a real `0/81 (0%)` is the exact event that produced `REC-219`, and D11 catches its consequence only on the next `/progress-update` — which is gate time, where a prose row now holds G6 open, so the cheap repair is discovered at the most expensive moment. The event is knowable at the moment it happens and by nobody else: only the run that writes the cell knows the cell used to be `—` | open | **The candidate is one report clause, not a new mandate**: a writer that turns a `—` or a `*` into a real figure says so in its own close-out report, so the legend is repaired in the session that broke it. Deliberately **not** a licence to edit the prose — that is the fork `REC-219` rejected, and this row must not reopen it. Step 1 measures which writers can actually make that transition (`/coverage`, `/coverage-audit`, `coverage-mark`, `coverage-bullet-add`, `authoring-progress-recount`, `study-block-close`) and whether each already has a report section to carry the line |
| REC-223 | the `REC-221` cold reviewer, 2026-09-08, as an incidental it was told to report and not fix | **The roadmap gates resolve note-authoring state from the level matrix's `Knowledge consolidation` prose, while the per-level authored figures now live in `## Authoring progress`.** `roadmap-review-prompt.md` l.262-265 and l.292-294 and `_roadmap-standard.md` l.145-149 all declare the gate reads "three `PROGRESS.md` sections and nothing else" — `## Projects`, `Practice completed`, and the matrix's `Knowledge consolidation` column — a list written before `## Authoring progress` existed. `progress-update-prompt.md` D7 l.326-328 says the numbers live in `Authoring progress` and the matrix cell should **quote** them, so the gate resolves a count against a transcription of it rather than against the section that owns it. Measured 2026-09-08 | open | **This is a scope question, not a bug**: quoting is not wrong, and widening a deliberately closed read-list is the expensive fork. Step 1 decides whether the matrix cell's quote can go stale relative to `Authoring progress` without any writer noticing — if `authoring-progress-recount` and `/progress-update` D7 keep them in lockstep, this row is discharged as designed-as-is |
| REC-224 | `REC-216`'s resolution, 2026-09-08, from a sweep it ran while measuring the repository's real testing facts | **The junior interview answers state a testing practice that contradicts both the repository and the roadmap it now declares.** `notes/interview-prep/junior/en/angular.md` l.720 answers *"Yes — I test services with Jasmine and TestBed"* and prescribes `HttpClientTestingModule`; l.732 defines a spy as *"A Jasmine function"*. Its `es/` twin carries both verbatim. Measured 2026-09-08: projects 01–06 run **Vitest 4** with zero `jasmine.*` references, `HttpClientTestingModule` is deprecated in Angular 20 (the 04 repair already used `provideHttpClient()` + `provideHttpClientTesting()`), and `REC-216` has just ruled the 07+ tool to be **Vitest + TestBed**. So the answer Victor would recite in an interview describes tooling he has never used and a module the framework has deprecated — and, worse, it claims a practice (*"I test services"*) that no project of his yet demonstrates | open | **Two defects in one answer, and only one is the tool name.** The factual half is cheap: `Jasmine` → `Vitest`, `HttpClientTestingModule` → `provideHttpClient()` + `provideHttpClientTesting()`, and the spy answer reframed on `vi.fn`/`vi.spyOn` with `spyOn` named as the Jasmine equivalent he must still recognise — the *write Vitest, read Jasmine* rule `_session-rules.md` now carries. The harder half is the **claim of practice**: until Step 9 of project 07 lands, an answer in the first person overstates what he can defend, so the fix owes a decision on whether the bank may speak in the future tense before the evidence exists. Both files, same commit, and the `[refined]` markers reconsidered — the pair carries **zero** `[refined]` markers and zero IDs, so nothing in it was frozen and the freeze clause of this row is void. Owner: `interview-prep-audit`. **Step 1 correction, 2026-09-08, measured against disk — three clauses of the finding above are wrong.** (a) The site set is **four files**, not this pair: also `notes/angular/junior/en/15-testing.md`, a whole note written on Jasmine + Karma, and `notes/general/junior/en/03-testing/10-testing-concepts.md` l.3 / l.107 / l.108, a stale copy of the ruled Testing rules table. (b) The claim-of-practice half is **inverted**. Every service spec in projects 01–06 is a 16–19 line CLI stub asserting `toBeTruthy()`, so there is no service test to claim — but the hand-written tests that do exist are **component** tests (`05-task-manager` confirm-dialog/task-dialog, `04-meal-finder` meal-card/category-filter, four in `06-hr-portal`) using `vi.fn()`, `ComponentFixture` and `componentRef.setInput`. The junior tip at l.749 stated the exact reverse and also named project 07 for components where the table says 08; that tip was the larger defect and this row never saw it. (c) The machinery half is **one clause, not a decision**: the anchor bar ("the anchor must be true") and the defensibility bar already forbade the false claim, and neither that standard nor `interview-prep-audit` names a testing tool anywhere. What was absent is what *replaces* a false claim, ruled by Victor 2026-09-08 — where the honest answer is "not yet", the answer teaches the concept with an example. **Handoffs named, per the cold reviewer's B2:** `15-testing.md` is owed to `/notes-audit` under plan entry 15 (`Status: pending`, `_run-tracker.md` `Notes J` = `0/18`) and is never hand-edited here; the general note is **`REC-228`**, because its plan entry's bullets are tool-agnostic and no coverage-driven run would force the table. |
| REC-225 | `REC-216`'s resolution, 2026-09-08, from Victor's question about where Playwright is heading | **Angular coverage has no end-to-end testing at any level, and the tool it would name if written today is not the one the postings name.** `notes/angular/coverage/junior.md` carries unit-testing bullets (Vitest/Jasmine recognition, spies, TestBed) and **no E2E bullet at all**; `notes/coverage/junior.md` mirrors that absence. Measured 2026-09: Playwright ~87M weekly npm downloads against Cypress's ~7.6M (>11:1), 45.1% adoption against 14.4%, 94% retention and the fastest adoption curve npm has recorded, while Cypress has plateaued. **Those figures are web measurements taken during the `REC-216` session, not repository evidence** — `_job-market-evidence.md` records `Jest` but names Cypress **zero** times, so the resolving run owes it an `evidence-intake` pass before the bullet may cite a Spanish posting at all | open | **The absence is correct today; the row exists so that it is filled correctly when it stops being.** `_job-market-evidence.md` names E2E twice — once inside `## Techo` (senior, off-limits to coverage by that file's own rule) and once in the synthesis stating it is asked **from mid onward** — so E2E does **not** enter junior coverage and does **not** enter project 07, and this row must not be resolved before the interview gate. When it is: the tool is **Playwright**, and the bullet states the same asymmetry `_session-rules.md` now carries for unit tests, as one shared principle rather than two coincidences — *write with the default that is winning, read the one the consultancy's repo already has*. Natural home: project 08 or post-gate. Owner: `/coverage angular` |
| REC-226 | the `REC-216` cold reviewer, 2026-09-08, as an `INCIDENTAL` it was told to report and not fix | **The employer-facing documents claim Jasmine as a skill Victor has never used, and an ATS keyword list is the worst place for a false one.** `cv-prompt.md` l.192 puts `Jasmine · TestBed` in the skills table and l.259 lists `Jasmine` among the ATS keywords; `_application-standard.md` l.116 carries the same keyword row; `linkedin-prompt.md` l.161 names `JUnit, Jasmine` in the skills line. Measured 2026-09-08: zero `jasmine.*` references in any project, all six on **Vitest 4**, and `REC-216` has ruled the 07+ tool to be Vitest + TestBed. Unlike `REC-224`, which is study material, this surface is read by recruiters and screening software | open | **Do not simply swap the word — the two halves have opposite failure modes.** In the **skills table** and the LinkedIn line, `Jasmine` is a claim of practice and is false: it becomes `Vitest · TestBed` once Step 9 of project 07 lands, and until then the row states what the projects actually demonstrate. In the **ATS keyword list** the calculus inverts: postings are still written with `Jasmine`, so dropping the token can cost a match on a filter that does no semantic reading. The row must rule whether a keyword may name a tool the candidate can *read* but has not *written* — the `_application-standard.md` honesty bar is the authority, and whatever it decides applies identically to `Cypress` when `REC-225` resolves. `TestBed` is unaffected: it is the invariant and he will have used it. Owner: `cv` / `linkedin`, gated on Step 9 |
| REC-227 | Victor, 2026-09-08, during `REC-224`'s step 1, on seeing that the whole of Angular testing is planned as one note | **The notes planner has no size bound and no subfolder convention, and its output already disagrees with itself across topics.** `notes-plan-prompt.md` → `Planning algorithm` step 3 groups concepts "only when they form one coherent learning unit" and states outright that there is **no target or maximum number of files**; its `## Paths` block defines `EN_DIR = notes/{topic}/{LEVEL}/en/` flat, and step 6 allocates flat two-digit prefixes. Measured 2026-09-08: entry `15 — Testing Angular behaviour` carries **12** coverage concepts, the heaviest of that plan's 18 entries, and is one `en`/`es` pair; meanwhile the General junior run of 2026-08-02 produced `notes/general/junior/en/03-testing/10-testing-concepts.md` — a subfolder the algorithm never mentions, recorded in `_run-tracker.md` as "12 English-only notes classified into intuitive thematic folders". So one topic is foldered and another is flat, from the same prompt, with no rule either way | open | **Two questions, and only the second is Victor's request.** (a) Does coherence alone bound an entry? A unit that is genuinely one learning outcome can still be a note nobody finishes, and `REC-191` is the precedent that cuts the other way: deleting a number deletes a gate, so a bound added here must be a test a cold reviewer can fail a **named** entry with, not prose that always passes. (b) Victor wants Angular testing split with at least one file dedicated to **reading** a Jasmine/Karma suite — the `REC-216` *write Vitest, read Jasmine* ruling made that a competence rather than a footnote, and entry 15's `Must answer` already carries both halves ("How do Vitest and Jasmine express equivalent spy intent?", "when is the legacy testing module only recognition knowledge?") inside one note. He accepts a `testing/` subfolder if the split yields several files and prefers flat if it does not, so the subfolder rule is owed either way and cannot stay an undocumented behaviour of one run. Note the ordering trap: entry 15 is `Status: pending` and `_run-tracker.md` reads `0/18` for Angular `Notes J`, so this is decided **before** `/notes-audit` writes the pair, not after. Owner: `notes-plan-prompt` |
| REC-228 | the `REC-224` cold reviewer, 2026-09-08, as its finding B3 — it falsified the disposition that row had given this file | **A General note teaches an Angular-specific tool table, it is wrong since `REC-216`, and nothing in the system is positioned to correct it.** `notes/general/junior/en/03-testing/10-testing-concepts.md` carries three false sites: l.3 links Jasmine as a `Docs:` reference, and l.107–108 state `Angular services \| Jasmine + TestBed` and `Angular components \| Jasmine + TestBed`, contradicting `_session-rules.md` → "Testing rules" row for row. `REC-224` deferred the file to `/notes-audit` under the `REC-046` rule; its cold reviewer measured plan entry 10 and falsified that. Entry 10's six coverage concepts (AAA/GWT, meaningful assertion, setup/teardown, isolation, determinism, case selection) are **entirely tool-agnostic**, so **no bullet owns a per-layer tool table** and a coverage-driven audit run is not forced to touch it. `_run-tracker.md` `Notes J` for General reads `0/21`: that route has never run once. The file has no `es/` twin | open | **The question is not which tool the table names — it is whether the table belongs in this topic at all.** `_topic-ownership.md` gives General "framework-neutral … testing vocabulary" and delegates "concrete framework implementations to Angular/Spring/Spring Boot"; a row reading `Angular services \| Vitest + TestBed` is a concrete framework implementation, so swapping the tool name would leave a boundary violation behind and re-import the same drift the next time the ruling moves. Two dispositions to choose between, and both are cheap: **delete** the rows and let the Angular and Java topics own their own tools, or **keep** them as a deliberate cross-stack orientation and give entry 10 a coverage bullet that owns them, which is what would put the table inside an audit's denominator and make this a tracker cell for real. `REC-046` is the precedent that has to be answered either way: this row exists only because the file's own writer provably would not produce the right output. The `Docs:` Jasmine link at l.3 follows whichever ruling wins. Owner: `/coverage` for General junior, then `/notes-audit` entry 10 |

## Closed

Resolved rows leave this file entirely: they become one line each in
`_recommendation-ledger-closed.md`, beside this one, ordered by ID and written to the closure schema in
step 4 above. That file is the deduplication source — a candidate matching a **rejected** line is
discharged with the reason kept there — and the reasoning behind any line is in `git log -p` on either
file, since one commit removes the row here and adds the line there.

## Suggested order for the open items

**The `REC-193` → `REC-191` pair is closed** — `REC-193` on `c72fb848`, `REC-191` on `7da3bad6`, both
2026-09-01, with `REC-194` (`4ed9c584`) as the residue `REC-193` opened and closed the same day. Why the
pair existed and what each half ruled is in `_recommendation-ledger-closed.md` and `git log -p`; nothing
in it orders an open row any more.

**Victor ruled the head of this queue again on 2026-08-31, later the same day, and this ruling
displaces the interview-prep one below: the CV bullet work closes first, in two sessions — `REC-181`'s
SPEC, then `REC-182`'s bilingual pair and lifecycle — and `REC-184` moves behind them. `REC-181` landed
the same day (`cdc75942`), so the head is now `REC-182`**. **Corrected 2026-09-04, when he re-scoped that
head — and both halves are now settled:** `REC-182` was the overwrite freeze alone and **closed the same
day** (`496c978e`, three review rounds, `approve-with-tightening`); its bilingual pair became `REC-208`,
**which was rejected on 2026-09-05, the day it was opened**, when the target fact its own prerequisite
demanded turned out to say the opposite — he applies in Spanish only. The order-of-operations warning this
paragraph told the opening session to read first was **moot** — the gate it warned about was cut, and
Victor stated the machinery is not yet in use, so there was no polished bullet for a freeze to strand.
**So this ruling is spent in full, and the head of this queue is unruled**: nothing ranks `REC-184`,
`REC-164` or the rest against each other. `REC-186`, the residue `REC-181` refused on Victor's ruling, closed the same day (`ebc4fb78`)
on its ranking half; its count half was `REC-187`, which took its place behind that head and closed the
same day too. Like the
ruling it displaces it is a call on value, and its input is his calendar, which no sweep of this file
produces: his declared target is a first developer job by **August–September 2026**, and
`notes/cv/cv-bullets.md` holds **one** bullet, written to the bar he rejected. **`REC-187` closed
2026-08-31 as well**, so nothing stands behind that head any more; what its session left for the queue
is not a row but a `_run-tracker.md` matter — the presentation evidence now exists and says a one-page
CV carries 2-3 projects, and there are still not that many polished bullets for it to choose between. **The order inside the
pair is forced rather than preferred**, and it is the one thing here that is not a preference:
`REC-182`'s `[refined]` marker freezes content, so refining bullets against the bar `REC-181` replaces
means breaking a freeze in order to raise it — build the bar, then the lifecycle that makes its output
permanent. **What this ruling deliberately does not carry into the ledger** is the operational half that
makes the SPEC worth anything — running `portfolio-audit` over the projects the CV will feature, so
bullets exist to hold to the new bar — which is a `_run-tracker.md` matter and fails this file's scope
test on purpose.

**Victor ruled the head of this queue on 2026-08-31, and it is a ruling on value, not a measurement:
the interview-prep TODO loop closes first, in two sessions — `REC-180`'s remaining `en`/`es` slice, then
`REC-184`. The first of the two landed the same day (`9ee8b52c`), so the head is now `REC-184`, and
`REC-185` — closed 2026-09-07 — and `REC-164` are still behind it.** That ruling displaced both, and it did not repair the gap
the next paragraph admits: the workable set is still unranked, and this entry ranks two rows out of it
by what they are worth to him, which is the one input no sweep of this file produces. **What the first
session did**, recorded because the second inherits it: `REC-180`'s stable IDs, `[refined]` freeze and
TODO channel went together — a TODO cannot name a question the bank has no ID for, and a channel with
nothing frozen behind it reopens nothing — and it carried the clause the row had drafted, that a TODO
about *voice and phrasing* is a first-class reopen. It also **unblocked `REC-184`'s Phase 0**, which was
waiting on question IDs existing; the row's own sequencing note in `_interview-voice-spec.md` had that
block as its only one. `REC-184` is what makes the loop *learn* rather than only repair, and its later
phases still wait on `REC-171` (g) — which waits on `REC-170` — so the split by phase that file
recommends is what keeps the corpus starting now. **What this priority does not carry**: `REC-180`'s three `[studied]` rulings (the route, `study-block-close`'s marker, and
whether `PROGRESS.md`'s `## Study progress` rows count project questions). The `es`-review owner its
bilingual half had added to that open set was resolved on its own, 2026-09-05 (`b781ae5a`), which is the
shape this paragraph is arguing for: each is a different analysis, and pulling them in because they
share a row is the batching failure this section names three paragraphs down.

**`REC-171`'s collection half shipped 2026-08-27, and the workable set has not been re-measured since; the three rows this paragraph last flagged as unranked — `REC-173`, `REC-174`, `REC-175` — all closed instead, `REC-174` on 2026-08-29, so what it says about them is history and what it says about the gap still stands.** What that half established still stands and is why it went first: the sink and its writers had to exist *before* the refined pairs accumulate, because a pair refined before the sink exists is a pair whose TODOs are gone. What remains of it is (g), which shares `REC-170`'s four-pair gate and is sequenced behind that row. **Of the rows this paragraph was written about, `REC-164` is still workable and still unstarted — `REC-172` closed 2026-08-27 — and the three that arrived after it were never ranked here at all before they were worked, which this paragraph records rather than repairs** — a suggested order asserted without measuring is worse than an admitted gap, which is the `REC-046` lesson applied to this section itself. Two of the three were residues of rows resolved that week (`REC-174` from `REC-172`, `REC-175` from two `notes-plan` runs) and one was note rot found by a pipeline (`REC-173`); none was obviously ahead of `REC-164`, and all three were taken before it anyway — a residue is picked up while its parent is still legible, which is a real reason and still not a measurement. Ranking what is open is a measurement pass, not a reading of this file. `REC-168` closed 2026-08-26 and it went first under *a correction
that stops a wrong run comes before a build* — until it settled, every breach a pipeline recorded was
deleted by that pipeline's next run, `REC-164`'s own sweep included. Its step 1 half-confirmed the
prediction written here and falsified the other half: `git log -p` on the reports **is** the sink, and
being recoverable turned out not to make the evidence countable, which is the rule it promoted.
**`REC-164` is `REC-163`'s residue.** That row, `REC-162` and `REC-163` were
all raised by a predecessor's cold reviewer, all verified against quoted cells and **re-scoped rather
than worked**, and all the same *shape* as the row that found them — a per-prompt or per-skill fact a
map cell compresses and licenses nowhere. `REC-162` and `REC-163` both closed 2026-08-19; the second
handed this one its licence outright, so what is left is the per-cell measurement of §9's `Primary
reads` across all nineteen `SKILL.md` files, in both directions. Four rules ordered the waves that drained this table
and order it now: **a correction that stops a wrong run comes before a build**, **an item
blocked on evidence is run, not edited**, **a chain is walked from its denominator up**, and above the
other three — never once wrong across eleven items — **budget the sweep, never the edit**: every row so
far lived in more places than it named, and in three of four cases it was the *cold reviewer*, not the
sweep, that found the last site.

Within an intake, exposure of secrets, unrecoverable partial writes, contradictory persistent formats,
circular truth and duplicate application rows precede wording and scope corrections. That head class is
empty too, and which rows drained, split or replaced themselves is in `git log -p`.

**The ordering that looks right and is not:** batching rows because they share a file. Rows share an
*analysis*, or a *kind of gap*, and neither is a priority — batching by file is how a row that fails the
bar ships on the back of one that does not. If the analysis splits once it starts, split the session
too: `REC-067` alone cost a locator sweep, a 20-defect injection suite and a cold review, and shipped
without `REC-084` rather than dragging it through a session it had outgrown.

**`REC-054` is not in this order and never will be** — it is `accruing`, not queued.

New self-reports append or update a row in `## Open`. A historical report remains immutable evidence;
its wording does not determine current status. The ledger does.

A resolved item leaves this file entirely — one line in `_recommendation-ledger-closed.md`, after any
rule it established moves to `_recommendation-resolution-doctrine.md` (step 4). **Nothing is lost by
that:** the reasoning is in `git log -p` on the two files the closing commit touches, because a
resolution written for the day it shipped stops being read long before it stops being true. What a
future reader needs from a closed item is the decision, not the argument — and if the argument matters
again, the row was not the right home for it.
