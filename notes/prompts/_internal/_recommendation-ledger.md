# Prompt-system recommendation ledger

Self-report recommendations use one of four states:

- `open` — observed and not yet adjudicated.
- `accepted` — agreed, with implementation still pending.
- `applied` — implemented and verified.
- `rejected` — intentionally not implemented; the reason is recorded.

**Moving an item to `applied` includes the two-map test** — `_session-rules.md` → "The two maps follow
every change to the machinery". An item that changed what a file contains, who writes it, when something
runs, or which prompts and skills exist carries its `README.md` / `_system-map.md` edit **in the same
commit as the fix**; one that changed none of those is reported as `maps unaffected`. The `Resolution`
cell says which of the two it was, so a later reader can tell a checked map from a forgotten one.

| ID | Source | Recommendation | State | Resolution |
|---|---|---|---|---|
| REC-001 | notes audit self-report | Add a live-repo/dependency fact-check gate to the notes author | applied | `_notes-write-prompt.md` contains the fact-check gate |
| REC-002 | coverage self-report | Permit a controlled diff input when coverage sections are restructured | applied | `coverage-prompt.md` documents the inherited-but-moved shortcut |
| REC-003 | progress self-report | Make Step F follow the active-branch rule | applied | `progress-update-prompt.md` now states that `main` only receives merges |
| REC-004 | progress self-report | Clarify Java-section cleanup when the section already exists | applied | D2 now requires moving misplaced pure-Java entries before additions |
| REC-005 | SQL exercises self-report | Keep blocking clarification questions brief | applied | SQL shell now requires options, consequence, and recommendation only |
| REC-006 | portability audit | Separate canonical workflows from Claude/Codex runtime syntax | applied | Shared session/runtime standards and dual launcher catalogs added |
| REC-007 | coverage prompt audit | Remove platform-specific tools, shell syntax, and literal tier names from the canonical coverage workflow | applied | `coverage-prompt.md` now translates runtime capabilities through `_agent-runtime-standard.md` and uses portable validation |
| REC-008 | coverage/content audit | Bound coverage to the junior hiring floor and stop unbounded adversarial expansion | applied | `coverage-prompt.md` now has topic budgets, an evidence-backed stopping rule, final-artifact reviewers, and explicit ownership boundaries |
| REC-009 | coverage/content audit | Recalibrate the 12 generated topic files and correct factual/coverage-future defects | applied | All topic coverage files were restored to the compact baseline, fact-corrected, ownership-cleaned, budget-checked, and mirrored into `notes/coverage.md` |
| REC-010 | coverage prompt run (Java junior, 2026-07-26) | Add a verify-gap fast path so a run that only consumes already-verified gaps skips the full market-floor re-derivation (Step 1) and the second Step 4 reviewer, while still judging each gap | applied | `coverage-prompt.md` "Run scope" section + Step 0/1/2/4 pointers; cold-reviewer verdict `approve-with-tightening`, tightening applied |
| REC-011 | notes-plan run (Java junior, 2026-07-26); reproduced (Spring Boot junior, 2026-07-27) | Planning algorithm rule 6 says allocate new files from the next unused two-digit number, but says nothing about a chapter inserted mid-route: the new file then sorts last in the folder while being read third, which breaks the standard's "00 → N reads as one journey". State whether within-level renumbering of an existing bilingual pair is permitted, or require the plan to declare the study-order/filename divergence | applied | `notes-plan-prompt.md` rule 6 now permits same-level renumbering of a bilingual pair (both languages together, prose byte-for-byte, links updated, reported as `renumber NN -> MM`, `Action: audit` preserved, never populating the split section), plus companion edits to the classification vocabulary, Update mode's declared outputs and the Final report fields. Applied in 9b4d4b1; cold-reviewer verdict `approve-with-tightening`, tightening applied verbatim. Second run's cost before the fix: reviewer-endorsed splits of two 24-concept Spring Boot entries were rejected, one on a 2384-line file |
| REC-013 | plan-audit run (07-timetrack, 2026-07-28), surfaced by Victor after the run | §14 specified UI behaviour and layout but no visual system (typography, spacing, theming mechanism, colour tokens, elevation/density, motion, accessibility, dark-mode ruling), and three requirements stated in its prose — the inspiration table's one-concrete-element, traceability and real-products rules — were absent from its pass line, which is the only thing specialists audit | applied | `_planning-standard.md` §14 rewritten: names the attractive-not-merely-correct target, adds the design-system table, motion and accessibility blocks and the visual QA checklist, and its pass line now enforces all of them plus the pre-existing inspiration rules; implementation-order item 9 sets the theme up before the first page; new invariant 11 (§14 checklist ↔ §15 done condition) so the checklist must actually run; `_plan-review-prompt.md`'s `data-model-api` §14 cell widened to the whole section and given invariant 11. Applied in 279c1c9; cold-reviewer verdict `approve-with-tightening`, tightening applied, then a second pass when the one-in-one-out budget did not land as predicted (557 → 536 against a 534 baseline) |
| REC-016 | plan-audit run (07-timetrack, 2026-07-28, third pass) — raised by Victor after REC-015 | Bullet 1 asks whether the work split turned out right, but names no evidence — and the orchestrator is instructed never to open the artefact, so its only input is the traces the specialists wrote about their own work. This run's bullet 1 declared the six-way split sound while five real defects sat in the committed plan. The close-out check does not compensate: its three lists probe file existence, commit contents and dispatch counts, never content quality | applied | `_pipeline-self-report.md` bullet 1 now requires naming the evidence: traces alone support "the machinery ran", never "the output is sound"; a step that reads the finished artefact whole and is not written by the slice owners outranks them; a pipeline without one must say so and claim no more. Applied in fe4274c; cold-reviewer verdict `approve-with-tightening` — it verified independently that no existing check inspects content, ruled bullet 1 the right home over the close-out check (whose value is being mechanical, and a judgement item would corrode that), inverted the draft to lead with the general rule since six of the seven orchestrators have no such verifier, cut a triple example to one, and required saying "not written by the slice owners" rather than implying `whole-plan` is independent of subagents in general. 242 lines, under budget, so one-in-one-out did not apply |
| REC-015 | plan-audit run (07-timetrack, 2026-07-28, third pass) — surfaced by Victor, who asked for the committed plan to be read end to end | The six specialists own only 14 of the 24 sections. §1, §2, §4, §5, §9, §11, §17, §18, §19 and §21 have **no content owner** (`branches-coverage` checks presence only), no row reads `PROJECT-BACKLOG.md`, and a rule stated in one section can be broken by prose in a section whose owner has no reason to read the first. A single end-to-end read of the just-committed plan found five such defects the all-green six-way review had passed | applied | `plan-audit.md` + `_plan-review-prompt.md` gain a seventh specialist, `whole-plan`, dispatched automatically after the six and before the orchestrator's commit: twelve fixed checks (ten orphan sections · cross-section contradictions · backlog-vs-plan), reads the file end to end, fixes directly, emits no ripples, always a twelve-row trace so a skim is mechanically visible. Applied in 013f1bb; cold-reviewer verdict `approve-with-tightening` — it verified the ten-orphan mapping independently, caught that the draft left six "six specialists" counts and two `{SCOPE}` enumerations stale (a `whole-plan` value that would have been invalid), required an explicit exemption from the tiered-read rule, rejected the draft's per-defect trace in favour of the fixed twelve rows (a per-defect trace lets an empty report pass trivially), and rejected one of the six defects as a discipline lapse rather than evidence. Both files under budget, so one-in-one-out did not apply |
| REC-014 | plan-audit run (07-timetrack, 2026-07-28, third pass) | No mandated check maps §10 endpoints to a §13 consumer, so a plan can declare an endpoint with no route, component, dialog, build step or frontend test and pass `data-model-api` — the specialist that owns both sections — green. It surfaced only as a discretionary observation by `rules-security` | applied | `_planning-standard.md` invariant 12 (every §10 endpoint is called from §13 or explicitly ruled backend-only with a reason); `_plan-review-prompt.md` `data-model-api` row gains it in both the *Owns* and *Reads from the standard* cells, and the line-130 ownership split lists it — that same line's pre-existing error (`branches-coverage` shown as owning invariants 6 and 8 instead of 6, 8, 9 and 10) repaired in the same edit. Applied in 984ff41; cold-reviewer verdict `approve-with-tightening` — it cut the drafted "and drawn in §14" clause as a near-duplicate of invariant 3 and wrong for dialogs. One-in-one-out: the §16 "Reminder, not a check" interview-prep parenthetical removed |
| REC-012 | plan-audit run (07-timetrack, 2026-07-21); reproduced (07-timetrack, 2026-07-28, twice) | Give the specialist order an adjudication rule for cross-concern ripples, and state whether a reconciliation re-dispatch must be a cold subagent or may resume the original specialist's context | rejected | Bar condition 3: the ripple protocol reached the correct result both runs (2026-07-28: `rules-security` → `data-model-api`, one re-dispatch, cap held). Friction only — the run would have been faster, not different. The cold/warm question is recorded here so it is not re-proposed as new |
| REC-017 | notes-plan run (Angular junior, 2026-07-29); standing authorization from Victor after the run | Define the planner outcome when the cold route review requires same-level renumbering but legacy English notes have no Spanish counterparts, so the bilingual-only renumbering rule cannot execute and the physical `00 → N` route remains broken | applied | `notes-plan-prompt.md` now permits same-level English-only legacy renumbering under Victor's standing authorization, keeps cross-level relocation bilingual-only, forbids fake Spanish copies, and assigns the new prefix to the planned missing Spanish path for later creation by `notes-audit` |
| REC-019 | notes-plan run (Angular Material junior, 2026-08-02) | Rule 6 permits same-level renumbering but is silent when the required renumber target prefix is occupied by a file the same plan lists under `## Unassigned existing notes`; the collision blocked a cold-reviewer-accepted split of an 11-bullet entry | applied | `notes-plan-prompt.md` rule 6 now states that an unassigned note does not reserve its prefix — it is renumbered above the route's last entry, stays unassigned with no plan entry and no Spanish path, and reports as `renumber NN -> MM (unassigned)`. Applied in 7a90dfc; cold-reviewer verdict `approve-with-tightening`, tightening applied verbatim. Cost this run: the entry-01 split was deferred, not applied |
| REC-018 | coverage run (CSS junior, 2026-08-01) | Reserve first-run behaviour for a genuinely new admitted topic with empty coverage; legacy registered topics must recalibrate without requiring retroactive admission, and middle/senior must never initialize a topic | applied | `coverage-prompt.md` now separates an uncalibrated selected level from `FIRST_RUN`; only an admitted, empty junior topic activates boundary migration, while existing bullets force normal classification with marker preservation and an empty admitted topic cannot start at middle or senior |
| REC-020 | sql-plan run (junior, 2026-08-03) | The standard's A2 never gave the revision points' spans and triggers a home in the route, so the doctrine kept the authoritative R1-R5 table; a step-ordering correction the cold route reviewer raised as BLOCKING could not be applied without editing a level-neutral file this prompt may not touch | applied | `_sql-plan-standard.md` A2 §1 now requires the revision table to carry each point's span and trigger and names the route their single source; A1 §8b now keeps only the cadence and the failure-driven focus mechanism and forbids restating the points. Applied in 49c1e62; cold-reviewer verdict `approve-with-tightening` — it corrected the report's own misdiagnosis (the migration spec was not at fault; it is junior-only, one-time and already spent), established that A2 was the right home over a new numbered section (three files cross-reference the route by section number), and required the A1 half because A2 alone would leave the doctrine free to keep its copy. Cost this run: two accepted BLOCKING findings went unapplied |
| REC-021 | sql-plan run (junior, 2026-08-03) — raised by Victor after the run | Victor judged `ROADMAP.md` to add nothing to route planning and asked for it to be dropped as an input | applied | Scoped rather than dropped: B2 makes it load-bearing, and this run's strongest reviewer finding came from its Stage-2 switch gate. `sql-plan-prompt.md` Paths and `_sql-plan-standard.md`'s input list now read two named sections only. Applied in 49c1e62; cold-reviewer verdict `approve-with-tightening` — it verified section by section what each consumer needs, caught that the draft's single-section scope starved the standard's "the pace fits the daily block it defines" and added `## Daily schedule (fixed from June 2)`, and rejected the draft's justification as factually wrong about what it excluded |
| REC-022 | sql-plan run (junior, 2026-08-03) — raised by Victor after the run | Add an observable `Learning outcome` field per step to Section C, the instrument `notes-plan` has and this standard does not | rejected | Bar condition 3: the mandated cold route reviewer caught label-shaped `Why here` lines and mis-sized targets without it, and `Done` (observable by construction, B6), `Q&A seed` and `Concepts` already carry the information. It would have invalidated all 14 steps of the just-committed route against `sql-plan-audit` for no output difference. Recorded so it is not re-proposed; the reviewer named tightening Section C's `Why here` line as where the pedagogical bite actually belongs |

| REC-023 | sql-plan-audit run (junior, 2026-08-04) | The four specialists' `Edits` columns do not partition the doctrine: §1, §5–§7, §8c, §9, §10 and §11 have no content owner, while `loop-and-fence` is assigned Section A1 (which requires §1's inputs) and invariants 6–9/12 (which live in §10) and the explicit §8c preservation check. It detected real defects there and was fenced out of fixing them, forcing the orchestrator to widen a fence mid-run — something the prompt nowhere authorizes, its only self-fix licence being a verified wrong path, count or cross-reference | applied | `sql-plan-audit.md` row 4's `Edits` cell now reads "the doctrine — any section · route header (metadata only)", aligning the table with the fence prose two lines below that already said "#4 owns the doctrine". Applied in 5d252c6; cold-reviewer verdict `approve-with-tightening` — it **rejected** the drafted second clause as a 40-word restatement of prose already present, substituting a one-word tightening ("the **whole** doctrine — the other rows' sections are exhaustive, its are not"), and independently caught that the finding's own list of unowned sections omitted §8c. 259 lines, under budget, so one-in-one-out did not apply |
| REC-024 | sql-plan-audit run (junior, 2026-08-04) | Section B5 of `_sql-plan-standard.md` still mandates a spoken exit question as part of a step's done condition, but doctrine §3 removed that format on 2026-08-03 with a reasoned amendment (retrieval practice belongs to the `simulator`/interview-prep track). The route complies with the doctrine, so B5 can only be satisfied by adding a done-condition format §3 forbids — and it is not machine-checkable, so it cannot satisfy B6 either. Every future audit will re-raise this | applied | Landed in c89c415, which the ledger was never updated to record; confirmed on Victor's call, 2026-08-04. B5 now reads "A step ends when its exercises are scored, and this track writes no interview questions" and forbids **both** removed formats by name — the `Aloud:` condition (2026-08-03) and the `**Q&A seed:**` line that had replaced it — so an audit re-adding either has failed the check rather than passed it. The fix went past the finding: `Q&A seed` was also struck from Section C's field list, so the step shape no longer asks the planner to author a question at all, and `sql-plan-prompt.md` rule 9 and its validation list enforce it on both the write and the reconcile path. Retrieval practice is owed to `notes/interview-prep/{LEVEL}/*/sql.md` via `/interview-prep-audit`, which exists and reads the same coverage file. Two stale downstream facts in `notes/prompts/README.md` were repaired with this row: the `sql-plan-prompt` output list still advertised "Q&A seeds", and the standard row still said "fourteen consistency invariants" against Section D's fifteen |

| REC-025 | sql-plan-audit run (junior, 2026-08-04) — raised by Victor while reviewing REC-023 | Phase 2's "They all edit the same file, so they **never overlap**" is false and predates the run: three `Edits` cells share `route §2` and two share `route §1`, so specialists overlap by design and it is the strict order that makes it safe. A specialist reading it would reasonably infer its sections are exclusive, which is the opposite of the contract | applied | Line 110 now reads "They edit the same two files, so they own **one concern each, never a section each**". Applied in 7348bad; cold-reviewer verdict `approve-with-tightening` — it cut the drafted 33-word replacement to 15 (the dropped clause restated the Hard rule at line 242, 132 lines later) and caught a second falsehood in the same sentence that the draft missed: "the same **file**" is singular where two files are open. **Precedent recorded:** the finding was first rejected under bar condition 3 and reversed on Victor's push; the reviewer ruled the reversal sound in category — condition 3 kills *additions* that earn nothing, while a false statement in a dispatch document is a defect regardless, per the same logic that lets the orchestrator fix a verified factual error unilaterally — but wrong about the length it licensed |

| REC-026 | prompt-system review requested by Victor, 2026-08-04 (`coverage` · `coverage-verify` · `notes-plan` · `notes-audit`) | `_coverage-standard.md` defines two markers, but every preservation rule in `coverage-prompt` names only the project one: the Step 2 pre/post capture, the Step 3 redraft rule, Reviewer B/C and validations 1 and 9. The first `sql-step-close` writes `✅ sql:{file-slug}` into `notes/sql/coverage/junior.md`; the next SQL recalibration could drop every one of them and validation 9 would still pass, since its multiset is defined over "evidence markers" | applied | `coverage-prompt.md` gains a `MARKERS` definition in Resolve paths covering both kinds, and the six rules above now reference it, including order preservation (the drill marker must stay left of the project marker's free-text clause). `coverage-verify`, `notes-plan` and `notes-audit` had the same singular wording in their digest paragraphs and now name both kinds while deferring to the canonical command |
| REC-027 | same review | `_run-tracker.md` declares `coverage-bullet-add` the only writer of the `⚠ stale` flag, but `coverage-prompt` creates far more of that debt and only *reports* the fingerprint mismatch (validation 12). Live cost: the SQL row read `Coverage J = 2026-08-03 — 12 additions, 2 splits` beside `Plan J = ⚠ stale 2026-07-31 (+3 bullets)`, so the flag understated the drift by an order of magnitude and pointed a reader to the wrong conclusion. Separately, `Plan status: stale` had no writer at all — all 13 plans on disk read `current`, making the `notes-audit` and `interview-prep-audit` guards vacuous | applied | Step 6.1 now sets `Plan status: stale` on every mismatched plan inside the coverage commit (metadata-only, entries untouched, reset by `notes-plan`), Step 6.6 writes the accumulating tracker flag and any sibling Coverage cell, validation 12 and the final summary require both. `notes-plan` Update mode writes `Plan status: current` and its close-out rewrites the cell whole; `notes-audit` guard 6 states the gate is live. `_run-tracker.md` documents the second writer. The SQL cell was corrected to `⚠ stale 2026-08-03 (+17 bullets)` from the two cells' own numbers |
| REC-028 | same review | `notes-audit` guard 10 required every dependency entry to be `complete`. `refined` is the stronger guarantee and no prompt may ever move an entry off it, so freezing the first chapter of a route permanently blocked every chapter that depends on it, with a stop message pointing at `notes-plan` — which by contract cannot fix it | applied | Guard 11 (renumbered) accepts `complete` **or** `refined`, states why the gate exists (the prose a later chapter builds on must exist), and treats unconsumed `Pending additions` on a dependency as reportable, not blocking |
| REC-029 | same review | `coverage-prompt` Step 6.1 writes `Verdict: superseded` and a `Superseded by Coverage SHA-256` line into a file whose "exact shape" is declared by `coverage-verify` and admits neither. A later verification run overwrites the file against a schema that never described the state it found | applied | `coverage-verify-prompt.md`'s format block declares the field, and a paragraph states that this prompt only ever writes `complete`/`gaps` while `coverage-prompt` owns the third value |
| REC-030 | same review | `_pipeline-self-report.md` mandates a run-start read of the orchestrator's own `_last-run-report*.md` `Status:` line in every step 0. `coverage-prompt` and `coverage-verify` do it; `notes-plan` and `notes-audit` mention their report only when writing it at the end — and REC-011, REC-017 and REC-019 all came out of `notes-plan` runs | applied | Both prompts gain the check as an early guard, with the "surface, never apply in this run" rule stated |
| REC-031 | same review | A middle or senior `coverage-verify` can target a gap at an earlier level, but `coverage-prompt` reads only `verify-{LEVEL}.md`, so a junior-targeted gap sitting in `verify-middle.md` was invisible to every junior run | applied | Required source 9 now takes `LEVEL`-targeted gaps from sibling findings files on full recalibration, judged under the same Step 2 rules and never mutating the sibling file; `coverage-verify`'s handoff section states both consumption paths |
| REC-032 | same review | `coverage-verify` required source 4 pointed at `_internal/_job-market-evidence.md`, which relative to its own folder resolves to `knowledge/coverage/_internal/` where that file does not exist; `_session-rules.md` and `_shared-context.md` were bare filenames in both coverage prompts | applied | The three shared-root inputs are repository-relative in both files; the two genuine `knowledge/coverage/_internal/` inputs stay folder-relative |
| REC-033 | same review | The progression gate is required by `coverage-prompt` Step 0.8, `notes-plan` guard 6 and `notes-audit` guard 11, but no file said what closes it or what to read to decide | applied | All three now cite "Progression gate" in `_coverage-standard.md` as the definition and name the observable evidence (the earlier level's `Notes` and `Interview` tracker cells against their plan denominators) |
| REC-034 | same review | Three smaller mismatches: `notes-audit` guard 8 requires sibling-level coverage files its read list omitted; its close-out records a `dry-run` outcome although the prompt has no dry-run mode, and its guard 13/14 no-op mapped to no outcome; and `notes-plan` renumbering leaves the `## Notes file executions` rows, keyed by `TOPIC + LEVEL + NOTE`, pointing at files that no longer exist | applied | Sibling coverage added to the read list with the reason; outcomes are now `completed` / `completed — no-op` / `blocked` with dry-run explicitly excluded; `notes-plan`'s close-out corrects the affected tracker rows |
| REC-035 | same review, extended on Victor's instruction to signal every prompt owed a re-run | The flag repair in REC-027 was applied to the SQL row only, and no plan header on disk said `stale`. Measuring all 13 junior plans against their coverage (stored `Coverage SHA-256` vs the canonical digest) found 11 stale, of which 4 — Angular, General, Java, Spring Boot — map every current bullet and are stale on the fingerprint alone: `notes-audit` refuses them while a `(+N bullets)` flag would have read as "nothing happened". `coverage-audit`'s junior convergence was also overtaken by the SQL recalibration two days later | applied | The `⚠ stale` section of `_run-tracker.md` now covers any overtaken cell and declares a `(fingerprint only)` form beside `(+N bullets)`; nine `Plan J` cells and the `coverage-audit` row carry measured flags, and the eleven stale plan headers were set to `Plan status: stale`. `coverage-prompt` Step 6.6 owns the `coverage-audit` flag going forward. CSS and Git keep empty `Plan J` cells — their plans predate the current prompt version, so they were never executions to flag; the empty cell already reads as pending |
| REC-036 | `plan-audit` prompt review requested by Victor, 2026-08-04 (a read of the prompt family, not a run) | `MODE = new` is the half no run ever exercised — every recorded run in `_last-run-report.md` is `review` on 07, and REC-013→016 hardened only what those runs touched. Four defects, all on the author side: (1) the gap analysis reads PROGRESS.md sections deleted on 2026-08-03, so `_plan-write-prompt.md` Step 1.3/Step 2 and the standard's §3 pass line and §4 "Originally learned in" all resolve against an inventory that no longer exists — every bullet reads as a gap and the choice-by-gap-count criterion is corrupted; (2) the author gets no truncation guard, read-to-EOF confirmation or acceptance check while reading the largest file in the pipeline (`notes/coverage/junior.md`, 2094 lines against the 2000-line silent cut); (3) `new` has no destructive-write guard — the number comes from "last completed + 1" and nothing checks whether `{PROJECT}/PLANNING.md` already exists, while Finishing's `git status` check verifies which files are staged, not `A` vs `M`; (4) the specialist acceptance check ("one row per check its slice owns") is unverifiable by an orchestrator that never reads the review prompt or the standard — only `whole-plan`'s fixed twelve rows can actually be checked | accepted (half applied) | **036-A applied 2026-08-05**, after a re-verification that corrected one diagnosis and widened two. Defect **(2) was partly misdiagnosed**: the author *does* carry a truncation guard (`_plan-write-prompt.md` Step 1.4, `wc -l` + offset passes) — what it lacked was the read-to-EOF *confirmation* in its report and any orchestrator-side acceptance check, the asymmetry against the reviewer, which now has both. Defect **(3) was already firing**: PROGRESS.md records 07 as `In progress`, so "last completed + 1" resolves to **07**, a folder that exists; Step 5 now takes the next number above the highest existing folder and stops if the path is occupied, and Step 1.6's "last completed project's PLANNING.md" reference was the same error (the last completed is 06, an Angular-only plan predating the 24-section standard — it now reads highest-numbered existing, ~1800 lines). Defect **(4)**: specialists now declare `checks owned: n` on line 1 and return exactly `n` trace rows, so the orchestrator checks a count instead of glancing at a non-empty table — it still never reads the reviewer prompt or the standard. **One defect not in the original finding, applied with them**: §3's Topic vocabulary was anchored to "exactly the PROGRESS.md section names" (11 values, sections deleted 2026-08-03) and justified by `progress-update`'s concept routing, which `fad2ec5` removed — re-anchored to the 13 `notes/` topic folders that own a `coverage/{level}.md` (adds Angular Material, JavaScript, Git; drops Deployment, whose concepts route to General), which is both live and checkable; `_plan-review-prompt.md`'s `steps-tests` cell follows. `_planning-standard.md` 567 → 568: one-in-one-out not applied, the added line carries two new valid values and a routing ruling, and forcing an unrelated cut would be worse. No cold reviewer: verified factual corrections and guards under the REC-025 precedent. **036-B remains open** — defect (1) and the marker rewire, held for REC-037; its agreed shape stands unchanged: **one orchestrator, no fork** — the seven-specialist procedure, ripple protocol, gates and self-report are ~60% of `plan-audit.md` and identical in both modes, and duplicating them repeats the silent drift the mirrored SKILL.md files showed jul–aug 2026. What splits is the **author**, whose two jobs differ in kind: gap analysis + choice (heavy inputs, one decision) and design + write 24 sections (long authoring). Rewire (1) onto the `✅ NN-slug — {evidence}` markers — unmarked bullet = new concept, bullet marked with an earlier project = review concept and its "Originally learned in" for free; PROGRESS.md keeps only the projects table and "Professional level by topic". See REC-037 for the component the split creates. **Also carries REC-038(e)** (folded in 2026-08-05): the same Step 1 input list still orders "Read the full Angular, Spring Boot, and SQL sections" of PROGRESS.md and describes `notes/coverage/junior.md` as "1600+ lines" when it is 2094 — both lines are rewritten by this split, never corrected on their own, since a fixed line count in a paragraph that still names deleted sections reads as maintained when it is not. **036-B scope, measured 2026-08-05:** defect (1) lives in **five** files, not the two recorded — beyond `_plan-write-prompt.md` (L50, L51-52, L80, L91, L94, L99-100) and `_planning-standard.md` (§3 pass line, §4 and its "Originally learned in" column), the same rot is in `_plan-architecture-prompt.md` L39, whose "what patterns Victor has already built and can be assumed known" enumerates precisely the deleted sections, and in `_plan-review-prompt.md` L63, which routes the `architecture` specialist to PROGRESS.md for the same judgement. **Four conditions the marker rewire must carry**, from `_coverage-standard.md`: (i) key on the **project** marker `✅ NN-slug` only — a bullet carrying just the drill marker `✅ sql:{file-slug}` was drilled in exercises, not built, and is still a *new* concept; (ii) accept both marker forms, since markers written before 2026-08-01 have no `— {evidence}` clause and are valid bare; (iii) the 07 **Angular tier is not backfilled** (PROGRESS.md L79-84), so until it is, unmarked Angular bullets the 07 frontend touches are not new concepts — a dated exception, not a standing one; (iv) the rewire **changes §3/§4's semantics**, it is not a mechanical substitution: markers record demonstration, never study ("an unmarked bullet means *not yet demonstrated*, never *not yet studied*"), so §3 becomes "first **demonstrated**" and §4's column "Originally **demonstrated** in" — an improvement, since both become falsifiable, but one that must be written into the standard rather than inferred. **Ordering:** REC-037 first or jointly — its brief *is* the split's first half, and the extraction seam it creates is legitimate under `_pipeline-self-report.md`'s form (a) (the write half never loads `notes/coverage/junior.md`), which in turn makes it a **contract requirement that the brief be sufficient input for §2, §3 and §4** — it must carry the gap bullets verbatim, or the write half reopens the 2094-line file and the split buys nothing. **036-B is now mostly spent, 2026-08-05:** REC-037's brief carries the marker rewire and all four conditions, `_plan-write-prompt.md`'s L50/L51-52/L80/L91/L94/L99-100 went with its deleted Steps 2–3, `_planning-standard.md`'s §3 pass line and §4 column carry the demonstrated semantics, and `_plan-architecture-prompt.md` L39-41 was folded in on the cold reviewer's finding — it was running a second gap analysis off the deleted sections inside a flow whose premise is that there is one. **What remains of 036-B is one line:** `_plan-review-prompt.md` L63 still routes the `architecture` specialist to `PROGRESS.md` "to judge level-appropriateness". Left deliberately — that judgement is answered by `Professional level by topic`, which is live, so the line is defensible as it stands and only its *wording* invites a reader back to the deleted sections. It becomes a one-word fix the next time that file is opened |
| REC-037 | raised by Victor, 2026-08-04, while reviewing REC-036 | **Two clauses of the finding were inexact and are corrected here** (re-verified 2026-08-05, file by file). `whole-plan` does **not** "check §2 against its pass line": §2 has no pass line, and neither do §1, §4, §5, §9, §11, §17, §18, §19 or §21 — the ten sections that row was created to own. Its instruction to check those ten "against the standard's pass line" is therefore unexecutable for all ten, which makes the choice *less* audited than the finding claimed, not more (filed as REC-041). And ROADMAP does not *explicitly* call its proposals non-firm — `_roadmap-standard.md` line 123 calls the file "a forward-looking strategy document, not a concept list and not a progress tracker", and the section is headed "candidate **ideas**"; the substance holds, the word "explicitly" did not. Everything else verified true as written: Step 3's seven criteria do run inside the author subagent, no specialist audits the choice, and "Gaps NOT closed" was a chat-only output. Original finding follows. Choosing the next project is the single decision worth more than the 24 sections it produces — a month of study — and it is made once, uncontested, inside the author's context: Step 3's seven criteria run in the same subagent that then writes the plan, and no specialist audits the choice (`whole-plan` checks §2 against its pass line, which is coherence, not "is this the best project"). ROADMAP.md is the only place candidates live, but it is a living forward-looking document whose proposals are explicitly **not firm decisions**, so nothing in the repo records *why* project 0X was chosen over the alternatives, and the author's "Gaps NOT closed" output — the natural input to the next choice — is printed to chat and lost | applied | Agreed shape, now built: a **runnable selection prompt** that reads `PROGRESS.md` (level table + coverage-demonstrated percentages + projects), the `✅ NN-slug` markers across the junior coverage mirror, `_shared-context.md`, `_job-market-evidence.md`, ROADMAP's candidates and gate list, and the published projects' domains — and writes a **durable one-page brief** (chosen project · gaps closed with their coverage bullets · the two concept lists · rejected alternatives with reasons · scope ceiling · gaps left for the next project). It carries a `Coverage SHA-256` and the last completed project, the same freshness device `notes-plan-{LEVEL}.md` uses, so `plan-audit` `MODE = new` can refuse a stale brief. Division of labour: **ROADMAP proposes (living, non-binding) · the brief decides (dated, contestable) · PLANNING builds**; `roadmap-review` at G8 resyncs ROADMAP afterwards. Runs standalone when Victor wants to think ahead, or as Phase 0 of `plan-audit`, with a cold second opinion (`endorse / endorse-with-scope-change / wrong project`) on one page **before** 24 sections are designed against it. **Applied 2026-08-05.** `notes/prompts/projects/plan/project-brief-prompt.md` (293 lines) is the 28th runnable prompt, launched by `/project-brief` in both catalogs; it writes `projects/briefs/project-brief-{NN}.md` and commits it alone. It sits **beside** `plan-audit` in the `plan/` family for the same reason `sql-plan-prompt` sits beside `sql-plan-audit`: one runnable plans, the other builds and audits. **The gap analysis is rewired onto the markers here, not in the author** — the four `_coverage-standard.md` conditions are Step 1's three rules plus the dated 07-Angular-backfill exception, which re-reads PROGRESS's own paragraph each run so it expires by itself. **The seam is real, not nominal:** `_plan-write-prompt.md` lost its Step 2 (gap analysis) and Step 3 (the choice), 265 → 242 lines, and no longer loads the 2094-line mirror — it digests it and greps three bullets as a check *on the brief*. The read-to-EOF truncation guard REC-036-A had just installed on the author **moved** with the read: it is now the brief's Guard 5 and its `Coverage read:` header field, which `plan-audit`'s new Phase 0 acceptance check verifies. Freshness is `notes-plan`'s device **plus a second field it has no equivalent of** — `Project markers counted:` — because the canonical digest strips markers by design and the brief is built from exactly what it strips: digest or project change is hard stale and refuses; marker drift is soft, named and passed to the author, so a brief written months ahead is not dead on arrival. §3/§4's semantics moved with it (`_planning-standard.md`: "first **demonstrated**", "Originally **demonstrated** in", the pass line keyed on `✅ NN-slug` with both forms valid and a drill marker alone not counting), and `steps-tests` gained the brief as an input so §3/§4 are checkable at all — previously nothing verified them. Cold-reviewer verdict `approve-with-tightening`, all eleven tightenings and three factual corrections applied: it caught that a `wrong project` brief stayed `Status: current` and would be silently picked up by the *next* `plan-audit` run (the one gate that exists to stop a month of misdirected work — now `contested`, and Phase 0 checks the verdict field); that neither consumer named the canonical digest command, so a plain `sha256sum` would have marked every brief stale on the first closed step; a Guard 3 deadlock where a fresh-but-defective brief could be neither used nor rewritten; the advisor/brief precedence over §3 being unstated; a `4e` left by the renumber; a `27` left in the README; and the `_plan-architecture-prompt.md` inputs still running a second, uncoordinated gap analysis off the deleted PROGRESS sections — folded in from REC-036-B rather than shipped as a contradiction. Collaterals closed: both launcher catalogs at 28, README's five counts (28 runnable · 16 orchestrators) plus its Projects table, feed-each-other list, run order and batch statement, `validate-prompt-system.ps1` (28 / 16, new pipeline path — passes), `_run-tracker.md` (a `project-brief` column whose row precedes the project folder), `ROADMAP.md`'s project-08 paragraph, which still said `plan-audit` picks the candidate, and `_session-rules.md`'s auto-commit list. `_planning-standard.md` 568 → 571: one-in-one-out **not** applied for the second consecutive change to that file — the three lines are the semantic contract the brief's output is checked against; the cut is owed by the next edit to it, and the reviewer named §3's Topic-vocabulary justification as the candidate |
| REC-038 | same review as REC-036 | Five smaller defects in the `plan/` family, two needing a ruling and three stale facts: (a) `_planning-standard.md`'s canonical G6 plans `progress-update · MODE = active` into **every** project's §23, but that prompt still rebuilds the per-technology concept lists PROGRESS.md dropped on 2026-08-03, so each new plan bakes in a gate that regresses the file it is meant to refresh; (b) `plan-audit.md` passes `DRY_RUN = true` to every dispatched specialist, but `_plan-review-prompt.md` ties that flag to the `SCOPE = all` standalone path only — three lines of dispatch text explaining a no-op; (c) `_planning-standard.md` §23's pass line says a gate must name "one of the **23**" prompts in `notes/prompts/README.md`, a count that file no longer states; (d) `plan-audit.md`'s batch-mode condense rule still says "its **six** specialist traces are spent" — seven since 013f1bb; (e) `_plan-write-prompt.md` describes `notes/coverage/junior.md` as "1600+ lines" when it is 2094 — the truncation guard still fires, but the number understates which files are at risk | applied | Applied 2026-08-05 in 95a9626, 4a27a5c and 7265612, after a re-verification that **corrected two of the five diagnoses**. **(d)** confirmed and extended: line 150 was the only wrong count among the thirteen `six`/`seven` statements in the `plan/` family, but `_plan-review-prompt.md`'s "six verbose reports saturate it" was wrong the same way and went with it. **(c)** was misdiagnosed — the README does state a count, as `26` in three places (lines 34, 58, 63) and again in the line-73 heading, against its own `27` two paragraphs below; the table lists 27 and both launcher catalogs hold 27 files at parity, so `27` is the truth. Fixed by dropping the number from the standard altogether (it has no business inventorying another file's contents) and correcting all four README occurrences. **(b)** confirmed and worse than recorded: the dispatch called `DRY_RUN` "plan-review's own no-commit switch", false twice over — a dispatched concern's no-commit comes from the `SCOPE ≠ all` rule at line 160 with or without the flag, and the standalone path never commits either, so the flag only labels a printed command block "ready to run" or "preview". Removed from the dispatch and declared `SCOPE = all`-only in the reviewer's config block; **deleting it outright is left open as Victor's call** — that is a design choice, not a factual correction. **(a) is false as written and moves to REC-039**: `fad2ec5` already stopped `progress-update` rebuilding the concept lists, the surviving defect is a different one, and it belongs to that prompt rather than to G6 — which fires at project close and reaches a new plan only as a checklist line, so it never blocked project 08. **(e) folded into REC-036**: the number is right (2094) but the guard fires on its own `wc -l` check, and the line sits three below an input list still ordering "Read the full Angular, Spring Boot, and SQL sections" of PROGRESS.md — sections deleted 2026-08-03 — so correcting it alone would make a dead paragraph look maintained. No cold reviewer ran: these are verified factual corrections under the REC-025 precedent, not design changes |
| REC-039 | REC-038 (a), re-diagnosed 2026-08-05 | `progress-update-prompt` rebuilds `PROGRESS.md` **whole**, while the closing rituals (`step-complete`, `backlog-task-close`, `coverage-mark`, `sql-exercises` review, `simulation-review`) now maintain it incrementally, each writing its own cell in the session that produced it — so the two writers can overwrite each other. Its own header has carried a `⚠ Pending redesign` banner since `a37b55a`, telling the reader to run it sparingly and diff before committing, and `_planning-standard.md` G6 plans it into **every** project's §23 closure checklist. Residues of the 2026-08-03 demotion also survive: Step E's Changes-made table still has rows for `Angular`, `Java`, `Spring Boot`, `CSS`, `Deployment`, `TypeScript`, `Architecture`, `Security`, `General` and `SQL` — sections that no longer exist, neutralized only by the "skip rows for sections that do not exist" line under it — plus the "Java section created for the first time" logging rule and Step F's commit-message example "add project 07 Spring Boot concepts" | applied | **Applied 2026-08-05**, after a pipeline-wide re-verification that **corrected three clauses of the finding and uncovered a defect twice its size**. **"Rebuilds `PROGRESS.md` whole" is false**: Step E already mandated *"targeted in-place edits (Edit tool, one edit per change) — do NOT rewrite the whole file"*. The defect was **ownership**, not scope, and it was not uniform. Measured section by section: `Coverage demonstrated` recounts to `491/1359` **exactly, all 13 rows** — its four deferring writers keep it perfect, so the safety net had nothing to catch; `Timed simulations` reads the same TRACKER.md. Both were harmless. The two that did damage: **`## Projects`**, where 07's cell reads `Steps 1–6 done, backend backlog fully closed, Step 7a next` while a `✅` scan of the plan yields `Steps 1–6 done, Step 7 in progress` — D5 would have flattened both the sub-step and the backlog fact; and **`Practice completed` → Exercise route**, where D3 had rotted into describing columns (`Exercises scored`) and a section (`§5`) that the live contract in `_sql-exercises-review.md` §4b replaced with `Corrected` / `Route progress` / `Steps closed` and §1 — running it would have *broken* the table. **"G6 is not touched" is also wrong, twice.** Its stated justification, "G7 and `cv-prompt` both read `PROGRESS.md`", is half false: `portfolio-audit` reads it in **none** of its four files — only `cv-prompt` does (plus `project-brief` and `review-audit`, which the row never named). And the gate's *closure condition* does change: an auditor that only reports cannot make PROGRESS accurate, so `has run` stopped implying `reflects this project`. The G6 row and §23's checklist line now close on **an empty drift report**, not on the run happening. **The defect neither REC-038(a) nor REC-039 saw: the demotion leaves half the prompt without a consumer.** Step A fanned out one cold subagent per project running the full concept extraction, but D1 has stated since 2026-08-03 that *"nothing from the extracted concepts is written into PROGRESS.md"*, and its replacement instruction — "report coverage work owed" — was **never executable**: deciding whether a concept is missing from the checklist needs the coverage files' contents, which the subagent is forbidden to read and the orchestrator only `grep -c`s. So `_concept-extraction-standard.md` was audited too, and splits cleanly: Steps 0–2 live (step status), **Step 3 live with a different owner** — `step-complete` reads it and states *"this ritual and `progress-update` must extract from the same place or the two records diverge"* — Step 4 **dead**, routing to ten sections deleted 2026-08-03 while citing `_planning-standard.md` §3 for a vocabulary REC-036-A re-anchored the same day, leaving its invalid-value flag **inverted** (it would fire on `Angular Material` / `Git` / `JavaScript` and pass `Deployment`); and its `PROGRESS_HINT` defined as `### Project NN` headings that `grep "^### Project" PROGRESS.md` proves absent, against the orchestrator's own redefinition as the `Status` cell — the two files disagreed on the definition of the one input they exchange. **What shipped:** the prompt is an **auditor** (430 → 414 lines) with an explicit ownership table naming, per section, its writer and this prompt's role; D7 is the only write and its `Practical evidence` cell is *preserve-then-append*, since `step-complete` shares it; D3 and D4 now **point at their owning contract instead of restating it**, which is what rotted D3 once and cannot rot again; D1 and D2 are tombstones and **the D-labels are deliberately not renumbered**, because `coverage-mark`, `coverage-bullet-add`, `coverage-prompt` and `coverage-audit` all cite "step D8" by name; D8 keeps its format/counting authority while losing its write, with the distinction stated ("owning a format is not owning the write"); Step E's dead Changes-made table — rows for `Angular`, `Java`, `Spring Boot`, `CSS`, `Deployment`, `TypeScript`, `Architecture`, `Security`, `General`, `SQL` — and the "Java section created for the first time" rule are gone, replaced by a two-table output whose second is `Section · What PROGRESS says · What the sources say · Owner to re-run`; a **`git diff` guard** enforces the contract mechanically (every hunk must fall inside the level matrix); Step F commits only when the matrix moved and its example message is fixed. The extraction standard gained a two-reader table and a Step 4 tombstone that names where routing lives now (`coverage-bullet-add` + `_topic-ownership.md`). Collaterals: both launcher catalogs, README's hub table, feed-each-other list, prompt table and "starting a new project" step 1, `_planning-standard.md` G6 + §23, and two hygiene repairs this audit turned up — `_last-run-report.md` had **no `Status:` line** although `_pipeline-self-report.md` requires one and this prompt's own step 0 reads it (backfilled), and `_run-tracker.md`'s `progress-update` cell was **empty** despite the 2026-07-16 run that wrote the report, the exact report-written-tracker-skipped failure that file documents (backfilled, flagged `⚠ stale`). No cold reviewer: the writes removed are removals, and the factual corrections are verified against disk under the REC-025 precedent. Prompt is under the 500-line budget, so one-in-one-out does not apply. **Spun off as REC-042**: `roadmap-review-prompt.md` L101-106 has the same rot |
| REC-040 | spotted 2026-08-05 while correcting the prompt counts of REC-038(c) | `notes/prompts/README.md` line 73 promises "each with a slash command of the **same name**", and that is false for 20 of the 27. Nineteen drop the `-prompt` suffix (`coverage-prompt` → `/coverage`, `progress-update-prompt` → `/progress-update`, …); only the seven `*-audit` prompts match literally. The twentieth is not a suffix at all: `code-review-prompt` launches as `/code-review-practice`, **deliberately** — `/code-review` is Claude Code's own built-in diff review, and the launcher file states the reason in its own rules block. So the catalog asserts a rule while the real convention is silent and its one load-bearing exception is documented only inside the launcher nobody reads before trusting line 73 | open | Not applied. The fix is one sentence, **not a rename**: state the actual convention (the command is the prompt name minus the `-prompt` suffix) and name the single exception with its reason, so the host-tool collision is recorded where the catalog is read. The divergence itself is correct — only its description is wrong, and a contributor who "repairs" the odd one out re-collides with Claude Code's own command. `validate-prompt-system.ps1` passes on all 27 because it checks launcher→prompt delegation targets, never name equality, so no automated check contradicts line 73 |

| REC-041 | REC-037's re-verification, 2026-08-05 | `_plan-review-prompt.md` gives `whole-plan` ten sections no other row owns — §1, §2, §4, §5, §9, §11, §17, §18, §19, §21 — and tells it that "for these ten only, also check content against the standard's pass line, since no other row does". **Not one of the ten has a pass line** — including §2, the section that states why this project was chosen: `grep -n '^- \*\*Pass:\*\*'` on `_planning-standard.md` returns pass lines for §0, §3, §6, §7, §8, §10, §13, §14, §15, §16, §23 and the branch/gate rule blocks, and for nothing else. So the one check written to cover the unowned sections is unexecutable on exactly the sections it was written for, and a specialist that reports twelve green rows has verified presence, not content — the fixed twelve-row trace REC-015 installed makes the skim visible only if the rows mean something | open | Not applied. Two candidate fixes, and the choice is a real one: **(a)** write pass lines for the ten in the standard — correct, but adds ~10 lines to a file already 71 over budget and hands `whole-plan` ten conformance checks on top of its coherence job; **(b)** re-word the `whole-plan` row to say what it can actually do for those ten — the section is present, non-empty, and says something the rest of the plan does not contradict — and drop the pass-line clause as the fiction it is. (b) is cheaper and honest; (a) is what would actually catch a §2 that names no gaps. Note the interaction with REC-037: §2's content now has an upstream source (the brief's gap table and decision paragraph), so a pass line for §2 would finally be checkable against something |

| REC-042 | REC-039's pipeline audit, 2026-08-05 | `roadmap-review-prompt.md` L101-106 dispatches its gap subagent with: *"find concepts in `coverage-junior.md` but not yet in `PROGRESS.md` — treat a concept as covered if `PROGRESS.md` has an entry"*. `PROGRESS.md` has held **no concept entries since 2026-08-03**, when the per-technology sections were deleted as an evidence-free duplicate of the coverage files. So every one of the **1359** junior bullets now reads as an uncovered gap, and the phase/gate edits the prompt makes downstream (L161-171: "mark a phase ✅ if its conditions are clearly met per PROGRESS.md") are built on that inversion. It is the exact twin of REC-036(1) in the plan family, one folder over — and `roadmap-review` is the prompt gate **G8** runs, so a project close walks straight into it. L183 ("markers to PROGRESS.md") and L208/L256's no-duplication checks are the same paragraph's neighbours and need reading with it | applied | **Applied 2026-08-05**, after a deep re-verification that confirmed the diagnosis and **widened the scope three ways**. The finding was right about the mechanism and about all five cited locations. **Timing correction, in its favour:** the defect has **never fired** — the only recorded run is 2026-07-21 (`df91763`), before the 2026-08-03 deletion, and G8 has not run since. Unlike REC-036(3), this was caught before it spent a run. **Scope widening (1) — the prompt holds 21 `PROGRESS.md` references, not 5.** Six more were dead and uncited: L12 and L24-27 (the `▶ Run first` header and the `Prerequisite` paragraph, **doubly rotten** — they justify the prerequisite by "the gap analysis reads PROGRESS.md directly", already false, *and* promise that `progress-update` makes PROGRESS current, which REC-039 removed the same day: it is an auditor that reports drift and repairs nothing); L78-79 ("what concepts are already covered"); **L260, Reviewer 2 invariant 2** — "compute it from coverage-junior.md vs PROGRESS.md", i.e. the same inversion *inside the cold reviewer*, so the verification tail would have inherited the doer's error instead of catching it; L289; and **L298-300**, the worst of them — "if a whole topic has no PROGRESS.md section at all, say so as one line instead of listing its concepts", written as an exception for Architecture and Security on 2026-07-21 and now firing on **all 13 topics**, collapsing the entire gap report to thirteen lines of "no section". **Scope widening (2) — `_roadmap-standard.md` was not in the finding at all**, and it is the file **both reviewers verify against**: fixing only the prompt would have left the invariants pointing at the deleted lists. L19-20 called PROGRESS the source of truth for "concepts covered so far"; L102 owned the SQL sync rule; L135 assumed PROGRESS still owns a concept list. (L57's "open junior **gates** from PROGRESS.md" survives untouched — those are the level matrix's `Next gate` column, which is live.) **Scope widening (3) — hygiene:** `_run-tracker.md`'s `roadmap-review` cell was **empty** despite the 2026-07-21 report, the identical report-written-tracker-skipped failure REC-039 found and fixed for its own cell only. Backfilled and flagged `⚠ stale`. **Q1 settled — `roadmap-review` reads the markers itself**, and the reason is recorded so it is not re-litigated. Taking the list from `project-brief` fails three ways: the brief's gap set is **filtered to what one project can close in 2–4 weeks** and to nine topics, so it is structurally blind to CSS, Git, JavaScript and General — the four topics no project will *ever* close and whose only route is the 13:30 block, which is exactly what this prompt's "gaps nothing in ROADMAP closes" group exists to surface; it would **close a dependency cycle**, since `project-brief` Step 3 reads ROADMAP's candidates and this prompt writes them; and the **lifecycles do not meet** — G8 fires at project close, *before* the next brief exists, and the prompt's other two triggers ("coverage changed", "it has been a while") have no brief at all. REC-037's objection to a second gap analysis does not reach this: that one was a second analysis *inside the same pipeline, on the same question, feeding the same artefact*. What is shared is the **rule**, not the computation — both prompts now point at `_coverage-standard.md` "Evidence markers". **Q2 settled — two of the three rulings survive, one does not.** The **phase table** survives whole: every gate on it resolves against `## Projects`, `Practice completed` and the level matrix's `Knowledge consolidation` column, none of which is a concept list — the fix was to *name those three sections* instead of saying "per PROGRESS.md", which sends a reader hunting for concepts that no longer exist. The **four other LeetCode gates** survive on the same three sections. The **SQL topic table does not**: PROGRESS keys SQL by *file*, ROADMAP's rows are *topic*-shaped, and the mismatch is already visible on disk — `ROADMAP.md` L234 reads ⏳ with `01-basics.sql` **closed 40/40**, and L235 reads ⏳ with `03-joins.sql` deleted and never regenerated, stale in both directions at once. Re-keyed to `practice/sql/junior/PLANNING-junior.md` §2, whose step headings carry the topic name *and* its scored/target counts (chosen over the `✅ sql:` drill markers, which are bullet-sized — 149 of them — and would need aggregating to answer a topic row). Reviewer 2 gains that file as a read. **A third question the finding did not raise, applied with it:** the 2026-07-21 self-report recorded as friction (#3, *recorded, not applied*) that subagent 2a's "one line per concept" is mis-sized against the coverage file. The rewire makes it blocking — the gaps stop being fuzzy prose and become **868 exactly enumerable unmarked bullets**, and one line each is an unusable report that saturates the doer. 2a's output is now contracted: one row per topic (`marked/total` + one sentence, all 13), at most 8 verbatim bullets per topic ranked by interview relevance, the SQL list, the read-to-EOF line and the Angular-exception count. **The rewire also deletes something:** 2a's "borderline" group loses its reason to exist — it existed to adjudicate fuzzy prose matching, and a bullet either carries a marker or it does not. Its one successor is the dated 07-Angular-backfill exclusion, which is now reported as a count so the under-reported Angular figure is visible rather than silent. `roadmap-review-prompt.md` 345 → 395, `_roadmap-standard.md` 125 → 139; both under budget, so one-in-one-out does not apply. No cold reviewer: the marker rules are transcribed from `_coverage-standard.md` and the rest are verified factual corrections under the REC-025 precedent. **Left deliberately unfixed:** `ROADMAP.md`'s two stale SQL rows. They are the *output* this prompt owns, and hand-patching them would spend the evidence its next run should be measured against |

| REC-043 | `review-audit` prompt review requested by Victor, 2026-08-06 (a read of the family, not a run) | Six defects, none of which the four recorded runs could surface, because five of them live in the parts a run never re-reads. **(1) The frontend has no security lens on full-stack projects.** `_review-standard.md`'s frontend-security greps (`[innerHTML]`, `bypassSecurityTrust*`, sensitive data in web storage) were gated to "Angular-only 01–06 — there is no cold security pass, so these live here", and `_review-flow-prompt.md` repeated the gate; the cold reviewer's `SCOPE` admits only backend resources and `security-infra`. So on 07+ **nobody** reads frontend code with a security mind — and the rationale inverts: 01–06 have no token to store, 07 does, and `frontend-infra` owns the auth service that stores it. **(2) The prompt contradicts itself about committing the backlog**, 3 places against 2: the header block, the "After the run" bullet and `_review-standard.md`'s closing line all said "not auto-committed, hands Victor the command", while Step 5 and the hard rules said "commit it yourself (2026-07-14)". The session rules settle it — `PROJECT-BACKLOG.md` auto-commits **in any flow** since 2026-07-29 — and the orchestrator reads the standard *first*, so the stale half arrived first. `notes/prompts/README.md` L180 carried it too, as did **both launchers** — which also still described the pre-slicing architecture ("code-quality + learning-objectives subagent, and for full-stack the cold attacker-hat security subagent") for a prompt that now fans out thirteen. **(3) Step 6 had drifted off the shared self-report contract**, which `_pipeline-self-report.md` L247-248 permits a "tailored version" of only under "same contract, same bar, same `_run-tracker.md` update": it mandated no `Status:` line, listed **six** local bullets instead of the shared five — dropping *Rule friction and rule breaches*, the bullet that exists to record rules a run broke — never mentioned the ledger reconciliation, the close-out check, the cold-reviewer refinement gate or `_run-tracker.md`, and staged **one** file where the contract requires two plus a `git show --stat HEAD` check. **(4) Step 3b is unreachable on a partial scope by numbering alone**: the config block said "`backend` runs only Steps 1–2" and Step 0 said "skip Steps 3–4", while Step 3b itself says "run it for each tier in {REVIEW_SCOPE}". **(5)** The security reviewer's trace is endpoint-shaped only — it never got the line-count + closing-quote proof the flow reviewer gained after the 2026-07-14 false "hardcoded JWT secret", although Step 5 applies "no trace, no review" to both. **(6)** The batch comment claimed the 30-day gate "applies per full-stack project", so an `all` run would re-review the six Angular projects regardless of date | applied | **Applied 2026-08-06.** Evidence for (3) was on disk: the `review-audit` column of `_run-tracker.md` was **empty for all seven projects** while every one of them had been reviewed — the report-written / tracker-skipped failure REC-039 and REC-042 each found for their own cell, here with a structural cause rather than a lapse, since the step never named the file. Backfilled all seven from each backlog's own `Last Reviewed` lines (01–04 on 2026-07-14, 05–06 on 2026-07-16, 07 backend on 2026-07-23), which is a **better source than the prompts' war-story citations** and also caught 02 and 04, whose runs are cited nowhere. **One contradiction the fix created and had to resolve:** the hard rule "review state lives in `PROJECT-BACKLOG.md` — nowhere else; a second copy of the date would drift" makes a tracker cell look like the forbidden index. Ruled and written into both files: the cell is an **execution record**, the backlog's per-tier lines are **review state**, the gate reads only the latter, and the two are allowed to differ. (1) is ungated in the standard, the flow prompt, the Step 3 dispatch and the 01–06 hard rule, with the grading of a JWT in `localStorage` pinned to the standard's own silent-plan rule — **Medium "decide and document"**, not the automatic High that "every security finding is High" would otherwise produce for the pattern every junior tutorial teaches. Step 6 now *executes* the shared contract instead of paraphrasing it and only says what this pipeline puts in the five bullets — shorter than the six it replaced. Step 3b is declared a per-tier step in three places. `review-audit.md` 452 → 474, under budget. **`_review-standard.md` 557 → 556 — one-in-one-out applied** (57 over budget): out went the two war-story paragraphs of the 30-day gate and the learning-objectives rubric, both compressed to the parenthetical clause the shared contract prescribes, the consistency-reviewer blockquote, and the **spent** per-tier `Last Reviewed` migration note — all seven backlogs already carry the two-line form, so it instructed nobody, and `review-audit.md`'s pointer to it went with it. (2), (3), (5) and (6) are verified factual corrections under the REC-025 precedent. (1) and (4) are **design changes**, so they went through the gate — first applied without it, then submitted the same day when the session gained the ability to dispatch. **`cold reviewer: approve-with-tightening / approve-with-tightening`**, and it earned its keep on three findings the drafter could not see. **It falsified a claim in the new text**: "the check has a real target there and none in 01–06" is wrong in both halves — `06-hr-portal`'s `auth.service.ts:21` persists the whole `currentUser` (role + personal fields) to `localStorage`, 03/04/05/06 all persist domain data, and 07's frontend is a stub with no target *today* — and the same paragraph contradicted itself two sentences earlier. **It caught a live contradiction the ungating created**: `review-audit.md`'s unqualified hard rule "Security findings are always High" against the standard's new Medium for a silent-plan `localStorage` token — and the orchestrator, which holds that hard rule, is the grader, so the hard rule was the operative one. Now reads "Every *confirmed* security finding is High", matching the standard's own wording. **And it caught an unreachable pointer**: the new bullet told the flow reviewer to grade "by the 'what confirmed means' rule below", which lives in the one section `_review-flow-prompt.md` explicitly orders it to **skip**, while `_review-standard.md` reserves that judgement for the orchestrator — so grading is now *reported up* (`file:line`, severity left to the orchestrator) instead of decided inside a standard-reasoning slice. It also confirmed the widest form of (1) against code rather than intuition: restricting the greps to `frontend-infra` would drop most real hits, since `05-task-manager`'s `task.service.ts` and 03/04's services keep persistence in **feature** folders that slice does not own. On (4) it rejected one hunk as redundant — after the edit the rule was asserted **four** times, and the standalone Step 0 paragraph said nothing the tightened scope bullets and the heading did not; deleted, with the "no backend tier on 01–06" edge folded into the bullet. `_review-standard.md` back to **557 — net-neutral**, the extra cost paid by the cold-pass intro, which no longer restates a boundary the frontend-security block now states precisely |
| REC-044 | `review-audit` self-report, run on `projects/07-timetrack` scope `backend`, 2026-08-06 | Step 0's unreviewed-code gate had **one** operational test — a completed ✅ step — and the fix campaign this pipeline's own backlog generates moves no step, so the test is structurally blind to precisely the code `review-audit` caused to be written. This run: backend 14 days inside the 30-day window, no ✅ step gained (Steps 1–6 were ✅ long before, Step 7a unstarted), but **76 backend commits and 49 dated `## Closed` lines** after its last review. The letter said "stop and offer FORCE" with a stop message — "reviewed 2026-07-23, < 30 days ago" — that is materially false about the tier's state; the rule's own stated purpose ("stop you re-reviewing the **same** code, not reviewing **new** code") said run. Running found two Highs. The gap is a proxy contradicting the concept stated two lines above it, not a run breaking a clear rule | applied | **Applied in `b301d4b`.** The gate now reads **two signals, either one enough**: a ✅ step completed after the tier's date, or a dated `## Closed` line for that tier. Tightened the two existing sentences in `review-audit.md` Step 0 and `_review-standard.md`'s gate sub-section rather than adding a clause — both files, because leaving the standard's closing "gained **no** completed step" would recreate the two-file drift the edit exists to prevent — and dropped "the backlog's own task notes", the vague ancestor the precise clause supersedes. `review-audit.md` 475 → 477, under budget. **`cold reviewer: approve-with-tightening`**, on a whole-file read. It **strengthened the evidence**: the *previous* run (2026-07-23) had already bypassed on closed tasks alone and graded itself "pipeline clean" without recognising the divergence — two runs reading past the test to the purpose, which is what moves this from a judgement call to a text defect. It corrected the draft's own numbers (76 commits, not 77; 49 closed lines, not ~35 — the report **understated** its case). Its required tightening: **drop "in this order"** — the signals are OR'd, and a stated precedence invites the exact inversion "the ledger only counts if no step moved". It confirmed no contradiction with the hard rule "review state lives in `PROJECT-BACKLOG.md` — nowhere else" (the new signal is read *from inside* that file, so it is more consonant than the ✅-step signal, which reaches out to PLANNING and PROGRESS) and no duplication with Step 5's ledger read (different step, and Step 5 never runs if Step 0 stops). It also recorded a **residue this edit does not close**: code that moves no step *and* closes no backlog task — ordinary in-step commits — stays invisible to both signals, and it advised **against** a third `git log` signal that would turn a bounded test into a judgement call |
| REC-045 | Victor's question, 2026-08-06: "is the moment to run `progress-update` well signposted, and will Claude tell me?" | REC-039 demoted `progress-update` to an auditor on 2026-08-05 and updated `_planning-standard.md`'s canonical **G6** row with the new rule — *the gate closes on a clean drift report, not on the run happening*. The demotion was **not propagated to the three live files that actually schedule the run**, so every place Victor reads at the moment the gate fires still describes the pre-demotion prompt: (a) `projects/07-timetrack/PLANNING.md` §23 G6 and its closure box — the standard governs *new* plans, and 07's was written before, so the only project with a live G6 kept the old wording and the box "PROGRESS.md reflects this project" is untickable by a prompt that writes one table; (b) `practice/sql/PLANNING.md` §9 **G3** — "reconciles the whole SQL section in one pass" is now false in the strongest sense, D3 measures the `Exercise route` tables and names `sql-exercises MODE = review` as their owner, so the gate as written asks the prompt to do something it refuses to do; (c) `_session-rules.md` "PROGRESS.md updates" — its one daily-session-facing line called it "periodic full audits", the only description Claude loads every session. Plus a fourth, structural: **`sql-step-close` never announces G3.** Its §4 performs the level-closed and revision-point checks but knows nothing about gates, and the only G3 announcement in the track lives in `_sql-exercises-review.md` keyed on the last *topic* (`report-queries`) while §9 keys the gate on the last *step* (13) — steps span topics, so the two can miss each other and the level ends with no gate named. `step-complete` and `backlog-task-close` both already carry a "gate due" line; the SQL ritual was the asymmetric one | applied | **Applied 2026-08-06.** (a) 07's G6 row and closure box backported from the standard, naming the four repair owners (`step-complete`, `coverage-mark`, `sql-grade`, `simulation-review`). (b) §9's G3 row and closure box rewritten to "measures … reports … does not repair", naming `sql-exercises MODE = review` §4b and `sql-step-close` as the owners a drift row hands the work to. (c) the session-rules bullet replaced by two: what the prompt is now, and **when to say it is due unprompted** — G6, SQL G3, and the six `▶ Run first` consumers (`cv`, `linkedin`, `cover-letter`, `project-brief`, `plan-audit MODE = new`, `roadmap-review`) — since a rule Claude loads every session is the only one that reaches a daily block. (d) `sql-step-close` §4 gains a third check, `Gate due`, plus a `Gate` row in its report table, with the topic-vs-step mismatch recorded inline as the reason it cannot be left to the grader. Both skill mirrors written in the same commit. **(a)–(c) are factual corrections** under the REC-025 precedent — three files describing a behaviour that changed on a dated, already-adjudicated decision — so no cold reviewer ran. **(d) is a small design addition**, and it is deliberately scoped to *announcing*: the ritual never runs the gate, exactly as `step-complete` does not run G3. **Left open:** no equivalent audit was done for the other gates' wording — this pass only followed `progress-update`, and G5/G7/G8 may carry their own pre-demotion assumptions in 07's §23 |
| REC-046 | Victor, 2026-08-06 | **The interview-prep family needs a re-verification pass, and a way to freeze a question once studied.** Two halves. **(1) Drift:** the family is `interview-prep-audit.md` + `notes-and-interview-prep-prompt.md` + three internals, and none has been re-read against the changes of Jul–Aug 2026 — the `✅ NN-slug — {evidence}` coverage markers, the deletion of PROGRESS.md's per-technology sections (2026-08-03), REC-039's demotion of `progress-update`, and the `_topic-ownership.md` altitude routing. Whether the family is still correct, and whether a prompt is *missing* (there is no interview-prep equivalent of `/notes-plan` — nothing plans which questions a level owes before the audit writes them), is unadjudicated. **(2) The studied marker exists but nothing sets it:** `[x]` on a question is already honoured as "studied — structural fixes only" by `interview-prep-audit.md` L270/L285-286 and `notes-and-interview-prep-prompt.md` L163-166. What is missing is the **writer**: no skill marks a question studied/refined during a study session, so the freeze can only ever be applied by hand, and no counter reads it. Also still owed from an earlier audit (memory, not a REC): the ⭐ priority pass on the old questions and the `java.md` blank-line fix | open | Not applied. Likely shape: one cold re-verification of the five files (same treatment REC-042 gave the roadmap family), then a small `interview-prep-mark` skill that sets `[x]` + the refined state on the questions worked in a session and reports the counter it feeds. Sequencing note: the counter is **REC-048's**, so the marker's write target should be decided there and not invented here. The "is a prompt missing" half is a design question and needs its own answer before anything is written |
| REC-047 | Victor, 2026-08-06 | **`notes-audit` should mark, in the topic's `notes-plan-{LEVEL}.md`, the coverage bullets each note actually absorbed as it writes them.** Today the plan's per-entry contract is `Status: pending/complete` plus prose (`Must answer`, `Learning outcome`, `Pending additions`), and **no entry carries an explicit list of the coverage bullets assigned to it** — verified on `notes/java/coverage/notes-plan-junior.md`: 17 entries, all `Status: pending`, zero `Coverage bullets:` fields. So the requested marking has nothing to mark against, and "which bullets are already written up" is currently answerable only by reading 17 notes | open | Not applied, and it is **two changes, in order**: (1) `/notes-plan` emits an explicit assigned-bullet list per entry (it already computes the assignment — it is what the `Coverage SHA-256` fingerprint and the remap machinery exist for; it just does not persist it in a countable form); (2) `notes-audit` ticks each assigned bullet as the note lands it, and reports any it could not absorb rather than silently dropping it. Doing (2) first produces a marker with no denominator. This is the **prerequisite of REC-048's "% of notes studied"** — until entries enumerate bullets, no honest percentage exists |
| REC-048 | Victor, 2026-08-06 | **PROGRESS.md should become the actual instrument of learning progress**: % of notes studied, % of interview-prep studied, SQL exercises done, simulations done — all split by **junior / middle / senior**, all written by the closing rituals, never by hand. Four things must be adjudicated before a line is written. **(a) It conflicts with a live rule:** `progress-update-prompt.md` D6 says "Never add or remove a section — PROGRESS.md's sections are fixed", written to stop the deleted per-technology concept lists from creeping back. The rule needs re-scoping (ban the *concept list*, not all growth), not silent violation. **(b) Authored ≠ studied**, and only one of the two exists: `notes-plan` `Status:` is an **authoring** state (has the note been written), while interview-prep `[x]` is a **studied** state. A "% of notes studied" needs a studied axis for notes that does not exist anywhere yet, plus REC-047's per-entry denominator. **(c) `Coverage demonstrated` already carries per-level percentages** from the evidence markers — the new rows either join that table or start a second one, and two tables of per-level percentages measuring different things is exactly the confusion that made the concept lists rot. **(d) Ownership:** every new cell needs a named writer in `progress-update`'s ownership table, or it becomes the first cell in the file with no owner | open | Not applied — this is the largest of the seven and the one that touches the most files. Order that makes it safe: settle (a) and (c) as a design ruling first (which table, which sections, what D6 now forbids), then REC-047 supplies the notes denominator and REC-046 the interview-prep writer, then the ritual-by-ritual wiring, then `progress-update`'s ownership table and D-steps gain the new rows. Note the SQL and simulation halves already have owners (`sql-grade`, `sql-step-close`, `simulation-review`) — it is the notes and interview-prep halves that are new machinery |
| REC-049 | Victor, 2026-08-06 | **Review the next-project decision prompt** (`projects/plan/project-brief-prompt.md`) so the choice is driven by `PROGRESS.md`, `_shared-context.md` and **the coverage points still unlearned per level**, rather than by a looser reading. It already reads PROGRESS.md and is gated on `▶ Run first: progress-update`, so the open question is narrower than "rewrite it": does its gap analysis consume the per-level `✅` evidence markers the way REC-042 just rewired `roadmap-review` to, or does it still infer gaps from prose? The two prompts answer adjacent questions from the same files and were last touched at different times | open | Not applied. Read it against `_coverage-standard.md` "Evidence markers" and REC-042's ruling before changing anything — that REC settled *why* two prompts may both compute gaps (they share the rule, not the computation, and their lifecycles do not meet), and the same argument should be re-applied here rather than re-litigated. Downstream consumer to keep in view: `plan-audit MODE = new` builds the plan from the brief, so a brief that picks wrong is not caught later |
| REC-050 | Victor, 2026-08-06 | **Review `ROADMAP.md` and its prompt** so the file matches the prompt system and consumes coverage **per level** properly. Partly pre-empted: REC-042 (applied 2026-08-05) rewired `roadmap-review-prompt.md`'s gap analysis onto the per-level evidence markers, re-keyed the SQL table to `PLANNING-junior.md` §2, fixed 21 stale `PROGRESS.md` references and updated `_roadmap-standard.md` alongside it. **But it has never run with that prompt** — the only recorded run is 2026-07-21, before the rewire — so no evidence exists about whether the file it produces is the right shape. `ROADMAP.md`'s own two stale SQL rows (L234-235) were left unfixed **on purpose**, as the evidence the next run is measured against | open | Not applied, and deliberately **blocked on a run, not on an edit**: the honest next step is to fire G8 (`/roadmap-review`) and judge the output, because editing the prompt again before it has ever executed would spend the evidence REC-042 preserved. Do not hand-patch L234-235 |
| REC-051 | Victor, 2026-08-06 | **The 30-day "Last Reviewed" window in `review-audit` is unwanted** — Victor reviews a project when he decides to, not when a timer allows it. Live in three places: `review-audit.md` L89 (applies to every project and per tier), L121-127 (stop-and-offer-FORCE when a tier was reviewed < 30 days ago), and `_review-standard.md` L50 (the per-tier gate itself) | open | Not applied. The distinction that matters when this is cut: the **date window** is the unwanted part, but the neighbouring rule at L127 — a tier that gained code no reviewer has seen since its last review — is the part with real value, and REC-044 has just been working on that same gate's blindness. So the likely fix is to demote the window from a **stop** to a one-line note ("this tier was reviewed N days ago") and keep the unreviewed-code test as the only thing that can change behaviour. Cutting both together would remove the only guard against re-reviewing byte-identical code |
| REC-052 | Victor, 2026-08-06 | **Build the simulations track as a real plan-driven flow, mirroring `practice/sql/` but adapted to simulations, Victor's level and target.** What exists today is two prompts (`simulation-generator-prompt.md`, `simulation-review-prompt.md`), a flat bank (`practice/simulations/{angular,spring-boot,sql}/`), `TRACKER.md`, and an **empty** `notes/prompts/practice/simulations/_internal/`. What the SQL track has and this one does not: a doctrine `PLANNING.md`, a per-level route built **from the coverage file** (`PLANNING-{LEVEL}.md` with steps, targets and a `Coverage SHA-256`), an audit that keeps that route true (`sql-plan-audit`), opening and closing rituals (`sql-block-open` / `sql-block-close` / `sql-step-close`), a grader that is the only door to the close (`sql-grade`), a friction record (`MISTAKES.md`), and gates wired to `PROGRESS.md`. The ask includes extra/`[Repaso]`-style batches for revisiting a technique, and PROGRESS.md updates from the closing rituals | open | Not applied — the biggest build of the seven, and the only one that is new machinery rather than a correction. Two things to settle before copying the SQL shape: (1) **a simulation is not an exercise** — it is timed, it is graded against a rubric, and its unit of progress is a whole test rather than a query, so "steps with exercise targets" may not map and the route may need to be keyed to §8c-style *unlocked techniques* instead; (2) the SQL track's own doctrine was rebuilt twice before it settled, so lift the **structure** (route from coverage, ritual per boundary, gates that close on evidence) and not the section numbering. Depends on REC-048 for where the simulation counters land in PROGRESS.md |
| REC-053 | system-map verification pass requested by Victor, 2026-08-06 (a read of the eleven skills against their real targets, after the map was written) | **`study-content-writer` is the one ritual never re-verified after the three-level notes migration, and it contradicts the notes standard in three places while writing straight into `notes/`.** **(a)** Step 3 says a new `en/` file gets its `es/` translation *"(same filename)"*; `_note-quality-standard.md` L83-84 says the opposite — *"never a copy of the English filename. The counterpart is identified by its number prefix"* — and `_session-rules.md` L371 repeats it. This is the one with a silent cost: `es/03-methods.md` looks correct and quietly breaks the prefix-only convention the whole tree follows, and no audit re-reads a filename. **(b)** Step 4 orders *"Update the `next file:` counter for the topic in CLAUDE.md"*. **That counter exists in no file** — `grep -rn "next file"` over `CLAUDE.md`, `AGENTS.md` and `_session-rules.md` returns nothing; CLAUDE.md became a thin adapter and the counter went with it. Numbering is now the notes-plan entry's authority (`_session-rules.md`: *"A plan entry is the authority for both exact paths"*), so an inline write should allocate no number at all rather than update a dead pointer. **(c)** Step 1 writes the note path as `notes/{topic}/en/` + `es/`, missing the `{level}` segment — its own frontmatter has it right, so the body predates the junior/middle/senior split. **The systemic half, which is why this is not just three text fixes:** the skill writes into the tree `/notes-audit` owns but touches **no plan entry** — it ticks no `Coverage concepts` `[x]` and sets no `Status:` — so a note written in a daily session is invisible to `notes-plan-{LEVEL}.md`, and would be invisible to any notes-studied percentage REC-048 builds. Rule on whether inline writing may target a *planned* entry at all, or is confined to refining a note that already exists | open | Not applied. (a)-(c) are text fixes, each needing the mirror write to `.agents/skills/study-content-writer/SKILL.md` in the same commit; the plan-marking half is a ruling and belongs beside REC-047/REC-048, never before them |

## Suggested order for the open items

Added 2026-08-06. The rows above are the authority on *what* each item is; this is the order that keeps
them from tripping over each other. Three rules produced it: **a correction that stops a wrong run comes
before a build**, **an item blocked on evidence is run, not edited**, and **a chain is walked from its
denominator up**, never from the visible end.

**Wave 1 — the plan family, before project 08 is planned.** `REC-041` → `REC-036`. Both live in
`plan-audit`'s specialists, both corrupt `MODE = new`, and `MODE = new` is what plans project 08 — the
only pipeline in the system with a dated appointment. 041 first: it is a text fix to an unexecutable
check, and 036's re-verification will read the same reviewer. Doing these after project 08 is planned
means planning it twice.

**Wave 2 — the free corrections.** `REC-040`, `REC-051`, and **`REC-053` (a)-(c) only**. Independent of
everything, minutes each, and 051 removes friction Victor meets the *next* time he wants a review rather
than the next time he audits the system. No reason to sequence them against anything; do them whenever a
session has a gap. 053's three text fixes come with one condition attached — each needs the mirror write
to `.agents/skills/` in the same commit — and they are worth doing before the next inline note is
written, since (a) is the only defect in the ledger whose damage lands in a filename nothing re-reads.

**Wave 3 — run what is blocked on evidence.** `REC-050`. It is not an edit: `roadmap-review` was
rewired by REC-042 and has never executed since, so the next honest step is firing it at G8 and judging
the output. Editing it first destroys the only evidence its next run could produce.

**Wave 4 — the tracking chain, in strict order.** This is `REC-048` and it cannot be started from the
part that is visible:

1. **`REC-048` ruling only, nothing written** — which table holds the new percentages (join
   `Coverage demonstrated` or start a second one), what D6 forbids now that it can no longer forbid all
   growth, and **whether a "studied" state for notes exists at all** — today nothing marks a note as
   studied, only as authored, and the 15:30 study block has no closing ritual to set it. Every later
   step needs this answer to know where it reports to. **Settle `REC-053`'s systemic half in the same
   ruling:** whether a note written inline by `study-content-writer` may target a *planned* entry, and
   if so what it must tick — an inline note that marks nothing is a hole in whatever denominator this
   wave builds, and the answer is the same shape as the one 048 owes.
2. **`REC-047` part 1** — `/notes-plan` persists the assigned bullets per entry. This is the
   denominator; without it no notes percentage is honest.
3. **`REC-047` part 2** — `notes-audit` ticks them as it writes.
4. **`REC-046`** — the interview-prep re-verification, then the marker-writing skill. It comes after the
   ruling because the skill must report to a decided target, and it can run in parallel with 2–3.
5. **`REC-048` wiring** — the rituals write their cells, and `progress-update`'s ownership table and
   D-steps gain the new rows. Last, because it is the only step that needs all the others to exist.

**Wave 5 — the consumers, re-read after the chain.** `REC-049`. `project-brief` chooses the next project
from exactly the data Wave 4 changes, so fixing it first means fixing it twice. **Exception with teeth:**
if project 08 is planned before Wave 4 lands, 049 moves up next to Wave 1 and is re-read afterwards — a
brief built on a wrong picture is not caught by any later gate.

**Wave 6 — new machinery.** `REC-052`. Last by dependency (it needs Wave 4's counters to know where a
simulation's progress lands) and by cost. It is not blocking anything: the two existing simulation
prompts already work, so this is a track being built around them, not a repair.

**Two orderings that look right and are not.** Starting the chain at `REC-047` part 2 — the marking is
the visible half, but it marks against a list that does not exist yet and reports to a target nobody has
chosen. And building `REC-052` early because it is the most exciting — a simulation track whose closing
ritual has no PROGRESS.md cell to write ends up with the same hand-maintained tracker it was meant to
replace.

New self-reports append or update a row here. A historical report remains immutable evidence; its
wording does not determine current status. The ledger does.
