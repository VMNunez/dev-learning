# Coverage prompt — last run self-report

**Date:** 2026-07-19 · **Target:** TOPIC = General, NOTES_PATH = notes/general/ · **Branch:** `fix/backend-backlog`

1. **Plan vs reality** — coverage was mature (113 lines), so the angles ran in parallel with the market analyst. Seven angles: the four standard ones plus **three** invented for the topic — "the wire" (read the raw HTTP exchange), "the environment beneath the app", and "process and delivery". Third consecutive run where the invented angles carried the output: the four standard angles converged almost entirely on Spring-Boot-owned material (Maven wrapper, `ddl-auto`, DataSource errors, Flyway, component scanning, starters), all dropped by the ownership grep. Angle 5 produced the raw-exchange, failure-response and caching sections; angle 6 produced build-outside-the-IDE, configuration precedence and the container-runtime section; angle 7 produced both delivery sections. **Worth promoting "the raw protocol / the wire" into the numbered list** — it is angle 5's own reasoning (the layer beneath the framework idioms) generalised past language topics.

2. **Report discipline** — clean. All seven angles returned exactly the three prescribed lines and wrote full reports to the scratchpad; nothing needed trimming. The scratchpad mechanism did its job — 231 proposals never entered the session raw.

3. **Failures & retries** — none. All eight subagents passed the acceptance check first time (seven "112 lines, read to EOF" proof lines; the reviewer's "99 items reviewed" matched what was sent; the market analyst confirmed the live web search ran and reported General's areas at near-zero explicit posting frequency, correctly shifting weight to interview norms per the under-3 rule).

4. **Rule friction and rule breaches** — no rule breached: step-0 guards ran (branch, Opus, checklist before Step 1), model overrides passed on every dispatch, both structural counts ran mechanically, Step 4c verified by diff, and the pre-commit `git status` caught three of Victor's modified project-code files and kept them unstaged. One serious friction, and it is a **tooling trap the prompt does not warn about**: the cross-topic ownership grep was first run as a bash `for` loop over ~32 terms and returned **empty for every single term** — a silent false negative. A sanity check (`grep -c "profile"` → 8 hits) exposed it. Trusted, it would have duplicated roughly thirty items already owned by Spring Boot, Security, Git and JavaScript into General — exactly the failure the check exists to prevent. Step 4a mandates the grep but says nothing about verifying the grep *itself* behaved. **Change worth considering: "a multi-term ownership grep returning zero hits across the board is a broken command, not an empty result — re-run one term standalone before trusting it."** The dedicated Grep tool worked where the bash loop did not.

5. **Verdict** — change worth considering: (a) the grep-false-negative guard in Step 4a above; (b) promote the "raw protocol / the wire" angle into the numbered angle list, on the same footing as angles 5 and 6.
