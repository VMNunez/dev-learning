# Coverage prompt — last run self-report

**Date:** 2026-07-13 · **Target:** TOPIC = Spring Boot, NOTES_PATH = notes/spring-boot/ (second update of the same mature file, now with the fan-out Step 4a)

1. **Plan vs reality** — The fan-out that the previous run recommended, and that commit `6a8582c` wrote into the prompt, **worked as designed on its first real outing.** Four uncapped angles (live code review · design and decisions · take-home/live coding · debugging what broke) ran in parallel and converged: the same holes surfaced independently from three or four directions (`localhost` inside a container, Lombok's compile-time annotation processing, the JDK/`UnsupportedClassVersionError` mismatch, the CORS preflight, Mockito strict stubs). That overlap is the stop rule firing, not wasted tokens. The market analyst (Sonnet) again returned **zero new items** — everything it derived was already covered. Third run in a row: on a mature topic it is now a confirmation step, and the entire yield comes from the adversarial fan-out.
2. **Report discipline** — Clean. All five subagents returned on-format, section-tagged lists with their own "OUT — post-junior" split. Nothing had to be trimmed or reformatted; no code dumps, no narrative padding. The design angle even self-policed a past decision, explicitly declining to re-propose the `@Async`/`@Scheduled`/file-upload material that commit `b37c6b8` had deliberately trimmed.
3. **Failures & retries** — None. All five ran synchronously and completed first try.
4. **Rule friction** — One, and it is mechanical rather than a defect in the reasoning rules: Step 4b's whole-section rebuild, done on Windows with PowerShell 5.1 `Get-Content`, **silently corrupted every em dash** (ANSI read of a UTF-8 file → `â€”`) across the mirrored section. It is invisible unless you re-read the bytes afterwards. Recovered with `git checkout` plus explicit `-Encoding UTF8` reads and a BOM-less `WriteAllLines`. The purpose filter itself gave no friction: three angles proposed HikariCP pool exhaustion, which an earlier run had deliberately demoted, and the "discarded ≠ vanished" rule made keeping it in `future-learning.md` an easy call.
5. **Verdict** — Pipeline clean; the Step 4a fan-out is validated and should stay. One change worth considering: add a line to Step 4b warning that on Windows the `notes/coverage.md` rebuild must use explicit UTF-8 reads and writes, or the sync quietly mangles every non-ASCII character in the mirrored section.
