# Coverage prompt — last run self-report

**Date:** 2026-07-13 · **Target:** TOPIC = Spring Boot, NOTES_PATH = notes/spring-boot/ (update of an existing, mature coverage.md)

1. **Plan vs reality** — The prompt's plan was **wrong in one structural way, and it matters.** Step 4a dispatches ONE adversarial interviewer and caps it at "the 12 questions you would actually ask". That single pass returned 13 gaps and read as convergent. On Victor's instruction I then ran three more adversaries with *different angles* (live code review · design decisions · take-home + production debugging) and no question cap: they returned ~90 further gaps, 83 of which survived as genuine junior-scope items, and forced 10 new sections. One interviewer with 12 questions finds the gaps that fit in 12 questions — it does not find the gaps. The market analyst (Sonnet) again added nothing, as in the 2026-07-12 run.
2. **Report discipline** — All four subagents returned clean, on-format tagged lists and correctly separated their own "OUT — post-junior" section; nothing had to be discarded for format. Overlap across the three new angles was high (MapStruct, `@Modifying`, `@WithMockUser`, H2-vs-PostgreSQL, catch-all handler surfaced in two or three each), which is the signal that the angle set had actually converged.
3. **Failures & retries** — None. All four ran synchronously and completed first try.
4. **Rule friction** — Two real ones. (a) The 12-question cap in Step 4a is a false ceiling on completeness and directly contradicts the standard's own completeness test ("if Victor studied ONLY these items, could he answer ANY question?"). (b) Neither the prompt nor the standard says what to do when an update pushes a section past the ">12 items → split" rule, or that Step 4b's sync must then mirror new/renamed *headings*, not just bullets — I split six sections and rebuilt the combined file's section wholesale.
5. **Verdict** — change worth considering, and it is not cosmetic: **replace the single capped adversary in Step 4a with a fan-out of angle-specific adversaries (code review · design decisions · take-home/live coding · production debugging), each explicitly uncapped, and stop when a new angle returns only duplicates.** Also state in Step 4/4b that an update may split an oversized section and that the sync mirrors headings too.
