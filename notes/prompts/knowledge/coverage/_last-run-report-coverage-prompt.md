# Coverage prompt — last run self-report

**Date:** 2026-07-18 · **Target:** TOPIC = Architecture, NOTES_PATH = notes/architecture/ · **Branch:** `fix/backend-backlog` · **Commit:** `57ef6b9`

1. **Plan vs reality** — the split held. Coverage was mature (78 lines), so the five 4a angles ran in parallel with the market analyst and consolidation was a single pass. The angle *selection* mattered more than the count here: the mandatory output-prediction angle was correctly dropped (meaningless for a non-language topic) and the two angles improvised for this topic — take-home/live-coding and existing-codebase onboarding — produced the run's only genuinely new sections. The four standard angles converged almost entirely on framework mechanics owned by Spring Boot.

2. **Report discipline** — clean. All six subagents returned bounded lists with their proof lines; the 4a angles each carried "78 lines, read to EOF" and the reviewer a correct "38 items reviewed". Nothing had to be trimmed or discarded.

3. **Failures & retries** — none. No re-dispatch; the failure protocol was not exercised.

4. **Rule friction and rule breaches** — no rule breached: branch guard ran, the session was on Opus, model overrides were passed on every dispatch, both structural counts ran mechanically, and the Step 4c sync was verified programmatically to EOF (the SQL run's heading-substitution trap did not recur — the transform was done in one scripted pass that handles line 1 separately). One real friction point: **Architecture is the inverse of the overlap case Step 4a is written for.** The prompt's worked examples are *foundational* topics that attract proposals from the layer above (JavaScript below Angular, Java below Spring Boot). Architecture sits *across* Spring Boot, Angular and General instead, so roughly two-thirds of the ~130 proposed gaps were already owned elsewhere — the ownership call was the most expensive judgement of the run and the one that shaped the output most. The prescribed "one grep, not a re-read" was not sufficient: it took a section-map grep, a 60-term grep, and two further greps scoped to individual topics' line ranges before ownership could be decided honestly. The rule was followed in spirit but a run following it literally would have under-checked and duplicated heavily.

5. **Verdict** — change worth considering: name the *cross-cutting* topic as the inverse of the foundational case in Step 4a's overlap guidance, with the explicit expectation that one grep will not be enough and that the honest outcome is dropping most proposals rather than adding them.
