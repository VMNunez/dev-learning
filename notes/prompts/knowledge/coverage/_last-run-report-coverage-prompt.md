# Coverage prompt — last run self-report

**Date:** 2026-07-18 · **Target:** TOPIC = Security, NOTES_PATH = notes/security/ · **Branch:** `fix/backend-backlog` · **Commit:** _(this run)_

1. **Plan vs reality** — coverage was mature (44 lines), so the six 4a angles ran in parallel with the market analyst and consolidation was one pass. Six angles: the four standard ones, plus onboarding (mandatory here — the Security interview is shown as real project code) and one improvised for the topic, **attacker mindset / threat modelling**. That improvised angle was the single highest-yield dispatch of the run: it produced almost all of `## Information disclosure`, `## Attacks on login and credentials`, and `## Transport and dependency risk`. The four standard angles converged almost entirely on Spring Security *wiring* — `SecurityFilterChain`, `@EnableMethodSecurity`, `hasRole`/`ROLE_`, proxy self-invocation, `UserDetailsService`, `OncePerRequestFilter` — every bit of which Spring Boot already owns. This is now the second consecutive run (after Architecture) where the improvised angle carried the output and the standard four converged on another topic's property.

2. **Report discipline** — clean. All seven subagents returned bounded lists with proof lines; the six angles each carried "44 lines, read to EOF" and the reviewer a correct "30 items reviewed". The market analyst confirmed the live web search ran and reported Security at **0/8 postings**, correctly shifting its weight to interview norms per the under-3 rule rather than padding with generic stack requirements.

3. **Failures & retries** — none. No re-dispatch; the failure protocol was not exercised.

4. **Rule friction and rule breaches** — no rule breached: branch guard ran, the session was on Opus, model overrides were passed on every dispatch, both structural counts ran mechanically (before and after the reviewer's fixes), and the Step 4c sync was verified programmatically by diffing bullets and headings. The heading-substitution trap did not recur — line 1 was transformed separately and asserted. Four things worth recording:

   - **The prompt's own framing warning proved correct, and inverted a second time.** The prompt cites the 2026-07-18 Security framing error (guessing JWT/CORS/injection "almost certainly" lived in other topics, when they are Security's own sections). Grepping first showed the *opposite* inversion from what that correction implies: Security does own those sections, **and** Spring Boot independently owns four full security sections (`Spring Security — the filter chain and configuration` / `— authorization and the current user` / `— authentication` / `JWT — design and trade-offs`, lines 853–892). Duplication runs outward *and* the wiring layer is owned elsewhere. Neither guess would have been right; only the grep was.
   - **Security is a cross-cutting topic and behaved exactly as the Architecture run predicted.** Roughly **80%** of ~130 proposed gaps were already owned — a higher fraction than Architecture's two-thirds. The Architecture run's suggested amendment (name the cross-cutting case, expect one grep not to be enough, expect to drop most proposals) is now in the prompt and was directly load-bearing here: it is the reason the run resisted adding a `## Spring Security configuration` section that six angles all asked for.
   - **A pre-existing sync drift was found and repaired.** `notes/coverage.md` was missing the *Broken access control (IDOR)* bullet that has been in `notes/security/coverage.md` — the combined `### Common vulnerabilities` held 6 items to the topic file's 8. A bullet-level drift like this survives any eyeball comparison; it was caught only because the verify step was run as a `diff` rather than a read. Worth making the programmatic diff the prescribed verification rather than "read side by side".
   - **Honest read note.** `notes/coverage.md` (2467 lines before, 2510 after) was **not** read end-to-end — Step 4a explicitly forbids that and prescribes greps. It was read via section map, term greps, and three targeted range reads (Spring Boot 826–940, Architecture 1243–1292, General 2394–2467), and the Security section was verified to its real end by diff. `notes/security/coverage.md`: 44 lines before, read to EOF.

5. **Verdict** — the prompt held; no change needed to its flow. Two small changes worth considering: (a) in Step 4c, prescribe **diffing** the mirrored bullets and headings programmatically instead of "re-read side by side" — this run found a real pre-existing drift that reading would have missed; (b) the angle list's "adapt to the topic" instruction is doing more work than its wording suggests — on two consecutive runs the improvised topic-specific angle produced most of the new sections while the four standard angles converged on another topic's property, so it may deserve to be stated as an expectation rather than a permission.
