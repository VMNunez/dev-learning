# Coverage prompt — last run self-report

**Date:** 2026-07-18 · **Target:** TOPIC = Angular, NOTES_PATH = notes/angular/

1. **Plan vs reality** — The work split as designed: one market analyst (Sonnet) + four adversarial angles (code review, design/decisions, take-home/live coding, debugging) on Opus, run in parallel. All five returned usable, on-target output; no angle was dropped or merged. Consolidated additions landed across the existing sections plus three new sections (Project bootstrap and configuration, Debugging Angular errors, State management strategy) and a split of the oversized "Components and templates" section into "Components, inputs and projection" + "Template syntax and rendering".
2. **Report discipline** — Clean. Every subagent returned only the required gap list / OUT list / proof line — no narrative, no code dumps.
3. **Failures & retries** — None; all five subagents passed the acceptance check on the first dispatch (source note per market item; "N lines, read to EOF" for every 4a angle).
4. **Model policy slip (generator ran on the wrong model, then corrected)** — The subagents carried the correct `model:` overrides (analyst Sonnet, all four 4a angles Opus). But the **generator role ran the first consolidation/word-crafting pass on Sonnet, not Opus** as the model-policy table mandates. That pass shipped three standard violations the Opus re-pass caught and fixed in a follow-up commit: a 16-item section (over the 12 limit) left unsplit, and two "one concept per item" violations (`environment.ts`+`fileReplacements` and `CORS`+`proxy.conf.json` each merged two concepts). Lesson: the generator model is not enforceable from inside the prompt — it depends on the session's own model being set to Opus before the run starts. Worth a one-line reminder in the prompt's Execution section to confirm the session model before Step 1.
5. **Rule friction** — Angular's notes are not yet migrated to `en/`/`es/` (flat numbered files still sit in the topic root per CLAUDE.md's documented state), so Step 1.3's "survey `{NOTES_PATH}en/`" instruction had no matching folder. Fell back to reading the file list only; the existing `coverage.md` was already detailed enough to anchor decisions.
6. **Verdict** — Change worth considering: add an explicit "confirm the session is running on Opus before Step 1" check to the Execution section — the model-policy table names Opus for the generator but nothing in the run flow verifies it, and a Sonnet generator pass demonstrably shipped standard violations this run.
