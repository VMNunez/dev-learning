# Pipeline self-report — 2026-09-04 — HTML junior first run (full recalibration + boundary migration)

Status: open

Skill-friction reconciliation: `_skill-friction.md` holds no `FRIC-` rows at all, so nothing was adjudicated and nothing was committed for it.

Ledger reconciliation: one finding cleared all four bar conditions and was filed as **`REC-207`** (`coverage-prompt.md` Step 5, mirror-rebuild shape rules). No breach log was created — this run skipped no mandated step, so there is no breach to log, and the contract creates that file on the first breach and never before.

- **Plan vs reality** — The four-role split held and the roles were not redundant: the analyst derived a floor blind, A found factual errors, B found ownership twins, and **C found twins that A and B had both read past** — two of them in `senior.md`, a file the run authored itself and neither normal reviewer was scoped to compare against adjacent topics. **This pipeline has no step that reads the finished artefact whole and is independent of the slice owners**, so beyond the four mandated reports there is no evidence the output is sound past what they prove. Two mis-sizings to record. First, **Reviewer A ran before `middle.md` and `senior.md` existed** — the prompt's `FIRST_RUN` text never says a new topic needs all three level files, but `validate-prompt-system.ps1` refuses the topic without them, so the orchestrator discovered the requirement from a failing validator *after* A had already reported "nothing belongs at middle or senior". A's `aria-live` finding then contradicted a middle placement A had never seen, and the orchestrator adjudicated it to junior on A's authority. Second, C's remit excludes new scope, and it was the only role positioned to notice that the run's own senior authoring had reached into two adjacent topics; it reported both anyway, correctly, and labelled the third as outside its remit.
- **Report discipline** — Nothing had to be trimmed or discarded. All four reports carried their `N lines, read to EOF` proofs and stayed inside their mandated return shapes. One accuracy note: A reported the target at 98 lines and 71 bullets, which was true of the draft it read and not of the file that shipped.
- **Failures & retries** — No dispatch failed and no re-dispatch was required. 4 of 4 mandated dispatches ran (market analyst, Reviewer A, Reviewer B, Reviewer C — the last mandatory because `FIRST_RUN` is true). Three reviewers returned `BLOCKED`; every finding was adjudicated and applied or recorded before the commit.
- **Rule friction and rule breaches** — No guard, role, validation, mirror, progress, commit, report or tracker step was skipped, and no rule was breached. The friction, and the run's one real finding, is **Step 5**. It states three rebuild rules — headings demote, bullets and order stay identical, "introduction appears once" — and is silent on the two shape rules the mirrors actually follow: a *sectioned* topic file has its intro dropped from the mirror while a *flat* one (every `senior.md`) keeps it, and `notes/coverage/senior.md` separates topic sections with `---` where the other two levels do not. The first rebuild broke both. What makes this worth a ledger row rather than a note is that **Step 5's own validations cannot detect either defect** — validation 1 compares bullet text and order, validation 2 compares headings, and the repo validator's invariant 2 compares only the top-level bullet multiset; intro prose and a `---` are invisible to all three. The intro half was caught by accident, by diffing an *untouched* topic's section and finding it disagreed identically; the separator half was caught by Reviewer C and volunteered as "a nit outside my remit". Neither was caught by anything the prompt mandates. Filed as `REC-207`. A second, smaller friction is recorded in bullet 1 and not filed: `FIRST_RUN` never states that a new topic owes all three level files.
- **Verdict** — change worth considering: Step 5's mirror rebuild is silent on the intro and separator shape rules, and none of its validations can see the resulting defect (`REC-207`).
