# Coverage prompt — last run self-report

**Date:** 2026-07-27 · **Target:** TOPIC = Spring Boot, LEVEL = junior, MODE = update · **Branch:** `fix/backend-backlog`
**Status:** open — clean run; no prompt change recommended

Ledger reconciliation: no new prompt-change recommendation this run. `_recommendation-ledger.md` is unchanged; REC-011 stays `open` and belongs to `notes-plan-prompt`, not to this orchestrator.

1. **Plan vs reality** — Second execution of the verify-gap fast path (REC-010) on this topic; all four entry conditions held, so Step 1 and the second reviewer were skipped as written. Scope sized correctly: four gaps in, three items added to junior and one moved up to `middle.md`, one scoped reviewer. 133 → 136 junior items, 14 → 15 middle items.
2. **Report discipline** — The scoped reviewer stayed inside its four-axis mandate and returned the proof line for all four files. Nothing trimmed.
3. **Failures & retries** — No subagent failed; no re-dispatch. One orchestrator error, caught by the mandated validation: deleting the rejected item's line swallowed a newline and merged two adjacent bullets into one. Step 5 validation 1 (mirror/target item parity) surfaced it immediately. This is the second consecutive run in which an orchestrator mechanical edit damaged file structure and Step 5 caught it — the previous run lost a `---` separator in a scripted mirror rebuild. Both were caught, so this is evidence the Step 5 validations are load-bearing, not evidence of a prompt defect. Acting on the previous report, this run rebuilt the mirror by applying the identical three-item delta instead of regenerating the section wholesale, and the mirror diff came out clean on the first attempt.
4. **Rule friction and rule breaches** — No mandatory step skipped: step-0 guards, run-scope determination, gap classification, adversarial pass, the scoped cold reviewer, all nine Step 5 validations, the `verify-junior.md` supersede in the same commit, staged-path inspection before each add and commit. No new friction. Third consecutive report naming the same wart: a stale duplicate of this report still sits at `notes/prompts/_internal/_last-run-report-coverage-prompt.md`, now three runs out of date, so a step-0 reader who opens that path instead of this one gets a report from 2026-07-24. It remains outside this run's declared outputs and was again not acted on; at three occurrences it should be deleted by whoever owns `_internal/` housekeeping rather than re-noted a fourth time.
5. **Verdict** — pipeline clean; no refinement earned. The run's signal is about role isolation rather than the prompt: of the four gaps the gate raised, the one the cold reviewer rejected on level (`@Scheduled`, unjustified specialisation at junior, moved to middle) was the one the *orchestrator* had added in its own adversarial pass during the preceding verify run — the reviewer caught an item its author could not judge, which is precisely what the author/reviewer split exists for. Two of the three accepted items also needed factual correction, one because it contradicted this same file's existing Open-EntityManager-in-View bullet — only reachable because the reviewer mandate requires reading the target file whole rather than the added lines in isolation. `coverage-prompt.md` is 218 lines, well under the alarm.

Close-out evidence (against disk, `git show --stat`): declared outputs `notes/spring-boot/coverage/junior.md`, `notes/spring-boot/coverage/middle.md`, mirror `notes/coverage/junior.md`, and the superseded `notes/spring-boot/coverage/verify-junior.md` all landed in `93ce1b2`. No inbox routing was produced — the one pending `_cross-topic-inbox.md` entry sits under `## General`, not under this topic, and was correctly left alone — so no second content commit was due. Required dispatches for this scope: 1 scoped cold reviewer; dispatched: 1. Report + `_run-tracker.md` committed together next.
