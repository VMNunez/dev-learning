# Coverage prompt — last run self-report

**Date:** 2026-07-26 · **Target:** TOPIC = Java, LEVEL = junior, MODE = update · **Branch:** `fix/backend-backlog`
**Status:** open — clean run; no prompt change recommended

Ledger reconciliation: no new prompt-change recommendation this run; the `_recommendation-ledger.md` rows (REC-001…009) stay `applied`. Nothing added or updated.

1. **Plan vs reality** — the prescribed split held: one cold market analyst set the junior floor (live search ran), the orchestrator classified and consumed the single verify gap, two cold reviewers challenged calibration and ownership. The run's real content was narrow — one `verify-junior.md` gap — but every mandated role still ran; no step was collapsed because the change was small.
2. **Report discipline** — all three subagents returned within their briefs and opened with the correct `154 lines, read to EOF` and item count on the first attempt; no acceptance proof had to be rejected or trimmed.
3. **Failures & retries** — three of three mandatory roles ran; no re-dispatch. The single content commit hit no sandbox/index block.
4. **Rule friction and rule breaches** — no mandatory step skipped: branch/status guards, whole-file EOF reads, live market floor, classification against the standard, verify-gap adjudication, adversarial pass, two cold reviews, mirror rebuild with full 116/116 parity + format + uniqueness + `git diff --check`, notes-plan SHA recompute (mismatch → stale, as expected), staged-path check, and commit verification all ran. Both reviewers independently flagged the added item as bundling several concepts; the finding was accepted and the item split (representation/comparison + division-by-zero), so a standard violation was caught pre-commit rather than shipped. One standing ambiguity resurfaced and was recorded, not acted on: Maven ownership under Java is a cross-topic-standard question, not resolvable inside a single-topic Java run.
5. **Verdict** — pipeline clean. No finding cleared the refinement bar: the reviewer findings were content (applied to the artifact), not prompt defects; the Maven-ownership item is friction (#3 — would not have changed this run's output), recorded and not applied.

Close-out evidence (against disk): declared update-mode outputs — topic scope file `notes/java/coverage/junior.md`, global mirror `notes/coverage/junior.md`, and the superseded `verify-junior.md` — changed in `21e297c`; no inbox routing was required (no other-topic concept surfaced). This report and `_run-tracker.md` (Java Coverage J + Verify J cells) are the remaining declared outputs, committed together next. `coverage-prompt.md` is 189 lines, below the ~500-line alarm.
