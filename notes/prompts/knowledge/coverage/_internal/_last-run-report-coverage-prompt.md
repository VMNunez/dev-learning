# Coverage prompt — last run self-report

**Date:** 2026-07-26 · **Target:** TOPIC = Spring Boot, LEVEL = junior, MODE = update · **Branch:** `fix/backend-backlog`
**Status:** open — clean run; no prompt change recommended

Ledger reconciliation: no new prompt-change recommendation this run. `_recommendation-ledger.md` rows are unchanged; REC-011 (notes-plan mid-route renumbering) stays `open` and was surfaced at step 0 as required — it belongs to `notes-plan-prompt`, not to this orchestrator.

1. **Plan vs reality** — Full recalibration: no `verify-junior.md` exists for this topic, so the fast path was unavailable and the default applied. The one-analyst/two-reviewer split was correctly sized — the analyst set the floor, Reviewer A found nine genuine gaps and five factual errors, Reviewer B found the ownership and structure defects. 103 → 130 items.
2. **Report discipline** — All three reports opened with the required proof lines and stayed read-only and evidence-shaped. Nothing had to be trimmed or discarded.
3. **Failures & retries** — No subagent failed; no re-dispatch. One orchestrator error: the first content commit was written with PowerShell here-string syntax inside the Bash tool, leaking a literal `@` into the subject line; caught by the mandated `git show --stat` and fixed with `--amend` before the second commit.
4. **Rule friction and rule breaches** — No mandatory step skipped: step-0 guards (branch, status, one topic, gate, scope), all whole-file EOF reads, the market analyst with its junior-floor acceptance proof, full classification of all three level files, the adversarial pass, both cold reviewers, mirror rebuild (130/130 bullet and 15/15 heading parity, no cross-level normalized overlap, no format violations), notes-plan SHA recompute, staged-path inspection before each add and commit. **One environment hazard worth recording:** a stale duplicate of this report lives at `notes/prompts/_internal/_last-run-report-coverage-prompt.md` (dated 2026-07-24, same topic) while the live path is this folder's `_internal/`. A step-0 reader who opens the wrong one gets a two-day-old status. Not acted on — outside this run's declared outputs.
5. **Verdict** — pipeline clean; no refinement earned. Reviewer B was the first role in this topic's history to read the global mirror's *other* topic sections, which is what surfaced eleven ownership overlaps that had survived a previous coverage-audit; that is the reviewer mandate working as written, not a prompt gap. `coverage-prompt.md` is 219 lines, well under the alarm.

Close-out evidence (against disk, `git log --name-only`): declared outputs `notes/spring-boot/coverage/junior.md`, `notes/spring-boot/coverage/middle.md`, and mirror `notes/coverage/junior.md` landed in `d6109d7`; inbox routing landed separately in `c68e377`. No `verify-junior.md` existed, so nothing to supersede. Required dispatches: 3 (one market analyst, two cold reviewers); dispatched: 3. Report + `_run-tracker.md` committed together next.
