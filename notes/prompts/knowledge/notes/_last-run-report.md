# Notes-audit — last run self-report

**Date:** 2026-07-12 · **Target:** SCOPE = folder, TOPIC = Java (16 files built: 15 existing 00–14 + 1 new 15-memory-model)

1. **Plan vs reality** — Split held up well. Planner found the one real coverage gap (memory model → new file 15) plus one TODO-heavy file (09), and the 15 per-file inspectors surfaced consistent, real defects (cold seams, en-behind-es drift, two non-compiling code samples in 12, broken 08-generics links in 05, in-section duplication in 04/06). One-cold-subagent-per-file was correctly sized — no inspector or builder was overloaded.
2. **Report discipline** — Clean. Every subagent returned a section-by-section trace and a bounded report; nothing had to be trimmed or discarded. The gold-standard file (08) inspector summarised rather than giving a literal heading-by-heading trace, but its finding (es-only drift) was sound.
3. **Failures & retries** — Three interruptions from Anthropic **session limits** (not pipeline faults): the 11-enums inspector and the stage-C committers of rows #3 and #10 were each cut off mid-work. Recovery worked as designed — `git log`/`git status` showed nothing was committed, and re-dispatching the exact stage resumed cleanly. The resumability model (finished rows `[x]`, skipped on re-run) proved essential across a run this long.
4. **Rule friction** — One genuine ambiguity: the new file 15 required a **CLAUDE.md counter bump**, which the orchestrator's "only notes/ paths staged" rule doesn't explicitly cover. Resolved by folding CLAUDE.md into row #2's commit as one logical change ("add memory-model note"). Second, minor: **es internal-link filename convention is inconsistent across the folder** — some es files link to English filenames (es/09, es/15 as created), most link to Spanish filenames; stage-C reviewers fixed several broken links ad hoc (e.g. 10-generics.md → 10-genericos.md in es/07). Worth a standard rule.
5. **Verdict** — pipeline clean; two changes worth considering: (a) a one-line rule for structural counter-bumps (CLAUDE.md) riding with the create-file commit, and (b) a stated es-links-use-Spanish-filenames convention so translators don't leave English-named links that later break.
