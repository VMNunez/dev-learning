# Notes-audit — last run self-report

**Status:** fact-check gate applied to `notes-write` in `3b5dea2` (2026-07-19). Second verdict item —
"planner should not treat row count as scope" — considered and **rejected: fails condition 3** (it
affects the orchestrator's scope estimate, i.e. planning cost, not the correctness of what reaches the
notes). Not to be re-proposed.

**Date:** 2026-07-15 (run spanned 2026-07-13 → 07-15) · **Target:** SCOPE = folder, TOPIC = Spring Boot — scoped by Victor to `notes/spring-boot/` only (`notes/java/` rows deferred to a future run). 14 Spring Boot rows built: 3 new files (11 business-logic, 12 production-debugging, 13 logging), 1 new section (Postman in 09), 10 existing-file rebuilds (00–10). One duplicate `06` file deleted by Victor mid-run.

1. **Plan vs reality** — Planner predicted 8 rows; Phase 1.5 inspectors surfaced findings on ~26 existing files — an order-of-magnitude scope blow-up. The per-file split (one cold subagent, four-stage pipeline per row) was correctly sized, but the planner materially under-scoped existing-file work: it lists files to inspect without predicting how many need rebuilds, so "8 rows" was never a real size estimate. The inspectors, not the planner, are where true scope became visible.
2. **Report discipline** — Reports were long but on-target (traces, verdicts, file lists); nothing discarded. The section-by-section trace gate held on every stage — no re-dispatch was ever needed for a missing trace.
3. **Failures & retries** — Three hard interruptions from Anthropic **session limits** (not pipeline faults), killing the translator mid-file on rows 07 and 11 and stage B on row 02. Recovery worked every time: parity checked on disk (`grep`/`wc` headings + line counts) and the exact stage re-dispatched or verified. No subagent returned unusable content. Minor: two inspectors (05, 07) reported writing a worklist row that never landed (added by hand); inspector 13 searched the `es/` file by its English name and false-flagged missing parity (caught immediately).
4. **Rule friction** — Biggest friction: a **mid-session manual commit by Victor** (`85a7c34`) during a session-limit gap committed the in-flight `en/02` AND tracked `notes-worklist.md`. That desynced `es/02` (fixed by resuming at B/T/C) and put the temporary worklist under version control (fixed with a `git rm --cached` commit at the end, `407f168`). The pipeline has no guidance for "the working tree changed under you between stages." Second, the run also spanned a **branch change** (`feat/timeentry-workflow` → `projects/07-timetrack` → `fix/backend-backlog`) with Victor's own uncommitted backend code present throughout — the "never stage `projects/**`" reminder in every stage-C dispatch was load-bearing and worked.
5. **Verdict** — change worth considering: **the author stage (notes-write) needs a hard "verify every code fragment and every framework version-fact against the live repo/pom before writing, never from memory" gate.** This run caught fabricated code and/or wrong version-facts in 01/02/03/04/05/06/08/09 (a `@JsonIgnore` that doesn't exist, a fake `JwtService`, a `config/JwtProperties` that isn't there, a whole `Transaction`/`TransactionService` domain, `@MockBean` vs Boot-4 `@MockitoBean`, `spring-boot-starter-web` vs `-webmvc`, the `spring-boot-starter-test` split) plus ~8 invented Baeldung URLs across 04/06/07/08. B caught all of them — but only because B ran on Opus with an explicit fact-check mandate; on a cheaper reviewer these ship. Factual correctness is currently carried by stage B, not prevented at stage A. Secondary: planner should state "existing-file findings are unknown until inspection — do not treat row count as scope." Prompts otherwise sound; these two are the open items from this run.
