# Notes-audit — last run self-report

**Date:** 2026-08-25 · **Target:** Java / junior / note 02

**Status:** rejected

1. **Plan vs reality** — The split held on the plan's only `create` entry. Evidence beyond the traces: Stage B, cold and reading the finished English whole, caught five defects Stage A's own green trace had not — a `@Query` text block presented under a real project path that silently dropped two clauses of the real query, a missing `Docs:` line on the one concept section that had none, three Oracle links pinned to `javase/21` while the sibling chapter and the local JDK are 25, four undeclared forward references (`for (… : …)`, `List<>`, `e.getName()`) inside the `StringBuilder` examples, and a handoff sentence that contradicted the two loops in the section above it. A post-authoring reader that is not the author is again what caught all five. Stage C found nothing, which is again the weaker signal — it reads Spanish derived from an already-reviewed English file.
2. **Report discipline** — One return had to be checked rather than trusted. Stage T returned its `N lines, read to EOF` for the **source** file it read, not the target it wrote, and its parity statement miscounted the heading tiers (claimed 3 `##` + 12 `###`; the files carry 6 `##` + 13 `###`). Nothing had to be trimmed or discarded; no code dumps or narrative.
3. **Failures & retries** — None. Zero re-dispatches. Required dispatches: 4; actual: 4.
4. **Rule friction and rule breaches** — One breach, no friction. The trace gate says a stage whose trace or EOF proof is incomplete is re-dispatched once; Stage T's proof was incomplete in exactly that way and the run did **not** re-dispatch it — it verified parity mechanically instead (`grep -o '^#*'` paired across both files: 20 headings, identical levels and order; 60 fence lines each; 505 lines each), then let Stage C's independent `505 lines, read to EOF` on the Spanish stand as the missing proof. The cost here was nil because Stage C reads that exact file whole and passed it, but the gate was substituted, not satisfied. No guard, dispatch or commit boundary was otherwise skipped; the working tree's unrelated modified project file stayed out of all three commits.
5. **Verdict** — change **rejected on condition 2**: a clause letting the orchestrator discharge a stage's EOF proof mechanically when the next stage independently proves the same file. The trace gate is unambiguous and the run simply did not follow it — rewriting an already-clear rule buries the lesson, and the substitution only looked safe because a later stage happened to read the same file. The lapse to watch is the orchestrator's, not the prompt's. `notes-audit.md` is 233 lines, under the ~500-line budget.
