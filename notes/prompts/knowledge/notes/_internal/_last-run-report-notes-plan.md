# Last run report — notes-plan-prompt

Date: 2026-08-02
Target: Spring Boot / junior / update
Status: open

- **Plan vs reality** — The reconciliation was the largest this prompt has done: the stored fingerprint was stale and the diff was structural, not incremental. The mandated cold reviewer is the only step that reads the finished plan whole, and it earned its dispatch — 6 SEVERE findings, of which two were execution defects a green local check could not see (two entries pointing at filenames the renumber had not yet created, and a bilingual renumber whose Spanish half was declared only in prose). A local mechanical re-validation after applying findings then confirmed exact-once coverage assignment, numeric ordering, `Depends on` = `Prerequisites`, action-vs-file-existence, and zero broken local links. The split enforcement worked as intended.
- **Report discipline** — The reviewer returned every mandated proof element and 19 numbered corrections, correctly severity-marked. Two had to be discarded rather than trimmed, both format violations: writing content contracts into `Pending additions` on `pending` entries, and adding a *later* entry number to `Prerequisites`. This is the second consecutive run in which the reviewer proposed edits the plan format forbids — expected, since the prompt deliberately withholds the format rules from the cold input, and in both runs the discard cost nothing beyond folding the substance into `Narrative role`.
- **Failures & retries** — One cold reviewer required, one dispatched, one completed with full acceptance proof on the first attempt. No retry. No refinement gate needed (see Verdict).
- **Rule friction and rule breaches** — Required dispatches: 1 / 1. No gate skipped, no declared output missed. One friction point, recorded not applied: the relocation machinery is level-only, so when a *sibling topic* takes ownership of concepts, the correct outcome is an intact bilingual pair parked under `## Unassigned existing notes` with a prose disposition and no mechanism to hand it to the owning topic's plan. That is what the prompt says to do and it produced the right file; it also leaves pairs sitting in a topic that no longer claims them, waiting on a run this prompt cannot trigger. Fails bar condition 3 — the plan would be identical either way.
- **Verdict** — pipeline clean. REC-019 (unassigned notes do not reserve their numeric prefixes, applied last run) was exercised for the first time and did exactly what it was added for: it unblocked the chapter split the previous run had to defer. No new edit proposed, so no cold refinement gate was dispatched. Prompt is 357 lines, inside the health budget.
