# Last run report — notes-plan-prompt

Date: 2026-08-02
Target: Java / junior / update
Status: open

- **Plan vs reality** — Reconciliation against a stale fingerprint (`4c9d4cc9` → `b8216257`), driven entirely by coverage churn rather than by note drift: three bullets split into six, four added, 121 → 128 concepts. The stored plan had no concept checkboxes and no `Pending additions` fields, so a full metadata migration ran alongside; with every entry `pending` and no `refined` freeze anywhere, the migration was unambiguous and no coverage lock was at risk. The classification stage — one cold analyst per English note, sixteen in parallel, each reporting `N lines, read to EOF` and a per-section ownership table — returned 13 clean `keep` verdicts and 3 `requires split` claims. All three were rejected on inspection: each was a sub-20-line passage (a record compact constructor, a type-erasure aside, a Spring-Boot starter callout) inside a chapter whose central learning unit is unambiguously junior. The split section and its `blocked` outcome are for pairs whose *sections* are owned by different levels, not for prose overreach a `notes-audit` trim resolves — so the run continued and the three findings were converted into per-entry audit notes and scope-boundary must-answer questions instead. Worth recording: the per-file analysts were the only step that could have found these, and their real value was not the level verdict but the eight *unwritten* bullets they surfaced (wildcards, `YearMonth`, Javadoc literacy, `Arrays.asList`, `Optional.filter`, `toMap` merge, `allMatch`, Maven Wrapper) — coverage the old plan silently recorded as owned.
- **Report discipline** — The cold reviewer returned every mandated proof element and 14 numbered corrections, severity-marked, on the first dispatch. All 14 were accepted; none had to be discarded for format violations, which breaks the two-run streak noted in the previous report. Its strongest finding was self-referential and I would not have caught it: entry 01's rationale for refusing to split justified itself on *folder sort order*, in a document whose own header spends a paragraph establishing that folder order is not reading order. The plan was using its own convention when convenient and disowning it when inconvenient. That is a class of defect only a reader with no authorial investment finds.
- **Failures & retries** — One cold reviewer required, one dispatched, one completed with full acceptance proof on the first attempt. No retry. Sixteen classification analysts dispatched, sixteen returned with the required read-to-EOF proof line.
- **Rule friction and rule breaches** — Required dispatches: 1 / 1 (plus 16 discretionary classification analysts). No gate skipped, no declared output missed. One friction point, recorded not applied: the renumber clause in Planning algorithm step 6 is written as though the blast radius of a rename were local to the topic, and here it is not — twelve legacy files carry ~450 inbound links from the Spanish notes, the Spring Boot notes, `_note-quality-standard.md`, two prompt internals, both skill mirrors, and `_run-tracker.md`. The clause permits the rename and says nothing about a link surface that crosses into prompt-system machinery, so the planner is left to decide alone whether "required local-link corrections" authorises editing a standard and two skills. I declined and documented the refusal in the plan header. Fails bar condition 3 — the prompt text would not have changed the outcome, only the confidence.
- **Verdict** — pipeline clean. The one substantive observation for a future refinement pass is that `## Legacy notes requiring split` has no graded form: an analyst that finds *any* cross-level content in a file has only `requires split` (which blocks the entire run) or silence. Three files hit that boundary this run and all three were correctly resolved as prose-level trims, but the resolution came from the orchestrator overriding three subagent verdicts rather than from the analysts having a proportionate verdict available. If a future run has less headroom to second-guess, the failure mode is a blocked plan over eighteen lines of prose. Not proposed as an edit yet — one run is not evidence. Prompt is 357 lines, inside the health budget.
