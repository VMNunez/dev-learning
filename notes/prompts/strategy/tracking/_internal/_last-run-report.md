# Pipeline self-report — progress-update

**Date:** 2026-08-25 · **Target:** `MODE = active` — project 07-timetrack, plus SQL and simulations · **Status:** rejected — bar condition 3

1. **Plan vs reality** — the mandated split (one project subagent + one SQL subagent, simulations and the thirteen-topic matrix measured locally) held. This pipeline has no whole-artefact reviewer, so the two traces prove only that the machinery ran; the content guard is Step E's section-bounded `git diff`, which passed on a single hunk inside `Professional level by topic`.
2. **Report discipline** — the project subagent's three-line report carried one clause beyond its contract, citing the plan's §0 quick reference as confirmation. `_concept-extraction-standard.md` Step 2 excludes §0 as a source, but the returned status was derived `(from ✅ markers)` and the read verification was present, so the clause was discarded rather than re-dispatched — the prompt's re-dispatch trigger is a missing or truncated read, which did not fire.
3. **Failures & retries** — none. Both subagents returned usable work first time; last run's 1505-vs-1820 line-count retry did not recur (the plan now reads 1944 lines to EOF).
4. **Rule friction and rule breaches** — **one breach.** Step 0 item 2 asserts the shared session rules are "already loaded into your context by the supported agent runtime; do not re-read the file". In Claude Code only `CLAUDE.md` is injected, and it mandates reading `_session-rules.md` completely before changing files, so the run executed its measurement steps against the prompt's premise and read the rules file only at close-out. `roadmap-review-prompt.md` already contradicts that premise ("do not assume the shared session rules are loaded"), and `README.md`'s `Reads`-column fence restates it, so the premise is system-wide rather than local to this prompt. Cost: nothing — the file's `PROGRESS.md updates`, branch and commit-hygiene sections restate rules this prompt already carries inline, and the output was identical. **Friction, unchanged from 2026-08-08:** Step B again reported the deleted legacy `main:practice/sql/02-joins.sql` (10 exercises); D3 resolved it from route §1's explicit tombstone and produced no false drift. Second consecutive report naming it, but no step was skipped, so the two-in-a-row extraction trigger does not fire.
5. **Verdict** — pipeline clean; no prompt change proposed. The session-rules premise fails **bar condition 3**: the run would have produced a byte-identical drift report and matrix edit either way, so it is a discipline note, not a prompt defect. The legacy-SQL friction fails condition 3 for the same reason it did last run. `wc -l` on `progress-update-prompt.md` — 541 lines, over the ~500 smoke-alarm line; largest section is `## Step D — Measure every section against its sources` (~200 lines, D1–D10). No edit was drafted, so no cold reviewer was dispatched and one-in-one-out did not apply.

maps unaffected (no machinery edit) · map: corrected — `README.md` catalogue `Reads` cell for `progress-update-prompt.md`, which omitted the `_run-tracker.md` read D8 mandates for the `*` provisional-denominator flags
