# Pipeline self-report — progress-update

**Date:** 2026-09-12 · **Target:** `MODE = all` — projects 01–07, plus SQL and simulations · **Status:** rejected

1. **Plan vs reality** — the mandated split held: seven project subagents plus one SQL subagent, eight of eight dispatched and returned, with simulations and the fourteen-topic matrix measured locally. This pipeline has **no whole-artefact pass** written by anyone but the slice owners, so the traces support only "the machinery ran"; what the orchestrator's own measurement adds is stronger, because D3/D4/D8/D9/D10 are recomputed locally from primary sources rather than taken from a subagent — the one drift row and both D7 edits came from that local arithmetic, not from any returned report. The **mode** was resolved against the default: the configuration block defaults to `active`, and `_run-tracker.md` records `04-meal-finder`'s portfolio-audit blocking the same day with *"`progress-update MODE = all` is owed"*, so `active` would have produced a report whose `Scope:` line could not serve the one consumer waiting on it. Ran `all`.
2. **Report discipline** — clean. All eight subagents returned exactly the three contracted items, the SQL one a single table row, none needing a trim. No PLANNING.md excerpts and no reasoning traces arrived.
3. **Failures & retries** — none. Eight of eight usable first time; the 07 subagent read 2005 lines to EOF in offset passes and carried its verification.
4. **Rule friction and rule breaches** — **no breach.** Step 0's run-start check ran against a `rejected` report and proceeded silently; the branch guard ran on `chore/portfolio-audit-04`; the drift report committed alone and first (`git show --stat` listed one file) before the matrix commit, which listed only `PROGRESS.md`; the Step E diff check confirmed both hunks inside `Professional level by topic` with `Practical evidence` byte-unchanged. **Friction, third occurrence and now costed:** the prompt's step 0 does not name the `CLAUDE.md`-mandated `_session-rules.md` read — `REC-232`, open, raised off two `portfolio-audit` runs. This run read it first, and that is the only reason the mode question was visible: the rule that `MODE = active` "can never close G6 for a **completed** project" lives in those session rules and in no step of this prompt. The cost is now demonstrated rather than hypothetical, on a third pipeline. **Friction, resolved not recurring:** last run's D7 two-source ambiguity (a plan declaring `Plan status: current` while its tracker cell flags it stale) was settled this run by computing `_coverage-standard.md`'s canonical scope-byte digest for all thirteen plans — three match, ten do not, and the split reproduced the tracker's flags exactly, so the two sources never actually disagreed. That measurement is what exposed the two stale `Next gate` cells. **Friction, second occurrence, no cost:** the 07 subagent returned **Format C** again where the standard's Format B definition names project 07 by name. The step status was marker-derived and identical either way, so it is recorded, not routed — and the orchestrator may not read `PLANNING.md` to adjudicate.
5. **Verdict** — pipeline clean; no prompt change proposed. The mode-resolution finding is **rejected on condition 4**: the rule it would add is already stated, in `_session-rules.md` → "PROGRESS.md updates", and the reason this prompt cannot see it is `REC-232`, already open — a second row for the same gap would be the refill the ledger's preamble forbids. The Format B/C disagreement is **rejected on condition 3** for the second run running: the output would not have differed, and the definition it contradicts sits in a *standard*, which no run may edit. `wc -l` on `progress-update-prompt.md` — **588 lines**, up from 557 at the last run and over the ~500 smoke alarm, so one-in-one-out would bind any edit; largest section is still `## Step D — Measure every section against its sources` (~205 lines, D1–D11). No edit was drafted, so no cold reviewer was dispatched and one-in-one-out did not apply. No breach log exists for this prompt and this run created none.
