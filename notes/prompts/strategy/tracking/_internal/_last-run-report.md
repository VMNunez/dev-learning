# Pipeline self-report — progress-update

**Date:** 2026-09-07 · **Target:** `MODE = all` — projects 01–07, plus SQL and simulations · **Status:** clean

1. **Plan vs reality** — the mandated split held: seven project subagents plus one SQL subagent, with simulations and the fourteen-topic matrix measured locally. This pipeline has no whole-artefact reviewer, so the eight traces prove only that the machinery ran; the content guard is Step E's section-bounded `git diff`, which passed on a single hunk covering only the `Professional level by topic` rows. `MODE = all` cost eight parallel dispatches and returned six identical Format A reports — the six closed Angular projects can only ever return "all steps complete", so the mode's real work was the 07 report and the twelve local measurements.
2. **Report discipline** — one deviation. The SQL subagent prefixed its table with two prose lines ("Both HEAD and main have identical files", "Guard passed") that its contract does not ask for; the required per-topic row was present and correct, so the prose was discarded rather than re-dispatched. The seven project subagents returned exactly the three contracted items.
3. **Failures & retries** — none. All eight subagents returned usable work first time. The 07 subagent read 2003 lines to EOF, over the 2000-line truncation threshold Step 0 warns about, and its report carried the verification.
4. **Rule friction and rule breaches** — **no breach.** Step 0's run-start check ran against a `rejected` report and proceeded silently; the branch guard ran; both commits were preceded by `git status` and each carried one file. **Friction, new:** D7 requires the per-topic `Knowledge consolidation` cell to report whether the notes plan is current, but two sources answer that question and disagree — the plan file's own `Plan status:` field and `_run-tracker.md`'s stale flag. Architecture declares `current` while its `Plan J` cell carries `⚠ stale 2026-09-03 (+7 bullets)`; Java, Spring Boot and SQL each show a variant of the same split. D7 names neither source, so the run chose to report both, which is defensible but unstated. Cost: nothing measurable this run — the cells are prose, no percentage moved — but a later run reading only one source would silently produce a different matrix. **Friction, resolved:** last run's recurring legacy `main:practice/sql/02-joins.sql` report did not recur; Step B returned `01-basics.sql` alone on both refs, so the two-in-a-row extraction trigger does not fire and the item is closed.
5. **Verdict** — pipeline clean; no prompt change proposed. The D7 two-source ambiguity fails **bar condition 3**: the run would have produced the same matrix edit and the same drift report under either reading, since the disagreement changes the wording of a prose cell and nothing the gates read. Recorded here so a second occurrence, or one that does move a figure, clears the condition. `wc -l` on `progress-update-prompt.md` — 541 lines, over the ~500 smoke-alarm line; largest section is `## Step D — Measure every section against its sources` (~200 lines, D1–D10), unchanged from last run. No edit was drafted, so no cold reviewer was dispatched and one-in-one-out did not apply.
