# Project Backlog — TimeTrack

Improvement tasks for this project, written by `notes/prompts/projects/review/review-audit.md`.
Completed tasks stay checked off (✅) — never delete them, only add or update.

**Last Reviewed — backend:** 2026-07-23
**Last Reviewed — frontend:** never
**Overall quality:** Good — layered architecture, the DTO boundary (intact across every resource), the uniform `@RestControllerAdvice` error contract and the security infrastructure remain solid, and **every previously-blocking High is now closed on disk** (JWT filter → 401, entry role checks, `getById` active filter, inactive-user login, report status filter, `reopen`, `Specification<T>` filters, full user CRUD, `data.sql` → profile-gated `DataInitializer`). This re-review covered the large body of newly-built code (user CRUD, `reports/summary`, `reopen`, role checks) that no reviewer had seen since 2026-07-17. It found **one new High** — `POST /api/auth/login` returns only `token`, but PLANNING §10 documents `AuthResponse — token, name, role` and Step 7a's role-based routing depends on those fields — plus a cluster of plan/code reconciliations on the fresh endpoints (user DTOs diverge from §10, the by-employee projection field name, summary-vs-table status totals, the submit-time inactive-project check) and the known persistence hardening still open (no `@Transactional`, N+1, DTO size bounds).

> The `frontend` tier has never been reviewed because Step 7 (Angular) is the current step — there is no
> frontend code yet. The old single `**Last Reviewed:** 2026-07-06` line was migrated to the per-tier form
> on 2026-07-14; it was **not** carried onto the frontend line, which would have made an unbuilt tier look
> reviewed.

## Tasks

- [x] **[High]** — Add `@PreAuthorize("hasRole('MANAGER')")` to `GET /api/users` in `UserController` — currently any authenticated user (including EMPLOYEE) can list all users, contradicting PLANNING.md's "Users (Manager only)" spec *(Effort: Small)* — ✅ done 2026-07-09
- [x] **[High]** — Create a `UserResponse` DTO and map to it in `UserController`/`UserService` instead of returning the `User` entity directly — the raw entity currently exposes the BCrypt password hash in the JSON response *(Effort: Small)* — ✅ done 2026-07-09
- [x] **[High]** — Add `@PreAuthorize("hasRole('MANAGER')")` to `PUT /api/projects/{id}` and `DELETE /api/projects/{id}` in `ProjectController` — only `POST` currently has the check, so an EMPLOYEE token can edit or deactivate any project today *(Effort: Small)* — ✅ done 2026-07-08
- [x] **[High]** — Create `ResourceNotFoundException` (planned in PLANNING.md's folder structure but never added) and throw it from `ProjectService.getById/update/delete` instead of generic `RuntimeException`; add a handler for it in `GlobalExceptionHandler` returning 404 — right now a missing id falls through to a default 500 *(Effort: Small)* — ✅ done 2026-07-07: also extended to `TimeEntryService`; added `BusinessRuleViolationException` (400) for domain rule violations, and a generic `RuntimeException` catch-all (500) as a safety net
- [x] **[Medium]** — Filter `GET /api/projects` by role in `ProjectService.getAll()` — PLANNING.md's REST API spec says employees should see only active projects while managers see active + inactive; currently everyone sees everything *(Effort: Small)* — ✅ done 2026-07-08: added `findByActiveTrue()` to `ProjectRepository`, branch on role read from `SecurityContextHolder` authorities
- [ ] **[Low]** `[backend]` — Confirm `spring.jpa.show-sql=true` in `application.properties` is intentional for local dev — fine to keep for now, but flag before considering the backend portfolio-ready *(Effort: Small)*
- [x] **[Medium]** — Add Bean Validation (`@NotBlank`, `@NotNull`, etc.) to request DTOs and `@Valid` on controller method params — only `LoginRequest` has validation annotations today; `CreateProjectRequest`, `UpdateProjectRequest`, `CreateTimeEntryRequest` and others accept blank/null fields with no 400 response, even though `GlobalExceptionHandler.handleValidation` is already wired up and unused *(Effort: Medium)* — ✅ done 2026-07-13: added `@NotBlank`/`@NotNull` to `CreateProjectRequest`, `UpdateProjectRequest`, `CreateTimeEntryRequest`, `RejectRequest` + `@Valid` on their controller params; verified via Postman that Bean Validation accumulates all field errors in one response (unlike the fail-fast manual checks)
- [x] **[High]** — Add an `@ExceptionHandler(AccessDeniedException.class)` in `GlobalExceptionHandler` returning 403 — right now the generic `RuntimeException` catch-all intercepts `AccessDeniedException` first (it's a `RuntimeException` with no specific handler), so any `@PreAuthorize` rejection returns 500 instead of 403; confirmed via `DELETE /api/projects/{id}` with an EMPLOYEE token *(Effort: Small)* — ✅ done 2026-07-09: also introduced a unified `ErrorResponse` DTO (timestamp/status/error/message/fieldErrors + `@JsonInclude(NON_NULL)`) across every `@ExceptionHandler`, replacing inconsistent `Map.of("error", ...)` / `Map.of("errors", ...)` shapes
- [x] **[Medium]** — Run a full Postman test pass over every endpoint built so far (auth, projects, entries) — happy path + error cases (401/403/404/400) for each, to surface bugs like the `AccessDeniedException` 500 above before moving further into Step 5 *(Effort: Medium)* — ✅ done 2026-07-09: full pass over auth/projects/users/entries surfaced and fixed 2 more bugs — `GET /api/users` missing `@PreAuthorize` + leaking the BCrypt password hash (no `UserResponse` DTO), and unauthenticated requests returning an empty 403 instead of 401 (added `JwtAuthenticationEntryPoint`)
- [x] **[Medium]** — Add an `@ExceptionHandler(HttpMessageNotReadableException.class)` in `GlobalExceptionHandler` returning 400 — right now sending `PATCH /api/entries/{id}/reject` with no body at all falls through to the generic `RuntimeException` catch-all and returns 500 instead of 400; confirmed via Postman on 2026-07-10. A malformed/missing request body is a client error, not a server error *(Effort: Small)* — ✅ done 2026-07-10
- [x] **[Low]** — Design decision (not a bug): manual business-rule checks (`if (...) throw ...` in `TimeEntryService.create`/`submit`) stay fail-fast — each throws on the first violation found, later checks in the same method never run. Confirmed via Postman on 2026-07-09: stacking a nonexistent projectId + a future date + out-of-range hours in one request only surfaces the first check hit (`Project not found`). This only applies to manual imperative checks — Bean Validation (`@Valid`/`fieldErrors`) is a separate, declarative mechanism that already accumulates all field errors via `Collectors.toMap`, because Spring evaluates every annotation upfront regardless of order. No code change — documented as the project's convention *(Effort: N/A)*
- [x] **[Medium]** — Add an `@ExceptionHandler(MissingServletRequestParameterException.class)` in `GlobalExceptionHandler` returning 400 — right now a missing required `@RequestParam` (e.g. `GET /api/reports/by-project` with no `?month=`) falls through the generic `RuntimeException` catch-all (it isn't one — `MissingServletRequestParameterException` descends from `ServletException`, not `RuntimeException`), reaches Spring's `DefaultHandlerExceptionResolver`, and triggers an internal forward to `/error`; since `/error` isn't excluded from `.anyRequest().authenticated()` in `SecurityConfig` and `JwtFilter` skips error dispatches by default, that forward gets rejected as unauthenticated, returning a misleading `401` instead of `400`. Confirmed via Postman on 2026-07-14 while building Step 6 (Reports) *(Effort: Small)* — ✅ done 2026-07-14: added the handler using `e.getParameterName()` for a specific message; full mechanism documented in `notes/spring-boot/en/05-exception-handling.md`
- [x] **[Medium]** — Add an `@ExceptionHandler(MethodArgumentTypeMismatchException.class)` in `GlobalExceptionHandler` returning 400 — an invalid `@RequestParam` value that fails type conversion (e.g. `?month=2025-13`, not a valid `YearMonth`) is a `RuntimeException`, so it *was* reaching the generic catch-all, but silently with the wrong status (`500` instead of `400`) since no specific handler existed. Confirmed via Postman on 2026-07-14 alongside the bug above *(Effort: Small)* — ✅ done 2026-07-14: added the handler using `e.getParameter().getParameterName()` for a specific message

<!-- Tasks above this line predate per-tier tagging (2026-07-14); all of them are [backend]. -->

### Added by the backend review of 2026-07-14

#### High

- [x] **[High]** `[backend]` — Close the REJECTED branch of the workflow in `TimeEntryService`: PLANNING §8's diagram has `REJECTED ──→ (employee can edit and resubmit)`, but `update()` throws "You can only update DRAFT entries" and `submit()` throws "Employee can only submit DRAFT entries", so a rejected entry can never re-enter the flow — it is a dead end. Allow `update()` on DRAFT **or** REJECTED (resetting status to DRAFT and clearing `rejectionNote`), then fix §8, whose prose bullets ("edit/submit DRAFT only") currently contradict its own diagram *(Effort: Medium)* — ✅ done 2026-07-22: implemented as the dedicated `PATCH /api/entries/{id}/reopen` endpoint PLANNING §10/§17 already specified (owner-only, `REJECTED → DRAFT`, clears `rejectionNote`) rather than widening `update()` — `TimeEntryService.reopen()` + `TimeEntryController` + Postman-verified (owner reopen succeeds, non-owner 403, wrong source status rejected). Surfaced and fixed a related gap in the same pass: every state-machine guard in `TimeEntryService` (`submit`, `approve`, `reject`, `update`, `delete`, and the new `reopen`) was throwing `BusinessRuleViolationException` → 400 for what is actually a state conflict; split into a new `InvalidStateTransitionException` → 409, keeping `BusinessRuleViolationException` → 400 only for input-data rules (hours range, future date, inactive project). Documented in `notes/spring-boot/en/05-exception-handling.md` (+ `es/05-manejo-excepciones.md`)
- [x] **[High]** `[backend]` — Wrap the token parsing in `JwtFilter` (`extractUsername` at :39, `loadUserByUsername` at :42) in a `try/catch` for `JwtException` + `UsernameNotFoundException`, and on failure call `filterChain.doFilter(...)` without setting the context so `JwtAuthenticationEntryPoint` returns 401. Trigger: any expired (24h), tampered or malformed Bearer token → jjwt throws *inside the servlet filter*, where `@RestControllerAdvice` never sees it → client gets **500 instead of 401**, and the Angular interceptor (Step 7) will have no 401 to react to *(Effort: Small)* — ✅ done: verified on the 2026-07-17 review — `JwtFilter:42-55` now wraps `extractUsername`/`loadUserByUsername`/`isValid` in `catch (JwtException | UsernameNotFoundException)`, logs a warning and continues without setting the context → `JwtAuthenticationEntryPoint` returns 401
- [x] **[High]** `[backend]` — Enforce the "inactive users cannot log in" rule (PLANNING §8) by chaining `.disabled(!user.isActive())` on the `UserDetails` builder in `UserDetailsServiceImpl:23-27`, and add a `DisabledException` handler returning the same generic 401 as `BadCredentialsException` (no user enumeration). Today `active` is never checked, so a deactivated user logs in normally **and** keeps full access with an already-issued token — soft delete revokes nothing. Found independently by 5 of the 9 review slices *(Effort: Small)* — ✅ done 2026-07-22: `UserDetailsServiceImpl.loadUserByUsername` now chains `.disabled(!user.isActive())`; new `@ExceptionHandler(DisabledException.class)` in `GlobalExceptionHandler` returns the same generic 401 body as `BadCredentialsException` ("Invalid email or password"), no enumeration. Verified via Postman: deactivating a user's `active` flag in pgAdmin and attempting login returns 401 with the generic message; reactivating restores login
- [x] **[High]** `[backend]` — Filter the two report aggregates in `TimeEntryRepository:17-31` by `status`: they filter on `date` only, so `GET /api/reports/by-employee?month=…` sums DRAFT and REJECTED hours as if they were real worked hours. Decide the reporting status explicitly (APPROVED only, or APPROVED + SUBMITTED) — the whole DRAFT→SUBMITTED→APPROVED state machine exists so the manager's numbers can be trusted, and this silently defeats it *(Effort: Small)* — ✅ done 2026-07-22: decided `APPROVED` only (a report should only count hours a manager has actually signed off on); both `getHoursByProject` and `getHoursByEmployee` add `AND te.status = com.victor.timetrack.model.EntryStatus.APPROVED` as a JPQL enum literal (fixed rule, not a caller-supplied param). Verified via Postman + pgAdmin cross-check: DRAFT/REJECTED/SUBMITTED entries correctly excluded from both reports' totals. Documented in `notes/spring-boot/en/04-spring-data-jpa.md` (+ `es/04-spring-data-jpa.md`)
- [x] **[High]** `[backend]` — Add `@PreAuthorize("hasRole('EMPLOYEE')")` to `POST /api/entries`, `PUT /api/entries/{id}`, `DELETE /api/entries/{id}` and `PATCH /api/entries/{id}/submit` — PLANNING §10 marks all four "Employee only", but none carries a role check, so a MANAGER token can create/edit/delete/submit entries. `approve`/`reject` *are* guarded, so half the resource's role contract is enforced and half is not *(Effort: Small)* — ✅ done 2026-07-22: added to `create`, `submit`, `update`, `delete`, and also `reopen` (added after this task was written, same gap applied to it). Verified via Postman: all five return 403 with a MANAGER token; `approve`/`reject` still correctly reject an EMPLOYEE token; EMPLOYEE token still succeeds on all five
- [x] **[High]** `[backend]` — Apply the same role/active filter in `ProjectService.getById` (:37) that `getAll()` already applies: an EMPLOYEE can read any **inactive** project by guessing its id, because the "employees see active projects only" rule is enforced on the list endpoint but not on the detail endpoint. Throw `ResourceNotFoundException` (404, not 403 — do not confirm the project exists). Classic broken object-level authorization: the filter on the collection, forgotten on the item *(Effort: Small)* — ✅ done 2026-07-22: `getById` now looks up the project first, computes `isManager` the same way `getAll()` does, and throws `ResourceNotFoundException` when `!isManager && !project.getActive()`. Verified via Postman: MANAGER sees an inactive project (200), EMPLOYEE gets 404 on the same id, EMPLOYEE still sees an active project (200), and a genuinely nonexistent id returns the identical 404 body. Documented as BOLA in `notes/security/en/05-security-vulnerabilities.md` (+ `es/05-security-vulnerabilities.md`)
- [x] **[High]** `[backend]` — Add the missing query filters to `GET /api/entries` — PLANNING §10 specifies `?month=`, `?projectId=`, `?status=` and `?userId=` (manager only), and the §14 dashboards are designed around `?month=` and `?status=SUBMITTED`. Today the endpoint accepts no params at all and returns everything. Step 7 (the current step) cannot build the planned dashboards without this *(Effort: Medium)* — ✅ done (feature): verified on the 2026-07-17 review — `TimeEntryController.findByFilter` + `TimeEntryService.findByFilter` accept all four params, the manager-only `?userId=` is enforced (an employee's own id from `SecurityContextHolder` always overwrites the query param), and the JPQL query is injection-clean (named `:params`, no concatenation). ⚠️ Implemented as JPQL, **not** the `Specification<TimeEntry>` approach this note previously described — see the reconciliation task below.
- [x] **[High]** `[backend]` — Implement `GET /api/reports/summary?month=` (total hours, total entries, approved vs pending) — it is in PLANNING §10 and both the Manager dashboard ("Total this mo.") and the Reports page stat cards consume it, yet it does not exist even though Step 6 is marked ✅. Either build it now or move it explicitly to Step 7 and amend PLANNING so the frontend is not coded against a missing route *(Effort: Small)* — ✅ done 2026-07-22: new `ReportSummaryResponse` DTO (`totalHours`, `totalEntries`, `approvedHours`, `pendingHours`); `ReportService.getSummary` fetches the month's entries via `Specification.dateBetween`, then computes the four fields with `filter → map → reduce` streams over `BigDecimal` — `approvedHours`/`pendingHours` sum by status, `totalHours = approvedHours.add(pendingHours)`, `totalEntries` counts APPROVED+SUBMITTED only (same "trusted hours" convention as the other two reports); `GET /api/reports/summary` added to `ReportController`, MANAGER-only. Verified via Postman against pgAdmin data, including an empty month (all zeros, no exception). Documented `reduce` with a worked trace in `notes/java/en/09-streams-lambdas.md` (+ `es/09-streams-lambdas.md`)
- [x] **[High]** `[backend]` — Build the missing user-management endpoints (`POST /api/users`, `PUT /api/users/{id}`, `DELETE /api/users/{id}` soft delete), all `@PreAuthorize("hasRole('MANAGER')")` per PLANNING §10 "Users (Manager only)". Only `GET /api/users` exists, so the only way to create an employee today is editing `data.sql` by hand and the app cannot be demoed end to end. `create` must BCrypt-hash the password and map a duplicate email to a 409 *(Effort: Medium)* — ✅ done 2026-07-23: `CreateUserRequest`/`UpdateUserRequest` DTOs (name/email/password/role on create, name/email/role on update — password change out of scope, left for a future dedicated endpoint); `UserService.create` hashes the password with the existing `PasswordEncoder` bean and checks `findByEmail` before saving; `update` re-checks the email only when it changed (so re-saving the same email doesn't false-positive as a duplicate) and requires an explicit `save()` since the project has no `@Transactional` yet (no dirty-checking); `delete` is a soft delete (`active = false`), reusing the same pattern as `ProjectService`. Duplicate email on both `create` and `update` throws `DataIntegrityViolationException` → the existing 409 handler. Verified via Postman: full happy path + 401/403/404/409/400 (including multi-field validation accumulating all errors) on all three endpoints, a newly-created user can log in with BCrypt round-tripping correctly, and a soft-deleted user gets the generic "Invalid email or password" 401 on login (no enumeration, per the earlier `DisabledException` task) instead of a specific "disabled" message
- [x] **[High]** `[backend]` — Fix the `User.active` default in `User.java:30-31`: the field is a primitive `boolean` (defaults to `false`) and `@ColumnDefault("true")` never applies, because Hibernate always includes the column in the INSERT. Any user persisted from Java without an explicit `setActive(true)` is saved `active = false` and — once the `.disabled()` task above lands — can never log in. Dormant today only because the sole user comes from `data.sql`, which sets `active` explicitly. Initialise the field (`private boolean active = true;`) and add `@Column(nullable = false)` *(Effort: Small)* — ✅ done 2026-07-23: `User.active` now `private boolean active = true;` with `@Column(nullable = false)` alongside the existing `@ColumnDefault("true")` — the Java-side default covers rows created through Hibernate, the DB-level `NOT NULL` covers rows written by raw SQL. Verified in pgAdmin (`information_schema.columns`: `is_nullable = NO`, `column_default = true`) and confirmed the app starts cleanly against existing `data.sql` rows (no `contains null values` error, since the seed already sets `active` explicitly). Documented the two-doors distinction in `notes/spring-boot/en/04-spring-data-jpa.md` (+ `es/04-spring-data-jpa.md`)
- [x] **[High]** `[backend]` — Stop committing a default admin: `data.sql` seeds `manager@timetrack.com` with a fixed BCrypt hash and re-runs on every boot (`spring.sql.init.mode=always`), so the hash is public in git, offline-crackable, and the account would ship to every future environment including Docker (Step 11). Move the seed behind a `dev`/`local` profile and take the password from an env var *(Effort: Small)* — ✅ done 2026-07-23: deleted `data.sql` and `spring.sql.init.mode=always`; replaced with a `DataInitializer implements CommandLineRunner` annotated `@Profile("dev")`, so the seed bean is not even instantiated outside `dev`. The admin email/name/password come from `application-dev.properties` (`app.admin.*`), with the password read from the `ADMIN_PASSWORD` env var (`${ADMIN_PASSWORD}`) — the BCrypt hash is now generated in Java at runtime via the existing `PasswordEncoder` bean, never stored in git. The runner is idempotent (`findByEmail(...).isPresent()` guard, replacing the old SQL `ON CONFLICT DO NOTHING`). Verified via Postman: with `SPRING_PROFILES_ACTIVE=dev` the app seeds the manager on an empty DB and login returns a JWT; the stored hash differs from the old public one

#### Medium

- [x] **[Medium]** `[backend]` — Add `@Transactional` to the write methods of `TimeEntryService` / `ProjectService` / `UserService` and `@Transactional(readOnly = true)` to the read and report methods. There is currently **no `@Transactional` anywhere in the codebase**: every repository call runs in its own auto-commit transaction, so the read-then-write methods (`update`, `submit`, `approve`, `reject`) have no atomic boundary. No wrong output today (each method is a single write), but it is the most-asked persistence question in Spanish junior interviews — and it is a hard prerequisite for the N+1 fix below *(Effort: Small)* — ✅ done 2026-07-28: `@Transactional(readOnly = true)` on every pure-read method (`ProjectService.getAll/getById`, `UserService.getAll`, `TimeEntryService.findByFilter`, all three `ReportService` methods); `@Transactional` on every write method across `ProjectService`, `UserService`, `TimeEntryService` (`create`, `update`, `delete`, plus `submit`/`approve`/`reject`/`reopen` on entries). Verified via Postman: happy path of create/update/delete unchanged on projects, users and entries.
- [ ] **[Medium]** `[backend]` — Fix the N+1 on `GET /api/entries`: `TimeEntry.user` and `TimeEntry.project` are `@ManyToOne`, which defaults to `FetchType.EAGER`, so listing N entries fires 1 query + up to 2N extra selects. Set both to `FetchType.LAZY` and add a fetch-join (`@EntityGraph` or `JOIN FETCH`) for `findAll()`/`findByUser()`. **Depends on the `@Transactional` task above** — with `open-in-view=false` and no transaction, `toResponse()` calling `getUser().getName()` on a lazy proxy throws `LazyInitializationException` *(Effort: Medium)*
- [ ] **[Medium]** `[backend]` — Group the report aggregates by id, not by display name: `TimeEntryRepository` does `GROUP BY te.project.name` / `te.user.name`, so two employees called "Ana García" are merged into one row and a rename splits history. Group by `te.user.id, te.user.name` and expose the id in the projection — the frontend needs it as a row key anyway *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Constrain `TimeEntry.hours` to the planned `DECIMAL(4,2)`: the entity column has no precision/scale and the DTO has no range annotations, so `hours: 7.123456` passes the service's 0.5–24 check and is persisted at the DB's default scale — the stored value silently differs from what the user typed and every report `SUM` drifts with it. Add `@Column(precision = 4, scale = 2)` on the entity and `@DecimalMin("0.5") @DecimalMax("24") @Digits(integer = 2, fraction = 2)` on `CreateTimeEntryRequest` *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Make `Project.active` a primitive `boolean` with `@Column(nullable = false)` + `@ColumnDefault("true")`: it is a nullable `Boolean` with a Java-side default only, so a row inserted outside JPA with `active = NULL` makes `TimeEntryService.create()`'s `if (!project.getActive())` throw an unboxing NPE → a **500 "Internal server error" on a normal create request** instead of a clean business error *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Enforce the duplicate-project-name rule in `ProjectService.create/update` with `existsByName(...)` + `BusinessRuleViolationException`. PLANNING §16 plans a test for "duplicate name → 409", but the rule is not in the service at all: it only surfaces as a DB `DataIntegrityViolationException` mapped to a vague "A resource with this value already exists". Business rules belong in the service layer, and this one cannot be unit-tested where it currently lives *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Decide and implement the reactivate path for soft-deleted projects: `ProjectService.update()` neither blocks editing an inactive project nor can restore one, so `active = false` is a one-way door whose only exit is a manual DB edit. Either add `active` to `UpdateProjectRequest` (manager-only reactivate) or reject updates on inactive projects *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Decide the self-approval rule: a MANAGER with their own SUBMITTED entry can approve or reject it (`TimeEntryService:98,113` check the role but never that the entry's owner ≠ the caller). Not listed in §8, so this is a **decision, not yet a violation** — either block it (`UnauthorizedException` when owner == caller) or state in §8 that managers do not log hours. Segregation of duties is the standard follow-up question on any approval workflow *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Set `@GeneratedValue(strategy = GenerationType.IDENTITY)` on `User`, `Project` and `TimeEntry`: all three use a bare `@GeneratedValue`, which resolves to `AUTO`, and Hibernate 6 then picks a **sequence** generator rather than the DB auto-increment/identity the §7 entity tables specify *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Replace Lombok `@Data` on the three JPA entities with `@Getter`/`@Setter` + `@EqualsAndHashCode(onlyExplicitlyIncluded = true)` on the id and `@ToString(exclude = {"user","project"})`. `@Data` generates `equals`/`hashCode` over every field including the DB-generated id and the `@ManyToOne` associations, so an entity put in a `HashSet` before flush becomes unreachable once `id` is assigned, and `toString()` walks the relations. "Why is `@Data` on an entity wrong?" is asked directly at consultancies *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Add the missing DB-level not-null constraints the plan requires but `ddl-auto=update` never created: `@Column(nullable = false)` on `User.name`, `User.role`, `User.active`, `TimeEntry.status`, `Project.active`. The Java-side defaults protect only rows the app creates; the DB constraint is what actually protects the invariant the whole state machine rests on *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Add the missing `createdAt` field to `User` (`@CreationTimestamp`, not null) — §7 requires it and both `Project` and `TimeEntry` have it, so `User` is the lone outlier *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Bound every string and numeric field on the request DTOs with `@Size` / `@Email`: `LoginRequest` (no `@Email`, no max length — an arbitrarily long password hits BCrypt on every unauthenticated request), `CreateProjectRequest`/`UpdateProjectRequest` (`description` has no constraint at all; a `name` over 255 chars produces a **500 instead of a 400**), `CreateTimeEntryRequest.description` and `RejectRequest.rejectionNote` (both overflow `varchar(255)` and surface as a misleading 409). Validation must fail at the edge, not at the persistence layer *(Effort: Medium)*
- [ ] **[Medium]** `[backend]` — Log the exception in `GlobalExceptionHandler`'s catch-all `@ExceptionHandler(RuntimeException.class)` (:105-111) before returning the generic 500. It currently swallows the stack trace with **no logging anywhere in the app**, so any unexpected bug is invisible — undebuggable in a demo. The generic body sent to the client is correct; the missing half is the detail sent to the log *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Make JWT validation explicit in `JwtUtil.isValid` (:37-43): it compares only the subject, and signature/expiry are verified merely as a side effect of `parseClaims` throwing. Parse once, then check the subject **and** `getExpiration().after(new Date())`, returning `false` on any `JwtException`. (Its `catch (JwtException)` is dead code today because `extractUsername` already threw in the filter — see the High above.) "How do you know the token is not expired or tampered with?" must be answered by a deliberate check *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Cut `app.jwt.expiration` from 86400000 (24h) to 15–60 min, and document the access/refresh trade-off in the README even though refresh tokens stay out of scope. A token stolen from `localStorage` is currently valid for a full day *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Decide what the reports do with soft-deleted projects and users: the aggregates filter on neither, so a project deactivated mid-month still appears as a report row with no `active` flag for the client to interpret. Keeping them is defensible (the hours *were* worked) — but return the flag in the projection so the behaviour is chosen, not accidental *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Introduce a separate `UpdateTimeEntryRequest` DTO: `PUT /api/entries/{id}` reuses `CreateTimeEntryRequest`, while the projects resource has the `CreateProjectRequest`/`UpdateProjectRequest` pair. Same problem, two solutions — pattern inconsistency reads as junior even when each half works *(Effort: Small)*

#### Low

- [ ] **[Low]** `[backend]` — Extract a shared `currentUser()` / `isManager()` helper: the `SecurityContextHolder` → `findByEmail` lookup is copy-pasted across five `TimeEntryService` methods, `Objects.requireNonNull(getAuthentication())` appears six times, and `"ROLE_MANAGER"` is a magic string in both `TimeEntryService` and `ProjectService` — compare against the `Role` enum instead. Repetition in the one place that decides *who the caller is* is exactly where auth bugs hide *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Close the entry-id existence oracle: `TimeEntryService` returns 404 for a non-existent id but 403 for another user's id, so an EMPLOYEE can enumerate which entry ids exist across the whole table. Return 404 for both — treat a non-owned entry as not found *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Set CORS `allowCredentials(false)` in `SecurityConfig:65-68` (auth travels in the `Authorization` header, not cookies, so credentialed CORS buys nothing and only locks the config into the stricter rules) and move the hardcoded `localhost:4200` origin into an `app.cors.allowed-origins` property so a deployed frontend does not need a recompile *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Mint the JWT from the authenticated principal, not the request body: `AuthService:21-24` discards the `Authentication` returned by `authenticationManager.authenticate(...)` and passes `request.getEmail()` to `generateToken`. Not exploitable today (`findByEmail` is an exact match), but taking the subject from unvalidated input rather than the verified identity is the habit that becomes a bug elsewhere *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Rename `UnauthorizedException`: it is mapped to **403 Forbidden** in `GlobalExceptionHandler:66-71` while its name says 401. A name that lies about its status is small but visible *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Add `ORDER BY SUM(te.hours) DESC` to both report queries — §14 shows both tables sorted by hours descending, and without it the row order is whatever Postgres returns, pushing the sort onto Angular and making the endpoint non-deterministic to test *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Replace the magic status codes with the enum: `ResponseEntity.status(200)` in `TimeEntryController` (5×) and `ReportController` (2×) should be `.ok(...)`, and `ResponseEntity.status(201)` in `ProjectController:36` should be `HttpStatus.CREATED`. `noContent()` is already used correctly on delete, so the classes are internally inconsistent *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Decide the contract for `GET /api/users`: `UserService.getAll()` returns every user including soft-deleted ones, unpaginated. Returning inactive users to a manager is defensible per §10, but the Team page needs the `active` flag surfaced clearly — and the unbounded `findAll()` wants a `Pageable` once the table grows *(Effort: Small)*

### Added by the backend re-review of 2026-07-17

Forced re-review (backend reviewed 3 days earlier) requested after PLANNING was refined. It re-confirmed every open task above and closed two Highs (JWT filter, `GET /api/entries` filters). New findings below.

#### Medium

- [x] **[Medium]** `[backend]` — Reconcile the `GET /api/entries` filter approach: the on-disk `TimeEntryRepository.findByFilters` (:15-24) is the JPQL `(:param IS NULL OR …)` pattern — the same one PLANNING §3 (line 48), §5 (lines 779, 782) and the "in progress" note above document as **abandoned** in favour of `Specification<TimeEntry>` + `JpaSpecificationExecutor` (recorded there as the concept learned, with the `42P18` PostgreSQL bug as the reason). The code and the refined plan now disagree. Pick one and make them match: **either** (a) implement the filter with `Specification<TimeEntry>` as the plan documents — so the code demonstrates the skill the plan claims Victor learned — **or** (b) if JPQL is the final decision, first verify via Postman that `GET /api/entries?month=…` (and with each param individually null) runs without the `42P18` "could not determine data type of parameter" error the note attributes to this exact `:start IS NULL OR :end IS NULL OR te.date BETWEEN :start AND :end` pattern, then rewrite §3/§5 and this note to describe the JPQL approach and drop the `Specification` claim. As-is, an interviewer asking to see the `Specification<T>` usage the plan advertises would find none *(Effort: Medium)* — ✅ done 2026-07-22: option (a) — replaced `findByFilters`/`@Query` with `TimeEntrySpecifications` (one static factory per filter, `cb.conjunction()` when null) + `TimeEntryRepository extends JpaSpecificationExecutor<TimeEntry>` + `TimeEntryService.findByFilter` building the combined `Specification` via `.where(...).and(...)` and calling `findAll(spec)`. Verified via Postman: single filters, combined filters, employee-own-userId enforcement (manager-supplied `userId` on an employee token is still overwritten), 400s on invalid `status`/`month`/`userId` unchanged, and a nonexistent `userId` returns `200 []` with no `42P18`. Documented in `notes/spring-boot/en/14-specifications-criteria-api.md` (+ `es/14-especificaciones-criteria-api.md`)

#### Low

- [ ] **[Low]** `[backend]` — Consistency: `UserController.getAll()` (:22-25) returns a raw `List<UserResponse>` while every other controller method wraps its body in `ResponseEntity<T>` (`AuthController`, `ProjectController`, `TimeEntryController`, `ReportController`). Spring wraps a bare return value in a 200 identically, so this is behaviourally equivalent — a pure style outlier — but the resource layer should return `ResponseEntity` uniformly. Pairs with the magic-status-code Low above (same `ResponseEntity`-hygiene theme) *(Effort: Small)*

### Added by the backend re-review of 2026-07-23

Full backend review (all 12 slices + the cross-slice consistency pass) run after the `fix/backend-backlog` branch closed ~10 Highs on 2026-07-22/23 — the "gate measures unreviewed code" rule applied, since the user CRUD, `reports/summary`, `reopen`, the `Specification<T>` refactor and the role checks had never been reviewed. Every open task above was re-confirmed; the closed Highs were verified fixed on disk. New findings below.

#### High

- [x] **[High]** `[backend]` — Complete the login response: `POST /api/auth/login` returns `AuthResponse` carrying only `token` (`AuthResponse.java:11`, `AuthService.login` at :20-25), but PLANNING §10 documents the body as `token, name, role` and the §14 dashboards + Step 7a role-based routing (`/projects` as EMPLOYEE must redirect away) need the role. Load the authenticated `User` after `authenticationManager.authenticate(...)` and populate `name` + `role` on the DTO — fixing this also lets you mint the token from the returned `Authentication` (see the existing Low), closing two findings at once. Without it the frontend has no role to route on when Step 7a starts *(Effort: Small)* — ✅ done 2026-07-28: `AuthResponse` now carries `token, name, role` (`role` typed as the `Role` enum, matching `UserResponse`'s convention); `AuthService.login` captures the `Authentication` returned by `authenticationManager.authenticate(...)` and mints the token from `authentication.getName()` instead of the raw `request.getEmail()` (closes the related Low), then loads the full `User` via `userRepository.findByEmail(authentication.getName())` to populate `name`/`role`. Verified via Postman: login as MANAGER and as EMPLOYEE both return the correct role.

#### Medium

- [ ] **[Medium]** `[backend]` — Enforce "cannot submit entries for an inactive project" (§8) in `TimeEntryService.submit` (:76-97): `create` and `update` both check `project.getActive()`, but `submit` checks only ownership + DRAFT status, so an entry drafted while its project was active can be submitted after the manager soft-deletes the project — the deactivated project silently re-enters the approval pipeline. Re-check `timeEntry.getProject().getActive()` in `submit` and throw `BusinessRuleViolationException`, or document in §8 that the check is create/update-time only *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Reconcile the user-management DTOs with PLANNING §10: `CreateUserRequest` requires a client-supplied `password` (`CreateUserRequest.java:18-19`) while §10 says the body is `name, email, role` and the account is "created with the default password" (§8: `Timetrack2024!`, `mustChangePassword` skipped for MVP); and `UpdateUserRequest` carries `email` (+ the service's email-change 409) while §10 says the update body is `name, role` only. Pick one per endpoint and make code and plan agree — either implement the default-password flow §8/§10 describe, or update §10/§8/§17 to document the manager-supplies-password + email-change decisions. As-is, an interviewer comparing the plan to the endpoint sees drift on a resource that was just built *(Effort: Medium)*
- [ ] **[Medium]** `[backend]` — Add a password policy on the sole account-creation path: `CreateUserRequest.password` is only `@NotBlank`, so a manager can create an account with a 1-character password and there is no password strength rule anywhere in the system. Add `@Size(min = 8, max = 72)` (72 = BCrypt's truncation boundary, worth documenting). Ties into the DTO-reconciliation task above — if the default-password flow is chosen instead, this collapses into that decision *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Fix the by-employee projection field name: `EmployeeHoursReportResponse.getEmployeeName()` (:6) produces a JSON field `employeeName`, but PLANNING §10's by-employee row documents `userName` (and `ProjectHoursReportResponse.projectName` correctly matches its §10 row). The Step 7c Reports table will bind to the documented `userName` and read `undefined`. Rename the getter + its JPQL `AS` alias to `userName`, or amend §10 to `employeeName` — pick one *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Make the report totals reconcile: `ReportService.getSummary` (:47-61) computes `totalHours` = APPROVED + SUBMITTED(pending) and `totalEntries` over APPROVED + SUBMITTED, but `getHoursByProject`/`getHoursByEmployee` count **APPROVED only** (the 2026-07-22 status-filter fix). A manager summing the by-project rows will not match the summary card for the same month, with no documented reason. Decide the rule (e.g. all aggregates APPROVED-only, with pending shown as a separate summary breakout) and make the three queries consistent, then record it in §8/§10 *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Apply the loaded `UserDetails` account-status check in `JwtFilter` (:47-51): the filter already calls `loadUserByUsername` (which sets `disabled(!active)`) but then builds the `UsernamePasswordAuthenticationToken` directly, ignoring `isEnabled()`. A user soft-deleted (`active = false`) *after* their token was issued keeps full access for the token's remaining lifetime (up to 24h), so the login-time inactive check is bypassed on every subsequent request. Run the loaded `UserDetails` through an account-status check (e.g. `new AccountStatusUserDetailsChecker().check(userDetails)`) before setting the context — the DB lookup already happens, so the fix is cheap. Compounds with the "cut token expiry to 15–60 min" Medium above *(Effort: Small)*
- [ ] **[Medium]** `[backend]` — Extract the duplicated entry validation in `TimeEntryService`: the three business checks (future date, project active, hours 0.5–24) are copy-pasted verbatim in `create` (:40) and `update` (:163), including re-instantiating `new BigDecimal("0.5")`/`("24")` each time. Pull them into a private `validateEntryData(request, project)` used by both, and hoist the bounds to `static final BigDecimal MIN_HOURS`/`MAX_HOURS`. The submit-inactive-project gap above is exactly the divergence this duplication invites *(Effort: Small)*

#### Low

- [ ] **[Low]** `[backend]` — Exclude the password from `LoginRequest`'s generated `toString`: `@Data` makes `toString()` include the plaintext `password`, so the day any request-logging or a framework body-dump is added, credentials land in the logs. No logger stringifies it today (latent), but the fix is one annotation — add `@ToString.Exclude` on `password` (or split `@Data` into `@Getter`/`@Setter`) *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Signal duplicate email with a domain exception in `UserService.create` (:31-35): it throws `DataIntegrityViolationException` manually — a Spring DAO exception meant for DB-layer violations used as a business signal. It maps to 409 correctly, but a dedicated `BusinessRuleViolationException`/`DuplicateEmailException` reads as the intended layered pattern rather than a workaround *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Unify enum-status comparison in `TimeEntryService`: `submit`/`reopen`/`create` compare status with `==`/`!=` while `approve`/`reject`/`update` use `.equals()`. Both are correct for enums; pick one (`==` is idiomatic and null-safe) and apply it throughout *(Effort: Small)*
- [ ] **[Low]** `[backend]` — Remove the now-vestigial `spring.jpa.defer-datasource-initialization=true` from `application.properties`: it only mattered while `data.sql` seeded on startup, and that was replaced by the profile-gated `DataInitializer` on 2026-07-23. Harmless but dead config *(Effort: Small)*
